---
title: Can Thinking Models Think to Detect Hateful Memes?
authors: "Mohamed Bayan Kmainasi, Mucahid Kutlu, Ali Ezzat Shahroor, Abul Hasnat, Firoj Alam"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01225v1"
tags: ["query:q7"]
score: 9.0
evidence: 利用思考型多模态大模型推断模因中的有害意图
tldr: 通过强化学习提升多模态大模型的推理能力，以检测多模态模因中的有害意图。
selection_source: fresh_fetch
motivation: 利用思考型多模态大模型推断模因中的有害意图。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
仇恨模因通常需要组合式多模态推理：图像和文本在孤立状态下可能显得无害，但它们的交互却传达了有害意图。尽管基于思考的多模态大语言模型（MLLMs）最近在视觉-语言理解方面取得了进展，但它们在仇恨模因分析方面的能力仍有待探索。我们提出了一种基于强化学习的训练后框架，通过任务特定的奖励和一种新颖的群体相对策略优化（GRPO）目标，提升了基于思考的 MLLMs 的推理能力。具体而言，我们（i）对现有的 MLLMs 在仇恨模因理解方面进行了系统的实证研究，（ii）通过蒸馏生成弱监督或伪监督的思维链（CoT）原理解释，扩展了现有的仇恨模因数据集，以及（iii）引入了一种基于 GRPO 的目标，共同优化模因分类和解释质量，以鼓励细粒度的逐步推理。在 Hateful Memes 基准测试上的实验表明，我们的方法达到了最先进的性能，准确率和 F1 分数提高了约 1%，解释质量提高了约 3%。我们将公开发布代码、数据集扩展和评估资源，以支持可复现性。

## 速览
**TLDR**：本研究探讨了思维型多模态大模型（MLLM）在仇恨模因检测中的应用。针对模因需结合图文进行复杂推理的特点，作者提出一种基于强化学习的后训练框架。该框架通过蒸馏生成思维链（CoT）扩展数据集，并采用改进的GRPO算法联合优化分类准确性与解释质量。实验证明，该方法在Hateful Memes基准上实现了SOTA性能，不仅提升了检测精度，还显著增强了模型生成细粒度推理过程的能力。 \
**Motivation**：仇恨模因的识别依赖于图像与文本间的复杂多模态推理，而现有思维模型在处理此类隐晦有害信息时的潜力尚未被充分挖掘。 \
**Method**：提出一种基于强化学习的后训练框架，利用GRPO算法和蒸馏生成的思维链（CoT）来联合优化模因分类精度与解释质量。 \
**Result**：在Hateful Memes基准测试中取得SOTA性能，准确率和F1分数提升约1%，解释质量提升约3%。 \
**Conclusion**：通过任务特定的奖励机制和思维链训练，可以有效提升多模态模型在复杂社交媒体内容分析中的推理与检测能力。

---

## Abstract
Hateful memes often require compositional multimodal reasoning: the image and text may appear benign in isolation, yet their interaction conveys harmful intent. Although thinking-based multimodal large language models (MLLMs) have recently advanced vision-language understanding, their capabilities remain underexplored for hateful meme analysis. We propose a reinforcement learning based post-training framework that improves reasoning in thinking-based MLLMs through task-specific rewards and a novel Group Relative Policy Optimization (GRPO) objective. Specifically, we (i) conduct a systematic empirical study of off-the-shelf MLLMs for hateful meme understanding, (ii) extend an existing hateful meme dataset by generating weakly or pseudo-supervised chain-of-thought rationales via distillation, and (iii) introduce a GRPO-based objective that jointly optimizes meme classification and explanation quality to encourage fine-grained, step-by-step reasoning. Experiments on the Hateful Memes benchmark show that our approach achieves state-of-the-art performance, improving accuracy and F1 by approximately 1 percent and explanation quality by approximately 3 percent. We will publicly release our code, dataset extensions, and evaluation resources to support reproducibility.

---

## 论文详细总结（自动生成）

这是一份关于论文《Can Thinking Models Think to Detect Hateful Memes?》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：探讨“思考型”多模态大语言模型（Thinking MLLMs）在处理复杂的仇恨模因（Hateful Memes）时的推理能力。
*   **研究动机**：模因的有害性通常具有隐晦性，图像和文本单独看可能无害，但结合后会产生有害意图。现有的多模态模型虽然在通用任务上表现优异，但在需要多步推理、结合文化背景和细粒度分类的仇恨模因检测领域，其“思考”机制（如思维链 CoT）的潜力尚未被充分挖掘。

### 2. 方法论
论文提出了一种基于强化学习（RL）的后训练框架，核心流程如下：
*   **数据增强（CoT 蒸馏）**：利用强商业模型（GPT-4.1）作为教师模型，针对现有数据集生成逐步推理的思维链（CoT）原理解释，构建伪监督信号。
*   **两阶段训练管线**：
    1.  **SFT 预热（Supervised Fine-Tuning）**：使用包含二分类标签、细粒度属性（受保护类别、攻击类型）和蒸馏 CoT 的数据对模型进行监督微调，使模型对齐输出格式。
    2.  **GRPO 优化（Group Relative Policy Optimization）**：引入 DeepSeek-R1 中使用的 GRPO 算法。该算法通过采样一组候选输出并计算相对奖励来优化模型，无需额外的奖励模型（Reward Model）。
*   **奖励函数设计**：定义了四个维度的组合奖励：
    *   **格式奖励 ($R_{fmt}$)**：确保模型遵循 `<think>` 标签结构。
    *   **标签奖励 ($R_{lbl}$)**：预测类别是否正确。
    *   **长度奖励 ($R_{len}$)**：防止解释过于冗长或简短。
    *   **语义奖励 ($R_{met}$)**：利用 METEOR 分数衡量生成解释与参考答案的语义相似度。

### 3. 实验设计
*   **数据集**：主要使用 **Hateful Memes** 基准数据集（约 1.2 万个模因），并结合了 Mathias 等人的细粒度标注和 MemeIntel 的人类验证解释。
*   **对比方法**：
    *   **预训练模型零样本/CoT 测试**：包括 Llama-3.2-11B、Llama-4-Scout-17B、Qwen3-VL-8B（指令版与思考版）、Gemma-3-12B 及 GPT-4.1。
    *   **SOTA 基准**：与近年来在该数据集上取得领先成绩的模型（如 Pro-cap、MemeIntel 等）进行对比。
*   **评估指标**：分类性能（Accuracy, Weighted F1, Macro F1）和解释质量（BERTScore, METEOR）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA H200 GPU**。
*   **软件框架**：采用 DeepSpeed ZeRO-3 进行全参数优化。
*   **训练细节**：SFT 阶段训练 3 个 epoch；GRPO 阶段每条输入采样 16 个生成结果，最大长度 4096 标记。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括：
    *   对 6 种主流 MLLM 在零样本和 CoT 提示下的系统性评测。
    *   针对 SFT 阶段不同监督信号（是否含细粒度标签、是否含 CoT）的消融实验。
    *   针对 GRPO 初始状态（冷启动 vs. SFT 预热）的对比实验。
*   **充分性评价**：实验设计较为全面，涵盖了从基础模型选择到训练策略优化的全过程。通过 LLM-as-a-Judge 验证了蒸馏数据的质量，确保了实验基石的客观性。

### 6. 主要结论与发现
*   **SOTA 性能**：提出的方法在 Hateful Memes 榜单上达到了 **81.2% 的准确率**，超越了之前的最优结果（79.9%）。
*   **CoT 的双刃剑效应**：在零样本测试中，CoT 提示能显著提升分类 F1 值（减少漏报），但往往会导致解释质量指标（BERTScore）下降，因为模型生成的推理过程与简洁的参考答案风格不一致。
*   **强化学习的价值**：GRPO 优化显著提升了模型的决策一致性和解释的语义准确性。
*   **预热的重要性**：冷启动 GRPO 效果较差，必须经过 SFT 预热才能有效利用强化学习提升推理能力。

### 7. 优点与亮点
*   **算法创新**：首次将 GRPO 算法应用于多模态仇恨模因检测，并设计了针对解释质量的语义奖励。
*   **细粒度推理**：通过蒸馏 CoT 和细粒度标签（如攻击类型），使模型不仅能“判断”，还能提供逻辑严密的“理由”。
*   **开源贡献**：承诺发布扩展后的数据集、代码和评估资源，具有很高的复用价值。

### 8. 不足与局限
*   **数据集单一**：实验主要集中在 Hateful Memes 一个数据集上，跨领域和跨语言（如非英语模因）的泛化能力有待验证。
*   **CoT 坍缩风险**：在 GRPO 训练动态分析中发现，模型可能会为了获取奖励而压缩推理长度（CoT collapse），这可能损害推理的深度。
*   **算力成本**：全参数微调和 GRPO 采样对算力要求较高，限制了其在资源受限环境下的部署。
*   **评估局限**：自动评估指标（METEOR/BERTScore）虽能反映语义，但仍无法完全替代人类对推理逻辑正确性的主观判断。

（完）
