---
title: "FactGuard: Agentic Video Misinformation Detection via Reinforcement Learning"
title_zh: FactGuard：通过强化学习实现的智能体视频虚假信息检测
authors: "Zehao Li, Hongwei Yu, Hao Jiang, Qiang Sheng, Yilong Xu, Baolong Bi, Yang Li, Zhenlong Yuan, Yujun Cai, Zhaoqi Wang"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22963v1"
tags: ["query:q6"]
score: 10.0
evidence: 基于强化学习的视频虚假信息检测智能体框架
tldr: 针对多模态大模型在视频谣言检测中推理深度固定且过度依赖内部假设的问题，本文提出FactGuard框架。该框架将验证建模为迭代推理过程，通过评估任务模糊性并主动调用外部工具获取关键证据来优化推理路径。结合领域特定的智能体微调与强化学习策略，FactGuard在FakeSV等多个数据集上达到了SOTA水平，展现了极强的鲁棒性与泛化能力。
selection_source: fresh_fetch
motivation: 现有多模态大模型在处理证据稀疏或需外部验证的视频谣言时，受限于固定推理深度和对内部生成的过度信任。
method: 提出FactGuard框架，通过迭代推理、外部工具调用以及结合监督微调与强化学习的两阶段训练策略来优化决策。
result: 在FakeSV、FakeTT和FakeVV等主流数据集上的实验证明，该方法取得了当前最优的检测性能。
conclusion: 通过将智能体推理与强化学习相结合，FactGuard显著提升了视频谣言检测的准确性、鲁棒性和泛化能力。
---

## 摘要
多模态大语言模型（MLLMs）通过统一的多模态推理，在视频虚假信息检测方面取得了显著进展，但它们通常依赖固定深度的推理，并过度信任内部生成的假设，特别是在关键证据稀疏、碎片化或需要外部验证的情况下。为了解决这些局限性，我们提出了 FactGuard，这是一个用于视频虚假信息检测的智能体框架，它将验证过程建模为基于 MLLMs 的迭代推理过程。FactGuard 显式地评估任务歧义性，并有选择地调用外部工具以获取关键证据，从而实现推理轨迹的逐步优化。为了进一步增强这一能力，我们引入了一种两阶段训练策略，将特定领域的智能体监督微调与决策感知强化学习相结合，以优化工具使用并校准风险敏感型决策。在 FakeSV、FakeTT 和 FakeVV 上的广泛实验证明了 FactGuard 的最先进性能，并验证了其卓越的鲁棒性和泛化能力。

## Abstract
Multimodal large language models (MLLMs) have substantially advanced video misinformation detection through unified multimodal reasoning, but they often rely on fixed-depth inference and place excessive trust in internally generated assumptions, particularly in scenarios where critical evidence is sparse, fragmented, or requires external verification. To address these limitations, we propose FactGuard, an agentic framework for video misinformation detection that formulates verification as an iterative reasoning process built upon MLLMs. FactGuard explicitly assesses task ambiguity and selectively invokes external tools to acquire critical evidence, enabling progressive refinement of reasoning trajectories. To further strengthen this capability, we introduce a two-stage training strategy that combines domain-specific agentic supervised fine-tuning with decision-aware reinforcement learning to optimize tool usage and calibrate risk-sensitive decision making. Extensive experiments on FakeSV, FakeTT, and FakeVV demonstrate FactGuard's state-of-the-art performance and validate its excellent robustness and generalization capacity.

---

## 论文详细总结（自动生成）

以下是对论文《FactGuard: Agentic Video Misinformation Detection via Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：短视频平台（如TikTok）的兴起加速了虚假信息的传播。现有的多模态大语言模型（MLLM）虽然在理解能力上有所提升，但在处理视频谣言检测时存在局限。
*   **核心问题**：
    1.  **固定深度推理**：现有模型多采用单次推理（Single-pass），无法根据任务难度调整思考深度。
    2.  **过度信任内部假设**：在证据稀疏或需要外部验证的情况下，模型容易产生“跨模态幻觉”，即凭空捏造或错误归因视觉/文本证据。
    3.  **缺乏主动验证**：模型无法识别自身知识的边界，难以主动获取外部证据来消除歧义。

### 2. 方法论
FactGuard 将视频虚假信息检测建模为一个**智能体（Agentic）迭代决策过程**。
*   **核心思想**：通过“评估歧义 -> 主动寻证 -> 迭代推理”的闭环，提升检测的准确性与可解释性。
*   **关键技术细节**：
    *   **两阶段推理架构**：第一阶段评估输入信息的充分性并决定是否调用工具；第二阶段结合工具反馈的证据进行最终裁决。
    *   **证据引导行动模块（Tools）**：
        *   **FactProbe**：基于RAG（检索增强生成）的外部知识检索工具，通过搜索引擎获取实时事实证据。
        *   **ClipScout**：针对视频的时间戳检查工具，对关键时间段进行抽帧分析，减少长视频的冗余干扰。
    *   **两阶段训练策略**：
        1.  **智能体监督微调（SFT）**：利用高性能模型（如Qwen2.5-VL-72B）生成高质量的智能体思维链（CoT）数据，引导模型学习结构化推理和工具调用。
        2.  **决策感知强化学习（RL）**：采用 **GRPO（群组相对策略优化）** 算法。
*   **奖励函数设计**：
    *   $R = R_{acc} + R_{format} + \lambda R_{risk} + R_{tool}$
    *   **风险感知奖励（$R_{risk}$）**：通过调整假阳性（FP）和假阴性（FN）的惩罚权重，使模型具备风险敏感性。
    *   **工具奖励（$R_{tool}$）**：仅当工具调用确实辅助了正确决策时才给予奖励，防止模型滥用工具。

### 3. 实验设计
*   **数据集**：使用了三个主流的视频谣言检测基准：**FakeSV**（短视频新闻）、**FakeTT**（TikTok视频）、**FakeVV**（复杂且具歧义的视频）。
*   **对比方法（Baselines）**：
    *   **判别式模型**：BERT, TikTec, FANVM, SV-FEND, FakingRec。
    *   **零样本 MLLMs**：GPT-4o, Gemini2-thinking, GPT-o1-mini, DeepSeek-R1, Qwen2.5-VL (7B/72B), InternVL2.5 等。
    *   **任务对齐推理模型**：Fact-R1。
*   **评估指标**：准确率（Acc）、精确率（Prec）、召回率（Rec）、F1分数。

### 4. 资源与算力
*   **算力配置**：实验在 **8 台 NVIDIA H100 GPU** 上运行。
*   **模型基础**：以 **Qwen2.5-VL-7B** 作为基础模型。
*   **训练细节**：SFT 阶段训练 1 个 epoch；RL 阶段（GRPO）训练 1 个 epoch，每个 prompt 生成 8 条候选轨迹。最大 Prompt 长度为 16,384 tokens。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个不同特性的数据集上进行了全面对比，涵盖了从传统深度学习到最新推理大模型的各类基线。
*   **消融实验**：详细分析了 SFT、RL、工具奖励、风险奖励等组件的贡献，验证了“先微调后强化”策略的必要性。
*   **可解释性分析**：引入 GPT-4o 作为第三方审计员，对模型生成的推理链进行忠实度、一致性和洞察力的量化评分。
*   **风险敏感性分析**：通过改变错误成本比例（$\alpha:\gamma$），展示了模型在不同应用场景下（如高精确度 vs 高召回率）的调节能力。
*   **评价**：实验设计严谨，对比维度丰富，充分证明了方法在性能和鲁棒性上的优势。

### 6. 主要结论与发现
*   **性能领先**：FactGuard 在所有数据集上均显著优于现有 SOTA 方法（如 Fact-R1 和 FakingRec）。
*   **减少幻觉**：通过主动获取外部证据，FactGuard 有效缓解了 MLLM 在面对未知事实时的“一本正经胡说八道”问题。
*   **强化学习的价值**：RL 不仅提升了准确率，更重要的是校准了模型的决策偏好，使其在不确定时更倾向于寻求证据而非盲目猜测。
*   **可解释性增强**：生成的推理轨迹比原生大模型更具逻辑性和证据支撑。

### 7. 优点
*   **智能体化思维**：打破了传统模型的被动推理模式，引入了主动寻证的智能体机制。
*   **风险可控**：通过奖励函数设计，允许开发者根据实际需求调整模型对误报或漏报的敏感度。
*   **工具集成**：FactProbe 和 ClipScout 的设计针对性强，分别解决了文本事实缺失和视频冗余的问题。

### 8. 不足与局限
*   **推理延迟**：由于涉及多轮推理和外部工具调用（如网页检索），其实时检测性能可能受限，不适合超大规模实时流处理。
*   **工具依赖性**：检测效果高度依赖于外部搜索引擎的质量和视频抽帧的代表性。
*   **算力门槛**：虽然使用了 7B 模型，但 RL 阶段的训练（尤其是 GRPO 轨迹生成）仍需要较高的算力支持（H100 集群）。

（完）
