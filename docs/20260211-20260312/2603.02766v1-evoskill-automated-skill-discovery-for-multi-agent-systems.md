---
title: "EvoSkill: Automated Skill Discovery for Multi-Agent Systems"
title_zh: EvoSkill：多智能体系统的自动化技能发现
authors: "Salaheddin Alzubi, Noah Provenzano, Jaydon Bingham, Weiyuan Chen, Tu Vu"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02766v1"
tags: ["query:semma"]
score: 10.0
evidence: 自动技能发现的自进化框架
tldr: EvoSkill是一个自动发现和优化多智能体系统技能的自我进化框架。针对现有智能体技能多为手工编写且难以跨模型迁移的问题，该框架通过迭代分析执行失败案例，自动生成并精炼结构化的可重用技能库。实验表明，EvoSkill在OfficeQA和SealQA基准测试中显著提升了准确率，并展现出优异的零样本跨任务迁移能力，为构建具备领域专业知识的通用智能体提供了新路径。
selection_source: fresh_fetch
motivation: 现有的智能体技能大多依赖人工设计，且现有的进化方法往往局限于特定模型或任务的底层代码优化，缺乏通用性和领域专业知识。
method: 提出EvoSkill框架，通过迭代分析执行失败原因来提议、编辑并固化结构化的技能文件夹，并利用帕累托前沿策略筛选能提升验证集性能的技能。
result: "在OfficeQA和SealQA基准上分别实现了7.3%和12.1%的准确率提升，且在SealQA上进化的技能在BrowseComp任务中展现了5.3%的零样本迁移增益。"
conclusion: EvoSkill证明了通过技能层面的自动化优化可以产生超越特定训练任务的、具有可迁移性的智能体能力。
---

## 摘要
编程智能体越来越多地被用作通用问题求解器，但其灵活性本身并不能赋予其处理专门任务所需的领域专业知识。最近的研究通过“智能体技能”（agent skills）来解决这一问题：即可重用的工作流和代码，用于增强智能体的领域特定能力。目前大多数技能都是手工构建的，而现有的进化方法主要优化低级产物（如提示词和代码），这些产物与特定模型和任务紧密耦合。我们介绍了 EvoSkill，这是一个通过迭代失败分析自动发现和改进智能体技能的自我进化框架。EvoSkill 分析执行失败，提出新技能或对现有技能进行编辑，并将其具体化为结构化、可重用的技能文件夹。智能体程序的帕累托前沿（Pareto frontier）控制着选择过程，在底层模型保持冻结的情况下，仅保留能提高留出验证集性能的技能。我们在两个基准测试上评估了 EvoSkill：OfficeQA，一个基于美国财政部数据的落地推理（grounded reasoning）基准测试，其精确匹配准确率提高了 7.3%（从 60.6% 提升至 67.9%）；以及 SealQA，一个具有噪声检索的搜索增强问答基准测试，其准确率提升了 12.1%（从 26.6% 提升至 38.7%）。我们还研究了在一个任务上进化的技能向另一个任务的零样本迁移能力；特别是，从 SealQA 进化的技能可以零样本迁移到 BrowseComp，在无需修改的情况下将准确率提高了 5.3%，这表明技能级优化产生了超越训练任务的可迁移能力。

## Abstract
Coding agents are increasingly used as general-purpose problem solvers, but their flexibility does not by itself confer the domain expertise needed for specialized tasks. Recent work addresses this through \textit{agent skills}: reusable workflows, and code, that augment agents with domain-specific capabilities. Most skills today are hand-crafted, and existing evolutionary approaches optimize low-level artifacts (e.g. prompts \& code) that are tightly coupled to specific models and tasks. We introduce \textbf{EvoSkill}, a self-evolving framework that automatically discovers and refines agent skills through iterative failure analysis. EvoSkill analyzes execution failures, proposes new skills or edits to existing ones, and materializes them into structured, reusable skill folders. A Pareto frontier of agent programs governs selection, retaining only skills that improve held-out validation performance while the underlying model remains frozen. We evaluate EvoSkill on two benchmarks: OfficeQA, a grounded reasoning benchmark over U.S.\ Treasury data, where it improves exact-match accuracy by \textbf{7.3\%} (60.6\% $\to$ 67.9\%); and SealQA, a search-augmented QA benchmark with noisy retrieval, where it yields a \textbf{12.1\%} gain (26.6\% $\to$ 38.7\%). We also investigate the zero-shot transfer capabilties of skills evolved on one task to the other; in particular: skills evolved from SealQA transfers zero-shot to BrowseComp, improving accuracy by \textbf{5.3\%} without modification demonstrating that skill-level optimization produces transferable capabilities beyond the training task.

---

## 论文详细总结（自动生成）

### 论文总结：EvoSkill —— 多智能体系统的自动化技能发现

#### 1. 核心问题与研究动机
*   **核心问题**：虽然编程智能体（如 Claude Code）具备极高的灵活性，但它们往往缺乏处理特定领域复杂任务所需的专业知识。
*   **研究动机**：
    *   **现有方案局限**：目前的“智能体技能”大多依靠人工手动构建，难以规模化；而现有的进化算法（如 AlphaEvolve）通常在提示词或代码等低级产物上进行优化，这些产物与特定模型和任务紧密耦合，缺乏可重用性和可迁移性。
    *   **目标**：开发一个能够自动发现、精炼并固化为结构化、可重用组件的技能进化框架，使智能体在不改变底层模型的情况下获得领域专家能力。

#### 2. 方法论：EvoSkill 框架
EvoSkill 的核心思想是通过**基于文本反馈的迭代失败分析**来诱导技能生成。
*   **核心组件（三个协作智能体）**：
    1.  **执行者 (Executor, A)**：在当前技能库的支持下执行任务。
    2.  **提议者 (Proposer, P)**：分析执行失败的轨迹、预测答案与真实标签，诊断失败根因，并提出创建新技能或修改现有技能的建议。它维护一个反馈历史记录 $H$，以避免重复错误。
    3.  **技能构建者 (Skill-Builder, S)**：将提议者的抽象建议转化为具体的技能文件夹，包含触发元数据、操作说明（`SKILL.md`）和可选的辅助脚本（Python/TS）。
*   **算法流程**：
    *   **初始化**：从无技能的基准智能体开始。
    *   **进化循环**：采用轮询方式从帕累托前沿（Pareto Frontier）选择父程序，在训练集上运行并收集失败案例。
    *   **变异与构建**：提议者生成技能变异建议，构建者生成候选程序。
    *   **筛选与更新**：在留出的验证集上评估候选程序。如果性能优于前沿中最弱的成员，则将其加入前沿并剔除旧成员。
    *   **固化**：最终输出性能最优的技能库。

#### 3. 实验设计
*   **数据集/场景**：
    1.  **OfficeQA**：基于美国财政部公告的落地推理基准，涉及数万页复杂的表格、图表和文本，要求极高的数值提取和合成能力。
    2.  **SealQA**：搜索增强问答基准，包含大量噪声和冲突的检索结果，测试智能体的搜索策略和验证能力。
    3.  **BrowseComp**：用于测试技能从 SealQA 迁移后的零样本（Zero-shot）表现。
*   **对比基准**：以使用 Claude Opus 4.5 模型的原始 Claude Code 智能体作为 Baseline。
*   **实验变量**：测试了不同规模的训练集（5%、10%、15%）以及“技能合并（Skill-merge）”配置。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **成本提示**：作者提到由于使用 Claude Opus 4.5 模型进行进化循环的计算成本较高，部分配置仅进行了单次运行，未进行大规模的随机种子方差分析。

#### 5. 实验数量与充分性
*   **实验规模**：在两个截然不同的领域（财务文档推理与开放域搜索）进行了测试，并进行了跨任务迁移实验。
*   **充分性评价**：
    *   **优点**：通过不同比例的训练集划分验证了数据效率；引入了定性分析，展示了生成的技能（如“经济时间序列分析”）具有高度的可解释性。
    *   **局限**：由于成本限制，缺乏多次重复实验的统计显著性分析；验证集规模较小（如 OfficeQA 仅 17 个样本），可能存在一定的过拟合风险。

#### 6. 主要结论与发现
*   **性能显著提升**：在 OfficeQA 上，精确匹配准确率提升了 **7.3%**（最高达 67.9%）；在 SealQA 上提升了 **12.1%**。
*   **技能互补性**：通过合并独立运行发现的技能，性能优于任何单一运行的结果，说明 EvoSkill 能从不同失败案例中提取互补能力。
*   **零样本迁移性**：在 SealQA 上进化的“搜索持久化协议”技能，在无需修改的情况下使 BrowseComp 的准确率提升了 **5.3%**，证明了技能级优化具有超越任务的通用性。

#### 7. 优点与亮点
*   **高层抽象**：不同于优化提示词，EvoSkill 优化的是“技能文件夹”，产物是人类可读、可编辑的结构化文档和脚本。
*   **模型无关性**：底层模型保持冻结，进化的技能可以作为插件应用于不同的智能体框架。
*   **闭环进化**：通过失败驱动的反馈机制，使智能体能够针对自身薄弱环节进行“精准补课”。

#### 8. 不足与局限
*   **计算成本**：依赖高性能大模型（如 Opus 4.5）作为提议者和构建者，进化过程开销较大。
*   **数据依赖**：虽然所需训练数据量小，但仍依赖于高质量的标注数据来诊断失败原因。
*   **过拟合风险**：在 15% 训练集上的表现略低于 10%，暗示在小规模基准上过度进化可能导致性能退化。
*   **多模态缺失**：目前主要集中在文本和代码，尚未扩展到视觉或更复杂的多模态交互技能。

（完）
