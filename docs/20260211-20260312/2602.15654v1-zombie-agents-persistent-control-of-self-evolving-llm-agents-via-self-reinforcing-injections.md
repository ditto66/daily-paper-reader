---
title: "Zombie Agents: Persistent Control of Self-Evolving LLM Agents via Self-Reinforcing Injections"
title_zh: 僵尸智能体：通过自增强注入实现对自进化大语言模型智能体的持久控制
authors: "Xianglin Yang, Yufei He, Shuo Ji, Bryan Hooi, Jin Song Dong"
date: 2026-02-17
pdf: "https://arxiv.org/pdf/2602.15654v1"
tags: ["query:q1"]
score: 8.0
evidence: 自进化LLM智能体通过长期记忆更新内部状态的安全风险
tldr: 本研究揭示了自我进化LLM智能体在长期记忆机制下的严重安全漏洞，提出了名为“僵尸智能体”的持久性攻击。攻击者利用间接注入手段，使智能体在正常交互中将恶意载荷存入记忆，从而实现跨会话的隐蔽控制。该研究设计了针对多种记忆机制的持久化策略，并通过实验验证了攻击的有效性，强调了现有防御机制在应对此类进化型威胁时的局限性。
selection_source: fresh_fetch
motivation: 自我进化智能体通过长期记忆优化性能，但这使得恶意指令可能被持久化存储，从而导致智能体被攻击者长期操控。
method: 提出一种包含感染和触发两个阶段的黑盒攻击框架，针对滑动窗口和检索增强等记忆机制设计了特定的持久化注入策略。
result: 实验表明，该攻击能使智能体在维持正常任务表现的同时，实现跨会话的持久控制并诱导其执行未经授权的工具操作。
conclusion: 传统的单会话提示词过滤不足以保护自我进化智能体，安全防御必须考虑长期记忆演化带来的持续性风险。
---

## 摘要
自进化大语言模型（LLM）智能体通过编写和重用长期记忆，在不同会话之间更新其内部状态。这种设计提高了在长时程任务上的性能，但也带来了安全风险：在良性会话期间观察到的不可信外部内容可能会被存储为记忆，并在随后被视为指令。我们研究了这一风险，并正式提出了一种被称为“僵尸智能体”（Zombie Agent）的持久性攻击。在这种攻击中，攻击者秘密植入一个跨会话存续的有效载荷，从而有效地将智能体转变为攻击者的傀儡。我们提出了一个黑盒攻击框架，该框架仅通过攻击者控制的网页内容进行间接暴露。该攻击分为两个阶段：在感染阶段，智能体在完成良性任务时读取中毒来源，并通过其正常的更新过程将有效载荷写入长期记忆；在触发阶段，有效载荷被检索或传递，并导致未经授权的工具行为。我们针对常见的记忆实现（包括滑动窗口和检索增强记忆）设计了特定机制的持久化策略，以抵抗截断和相关性过滤。我们在具有代表性的智能体设置和任务上评估了该攻击，测量了随时间推移的持久性以及在保持良性任务质量的同时诱导未经授权操作的能力。我们的结果表明，记忆进化可以将一次性的间接注入转化为持久性的侵害，这表明仅关注单次会话提示词过滤的防御措施对于自进化智能体而言是不够的。

## 速览
**TLDR**：本研究探讨了自进化大模型智能体在利用长期记忆提升性能时面临的安全风险，提出了名为“僵尸智能体”的持久性攻击。攻击者通过受控网页内容进行间接注入，使智能体在处理正常任务时将恶意负载存入记忆，从而实现跨会话的长期控制。该研究针对多种记忆机制设计了持久化策略，并验证了攻击在保持任务质量的同时诱导非法操作的有效性。 \
**Motivation**：自进化智能体的长期记忆机制可能将外部不可信内容转化为持久的恶意指令，从而产生跨会话的安全隐患。 \
**Method**：提出一种黑盒攻击框架，通过感染阶段将负载写入记忆，并在触发阶段利用特定策略绕过记忆过滤以执行非法操作。 \
**Result**：实验表明，该攻击能使智能体在多个会话中持续受控，且能有效抵抗滑动窗口和检索增强等记忆管理机制的防御。 \
**Conclusion**：针对单次会话的防御手段不足以应对自进化智能体面临的持久性注入威胁，亟需开发针对记忆演化的安全机制。

---

## Abstract
Self-evolving LLM agents update their internal state across sessions, often by writing and reusing long-term memory. This design improves performance on long-horizon tasks but creates a security risk: untrusted external content observed during a benign session can be stored as memory and later treated as instruction. We study this risk and formalize a persistent attack we call a Zombie Agent, where an attacker covertly implants a payload that survives across sessions, effectively turning the agent into a puppet of the attacker.   We present a black-box attack framework that uses only indirect exposure through attacker-controlled web content. The attack has two phases. During infection, the agent reads a poisoned source while completing a benign task and writes the payload into long-term memory through its normal update process. During trigger, the payload is retrieved or carried forward and causes unauthorized tool behavior. We design mechanism-specific persistence strategies for common memory implementations, including sliding-window and retrieval-augmented memory, to resist truncation and relevance filtering. We evaluate the attack on representative agent setups and tasks, measuring both persistence over time and the ability to induce unauthorized actions while preserving benign task quality. Our results show that memory evolution can convert one-time indirect injection into persistent compromise, which suggests that defenses focused only on per-session prompt filtering are not sufficient for self-evolving agents.

---

## 论文详细总结（自动生成）

以下是对论文《Zombie Agents: Persistent Control of Self-Evolving LLM Agents via Self-Reinforcing Injections》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：研究自进化（Self-evolving）LLM智能体在引入长期记忆机制后产生的新型安全漏洞。
*   **研究动机**：传统的提示词注入（Prompt Injection）通常是“瞬时且无状态”的，一旦会话结束或上下文重置，攻击即失效。然而，现代智能体通过读写长期记忆（如RAG或滑动窗口）来跨会话更新状态。论文旨在证明，这种“进化”能力会将一次性的间接注入转化为持久性的控制，使智能体变成受攻击者操控的“僵尸智能体”（Zombie Agent）。

### 2. 方法论
论文提出了一个黑盒攻击框架，分为两个阶段，并针对不同的记忆架构设计了特定策略：
*   **核心思想**：利用智能体正常的记忆更新流程，将恶意指令（Payload）植入其长期存储中，使其在后续无关会话中被重新激活。
*   **关键技术细节**：
    *   **第一阶段：感染（Infection）**：智能体在执行良性任务（如网页浏览）时，无意中读取了攻击者预设的恶意网页内容，并通过记忆进化函数 $F_M$ 将其写入存储。
    *   **第二阶段：触发与持久化（Trigger & Persistence）**：
        *   **针对滑动窗口（FIFO）记忆**：设计了**递归自我复制（Recursive Self-Replication）**策略。Payload 包含指令，要求智能体在每轮操作前重新访问攻击者页面或重写 Payload，防止其因窗口滚动而被截断。
        *   **针对检索增强（RAG）记忆**：设计了**语义别名（Semantic Aliasing）**策略。将恶意指令包装在多种高频、广泛的语义载体中，确保无论用户未来提出什么查询，恶意条目都能以高相似度被检索出来。
*   **公式化描述**：将智能体状态演化定义为 $M_{j+1} = F_M(M_j, C_j)$，攻击目标是使恶意载荷 $Z \in M_{j+1}$ 且在后续会话 $C_k (k>j)$ 中持续生效。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **模型**：Gemini-2.5-Flash 和 GLM-4.7-Flash。
    *   **任务**：数据窃取（Data Exfiltration）和恶意命令执行（Command Execution）。
    *   **数据集**：使用 `insta-150k-v1` 模拟真实的网页交互查询。
    *   **案例研究**：医疗助手（泄露病人隐私）和购物助手（金融欺诈与凭据窃取）。
*   **Benchmark 与对比方法**：
    *   对比了四种标准的间接提示词注入（IPI）方法：Naive（朴素注入）、Context Ignoring（忽略之前指令）、Escape Characters（转义字符）、Fake Completion（伪造对话结束）。
    *   评估了三种防御机制：Sandwich（三明治防御）、Instructional（指令提醒）、Spotlight（标记符防御）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该研究主要针对商业大模型（Gemini, GLM）的 API 进行黑盒攻击测试，不涉及模型本身的训练或微调，因此算力消耗主要集中在推理调用和环境模拟上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对滑动窗口做了 20+ 轮触发实验。
    *   针对 RAG 建立了包含 3,000 条记录的数据库，并进行了 300 轮感染和 20 轮触发测试。
    *   所有实验均采用 3 个独立随机种子取平均值。
*   **充分性评价**：实验设计较为**充分且客观**。涵盖了两种主流记忆架构，对比了多种基准攻击，并进行了消融实验（如不同的进化策略：原始历史、口头反思、经验精炼）。此外，还测试了在多种安全护栏下的鲁棒性，验证了攻击的实际威胁。

### 6. 主要结论与发现
*   **持久性威胁**：记忆进化机制确实能将瞬时攻击转化为跨会话的持久控制，传统防御在“记忆”这一维度上存在盲区。
*   **防御失效**：现有的提示词过滤和边界标记（如 Spotlight）无法有效阻止僵尸攻击，因为恶意指令是从智能体“信任”的内部记忆中提取的。
*   **机制利用**：攻击者可以利用 RAG 的语义检索特性，通过污染向量空间实现“全场景”触发。
*   **进化策略的影响**：虽然复杂的记忆精炼（如总结归纳）会降低攻击成功率，但无法完全消除风险，智能体仍可能将恶意逻辑“结晶”为永久规则。

### 7. 优点
*   **威胁模型新颖**：首次形式化定义了“僵尸智能体”概念，精准捕捉了自进化 AI 的安全痛点。
*   **攻击策略巧妙**：递归更新和语义别名策略极具针对性，解决了长文本截断和检索过滤的难题。
*   **实战导向**：通过医疗和电商的案例研究，直观展示了该漏洞可能导致的现实世界危害。

### 8. 不足与局限
*   **工具依赖性**：攻击高度依赖于智能体是否拥有“读网页”和“执行命令/发送请求”的权限。
*   **防御研究较浅**：虽然验证了现有防御的无效性，但提出的对策（如来源追踪、策略检查）较为宏观，缺乏具体的算法实现和验证。
*   **模型覆盖面**：主要测试了两个 Flash 版本的模型，对于推理能力更强或具有更复杂内置反思机制的模型（如 GPT-4o 或 O1 系列），攻击效果可能存在差异。

（完）
