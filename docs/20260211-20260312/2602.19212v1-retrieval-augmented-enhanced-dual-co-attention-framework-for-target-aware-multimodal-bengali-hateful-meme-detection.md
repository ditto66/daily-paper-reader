---
title: Retrieval Augmented Enhanced Dual Co-Attention Framework for Target Aware Multimodal Bengali Hateful Meme Detection
title_zh: 用于目标感知多模态孟加拉语仇恨模因检测的检索增强型增强双协同注意力框架
authors: "Raihan Tanvir, Md. Golam Rabiul Alam"
date: 2026-02-22
pdf: "https://arxiv.org/pdf/2602.19212v1"
tags: ["query:ai-mmd"]
score: 9.0
evidence: 社交媒体中的多模态仇恨模因检测
tldr: 针对孟加拉语这一低资源语言中多模态仇恨迷因检测的难题，本文提出了一种增强型双协同注意力框架（xDORA）。通过整合CLIP、DINOv2等视觉编码器与XLM-R等文本编码器，并引入检索增强生成（RAG）和FAISS非参数推理，有效解决了数据稀疏和类别不平衡问题。实验证明，该方法在仇恨识别和目标检测任务上均优于基准模型和LLaVA。
selection_source: fresh_fetch
motivation: 孟加拉语多模态仇恨迷因检测面临标注数据有限、类别不平衡及复杂的语码混合等挑战。
method: 提出xDORA框架，结合多种视觉与文本编码器，并引入基于FAISS的检索增强机制（RAG-Fused DORA）以提升上下文推理能力。
result: xDORA在仇恨识别和目标检测任务中分别达到0.78和0.71的F1分数，而RAG增强版本进一步将性能提升至0.79和0.74。
conclusion: 结合监督学习、检索增强和非参数化推理的多模态框架能有效应对低资源语言中复杂的文化与语言背景下的仇恨言论检测。
---

## 摘要
社交媒体上的仇恨内容越来越多地以多模态模因（memes）的形式出现，通过结合图像和文本来传达有害叙事。在孟加拉语等低资源语言中，由于标注数据有限、类别不平衡以及普遍存在的语码混合（code-mixing），自动检测仍然具有挑战性。为了解决这些问题，我们利用来自孟加拉语多模态攻击性数据集（MIMOSA）的语义对齐样本扩充了孟加拉语仇恨模因（BHM）数据集，从而提高了类别平衡和语义多样性。我们提出了增强型双协同注意力框架（xDORA），通过加权注意力池化整合视觉编码器（CLIP、DINOv2）和多语言文本编码器（XGLM、XLM-R），以学习鲁棒的跨模态表示。基于这些嵌入，我们开发了一个基于 FAISS 的 k-最近邻分类器用于非参数推理，并引入了 RAG-Fused DORA，该模型结合了检索驱动的上下文推理。我们进一步评估了 LLaVA 在零样本、少样本和检索增强提示设置下的表现。在扩展数据集上的实验表明，xDORA (CLIP + XLM-R) 在仇恨模因识别和目标实体检测方面的宏平均 F1 分数分别达到 0.78 和 0.71，而 RAG-Fused DORA 将性能提升至 0.79 和 0.74，相较于 DORA 基准有所增益。基于 FAISS 的分类器表现出竞争力，并通过语义相似性建模展示了对稀有类别的鲁棒性。相比之下，LLaVA 在少样本设置下效果有限，在检索增强下仅有微小改进，这突显了预训练视觉语言模型在未经微调的情况下处理语码混合孟加拉语内容的局限性。这些研究结果证明了监督、检索增强和非参数多模态框架在解决低资源仇恨言论检测中的语言和文化复杂性方面的有效性。

## Abstract
Hateful content on social media increasingly appears as multimodal memes that combine images and text to convey harmful narratives. In low-resource languages such as Bengali, automated detection remains challenging due to limited annotated data, class imbalance, and pervasive code-mixing. To address these issues, we augment the Bengali Hateful Memes (BHM) dataset with semantically aligned samples from the Multimodal Aggression Dataset in Bengali (MIMOSA), improving both class balance and semantic diversity. We propose the Enhanced Dual Co-attention Framework (xDORA), integrating vision encoders (CLIP, DINOv2) and multilingual text encoders (XGLM, XLM-R) via weighted attention pooling to learn robust cross-modal representations. Building on these embeddings, we develop a FAISS-based k-nearest neighbor classifier for non-parametric inference and introduce RAG-Fused DORA, which incorporates retrieval-driven contextual reasoning. We further evaluate LLaVA under zero-shot, few-shot, and retrieval-augmented prompting settings. Experiments on the extended dataset show that xDORA (CLIP + XLM-R) achieves macro-average F1-scores of 0.78 for hateful meme identification and 0.71 for target entity detection, while RAG-Fused DORA improves performance to 0.79 and 0.74, yielding gains over the DORA baseline. The FAISS-based classifier performs competitively and demonstrates robustness for rare classes through semantic similarity modeling. In contrast, LLaVA exhibits limited effectiveness in few-shot settings, with only modest improvements under retrieval augmentation, highlighting constraints of pretrained vision-language models for code-mixed Bengali content without fine-tuning. These findings demonstrate the effectiveness of supervised, retrieval-augmented, and non-parametric multimodal frameworks for addressing linguistic and cultural complexities in low-resource hate speech detection.

---

## 论文详细总结（自动生成）

### 论文总结：用于目标感知多模态孟加拉语仇恨模因检测的检索增强型增强双协同注意力框架

#### 1. 核心问题与整体含义（研究动机和背景）
本文关注的是**低资源语言（孟加拉语）**环境下，社交媒体中**多模态仇恨模因（Memes）**的自动检测问题。
*   **研究动机**：仇恨模因通过图像与文本的复杂交互传播有害信息。孟加拉语拥有超过2.3亿使用者，但在该领域的自动化检测面临三大挑战：
    1.  **数据稀缺与不平衡**：现有数据集规模小，且特定攻击目标（如社会群体）的样本极少。
    2.  **语言复杂性**：普遍存在语码混合（Bengali-English code-mixing）和深层的文化隐喻。
    3.  **跨模态融合难度**：简单的特征拼接难以捕捉图文之间的微妙讽刺或攻击性含义。

#### 2. 论文提出的方法论
论文提出了一套组合拳式的解决方案，核心架构为 **xDORA（Enhanced Dual cO-attention fRAmework）** 及其检索增强变体：
*   **数据集增强**：通过语义标签重映射，将 MIMOSA 数据集的样本整合进 BHM 数据集，使样本量从 7,109 增加到 9,342，显著缓解了类别不平衡。
*   **xDORA 架构**：
    *   **多编码器集成**：视觉端采用 CLIP (ViT) 或 DINOv2；文本端采用 XGLM 或 XLM-RoBERTa。
    *   **双协同注意力机制**：包含“以文本为条件的图对文注意力（I2T-ACT）”和“以文本为条件的图对图注意力（I2I-ACT）”，旨在捕捉图文间的互补信息及文本引导下的视觉重点。
    *   **加权池化**：通过软注意力机制对融合特征进行聚合。
*   **检索增强（RAG）策略**：
    *   **FAISS-Based k-NN**：利用 xDORA 生成的嵌入向量，在 FAISS 索引库中检索最相似的训练样本，进行非参数化分类。
    *   **RAG-Fused DORA**：将 xDORA 的预测概率与基于相似度加权的检索标签分布进行加权融合（Ensemble）。
    *   **RAG-Prompted LLaVA**：将检索到的相似样本作为 Few-shot 示例放入 Prompt 中，引导大模型（LLaVA）进行推理。

#### 3. 实验设计
*   **数据集**：扩展后的孟加拉语仇恨模因数据集（Extended BHM）。
*   **任务设置**：
    *   **Task 1**：二分类（是否为仇恨模因）。
    *   **Task 2**：四分类（目标实体检测：个人、社区、组织、社会群体）。
*   **Benchmark 与对比方法**：
    *   **纯视觉/纯文本**：CLIP, DINOv2, ConvNeXT; XGLM, XLM-R, mDeBERTa。
    *   **多模态基准**：原版 DORA, MAF。
    *   **大语言模型**：LLaVA-1.6-Mistral-7B（Zero-shot, Few-shot）。

#### 4. 资源与算力
*   **硬件环境**：使用了 Google Colab 和 Kaggle 平台提供的 **NVIDIA T4 或 L4 GPU**（16 GB VRAM）。
*   **优化技术**：采用了自动混合精度（AMP）训练以节省显存。
*   **训练细节**：Batch size 为 16，学习率为 $2 \times 10^{-5}$，最多训练 20 个 Epoch 并使用早停机制（Early Stopping）。
*   **未明确说明**：论文未给出具体的总训练时长或总计算量（如 TFLOPS）。

#### 5. 实验数量与充分性
*   **实验规模**：对比了 15 种以上的模型组合，涵盖了从单模态到多模态、从监督学习到大模型推理的多种范式。
*   **充分性与公平性**：
    *   **统计显著性**：使用了 **1000 次迭代的自助法（Bootstrapping）** 来计算 95% 置信区间，这在处理样本极少（如 TS 类仅 11 个测试样本）的情况下非常必要，保证了结果的客观性。
    *   **消融/变体对比**：详细对比了不同视觉与文本编码器组合（如 CLIP vs DINOv2）的效果。
    *   **评估指标**：采用 Macro-F1 分数，充分考虑了类别不平衡的影响。

#### 6. 主要结论与发现
*   **RAG 的优越性**：RAG-Fused DORA 表现最佳，Task 1 F1 达到 0.79，Task 2 达到 0.74。检索增强对**长尾类别（稀有类别）**提升巨大，例如在样本最少的 TS 类上，FAISS-kNN 的表现远超纯监督模型。
*   **编码器效能**：CLIP + XLM-R 的组合在孟加拉语语境下表现最稳健。
*   **LLaVA 的局限性**：预训练的大型视觉语言模型（LLaVA）在未经微调的情况下，对孟加拉语模因的理解能力较差（F1 仅 0.2-0.5 左右），即使加入 RAG 提示，提升也有限，说明其在低资源/语码混合语言上的预训练不足。

#### 7. 优点
*   **创新的融合策略**：不仅使用了复杂的协同注意力机制，还引入了非参数化的检索增强，有效解决了小样本类别的泛化问题。
*   **数据集贡献**：通过跨数据集整合，为孟加拉语研究社区提供了一个更平衡、更具挑战性的基准。
*   **严谨的评估**：引入置信区间分析，在低资源研究中体现了高度的学术严谨性。

#### 8. 不足与局限
*   **对 TS 类别的依赖**：尽管有提升，但社会群体（TS）类别的样本量依然极低（仅 117 个），导致该类别的预测稳定性仍有待提高。
*   **LLaVA 微调缺失**：论文仅测试了 LLaVA 的 In-context Learning 能力，未进行参数微调（Fine-tuning），这可能限制了对大模型潜力的挖掘。
*   **文化深度理解**：虽然模型在统计上表现良好，但对于某些极度依赖孟加拉特定历史背景或俚语的模因，模型仍存在误判风险。

（完）
