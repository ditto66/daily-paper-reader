---
title: "GatedCLIP: Gated Multimodal Fusion for Hateful Memes Detection"
title_zh: GatedCLIP：用于仇恨模因检测的门控多模态融合
authors: "Yingying Guo, Ke Zhang, Zirong Zeng"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20818v1"
tags: ["query:ai-mmd"]
score: 9.0
evidence: 用于仇恨模因检测的多模态融合
tldr: 针对多模态仇恨模因检测中图文交互复杂的挑战，本文提出GatedCLIP模型。该模型在CLIP基础上引入了可学习的投影头、动态门控融合机制以及对比学习目标，旨在优化语义空间映射并自适应加权视觉与文本特征。实验表明，GatedCLIP在Hateful Memes数据集上取得了0.66的AUROC，显著优于基准模型，且仅需35万个可训练参数，实现了性能与效率的平衡。
selection_source: fresh_fetch
motivation: 仇恨模因的有害信息通常源于良性图像与文本之间复杂的相互作用，传统方法难以有效捕捉这种多模态关联。
method: 提出GatedCLIP模型，通过投影头映射语义空间，利用动态门控机制融合特征，并结合对比学习保持跨模态对齐。
result: 在Hateful Memes数据集上，该方法将AUROC从CLIP基准的0.49提升至0.66，且仅包含35万个可训练参数。
conclusion: GatedCLIP通过轻量级的架构改进显著提升了多模态仇恨模因检测的性能，证明了动态融合机制的有效性。
---

## 摘要
在多模态模因（memes）中检测仇恨内容具有独特的挑战，因为有害信息通常源于良性图像与文本之间复杂的相互作用。我们提出了 GatedCLIP，这是一种视觉-语言模型，通过针对仇恨模因检测的专门架构改进，增强了 CLIP 的多模态能力。我们的方法引入了学习型投影头，将 CLIP 嵌入映射到任务优化的语义空间；一种动态门控融合机制，可自适应地加权视觉和文本特征；以及一个维持跨模态语义对齐的对比学习目标。在 Hateful Memes 数据集上的实验表明，GatedCLIP 实现了 0.66 的 AUROC，显著优于 CLIP 基线（AUROC 0.49），同时仅需 35 万个可训练参数，保持了计算效率。

## Abstract
Detecting hateful content in multimodal memes presents unique challenges, as harmful messages often emerge from the complex interplay between benign images and text. We propose GatedCLIP, a Vision-Language model that enhances CLIP's multimodal capabilities with specialized architectural improvements for hateful memes detection. Our approach introduces learned projection heads that map CLIP embeddings to a task-optimized semantic space, a dynamic gated fusion mechanism that adaptively weights visual and textual features, and a contrastive learning objective that maintains cross-modal semantic alignment. Experiments on the Hateful Memes dataset demonstrate that GatedCLIP achieves an AUROC of 0.66, substantially outperforming the CLIP baseline (AUROC 0.49) while maintaining computational efficiency with only 350K trainable parameters.

---

## 论文详细总结（自动生成）

### 论文总结：GatedCLIP —— 用于仇恨模因检测的门控多模态融合

#### 1. 核心问题与研究动机
*   **核心问题**：互联网模因（Memes）中的仇恨内容检测具有极高的挑战性，因为其有害性往往源于图像与文本之间复杂的相互作用。单独看图像或文本可能是良性的，但结合后却产生了贬损或仇恨的含义。
*   **研究背景**：现有的视觉-语言模型（如 CLIP）虽然在通用任务上表现优异，但在处理需要细粒度推理的仇恨检测任务时效果不佳。直接使用 CLIP 的平均嵌入进行分类，其 AUROC 仅为 0.49（接近随机猜测）。
*   **动机**：开发一种轻量级且高效的架构，在冻结 CLIP 编码器的前提下，通过改进融合机制来捕捉图文之间的深层语义关联。

#### 2. 方法论
GatedCLIP 在冻结的 CLIP 编码器基础上引入了三个关键组件：
*   **投影头（Projection Heads）**：
    *   将 CLIP 原始的 512 维嵌入映射到 128 维的低维语义空间。
    *   采用两层全连接网络，配合 ReLU 激活函数和 Dropout（0.2），旨在过滤通用特征并提取与仇恨检测相关的特定特征。
*   **动态门控融合机制（Gated Fusion Mechanism）**：
    *   核心思想是自适应地为视觉和文本特征分配权重。
    *   **公式**：通过拼接后的特征计算门控值 $g = \sigma(W_g \cdot \text{ReLU}(W_c[h_I; h_T]))$，最终融合特征为 $h_{fused} = g \cdot h_I + (1 - g) \cdot h_T$。
    *   这使得模型能根据具体样本决定更依赖图像（如含有仇恨符号）还是文本（如含有政治敏感词）。
*   **对比学习目标（Contrastive Alignment）**：
    *   在分类损失（交叉熵 $L_{cls}$）的基础上，增加了一个对比损失 $L_{contr}$，用于保持投影后图文表示的语义对齐。
    *   总损失函数为 $L = L_{cls} + \lambda L_{contr}$（其中 $\lambda = 0.01$）。

#### 3. 实验设计
*   **数据集**：使用 Facebook (Meta) 发布的 **Hateful Memes Dataset**，包含 10,000+ 条标注模因。该数据集专门设计了“挑战样本”，要求模型必须具备多模态推理能力。
*   **基准模型（Benchmark）**：
    *   **CLIP Baseline**：冻结编码器，仅对图文嵌入进行简单平均处理后接线性分类器。
*   **评估指标**：主要使用 AUROC（受试者工作特征曲线下面积），辅助使用准确率（Accuracy）。

#### 4. 资源与算力
*   **算力设备**：使用单台 NVIDIA GPU 进行训练（具体型号未标明）。
*   **训练效率**：
    *   采用混合精度（FP16）训练。
    *   训练 10 个 Epoch 约耗时 **40 分钟**。
    *   推理速度超过 100 样本/秒。
*   **参数量**：可训练参数仅为 **350K**（约占 CLIP 总参数量的 0.2%），极其轻量。

#### 5. 实验数量与充分性
*   **实验规模**：论文对比了 GatedCLIP 与 CLIP 基线在验证集上的表现，并展示了训练过程中的损失曲线和 AUROC 变化曲线。
*   **分析深度**：对门控值（Gate Values）进行了定量分析，验证了模型在不同类型模因上确实表现出了不同的侧重（图像侧重型 vs. 文本侧重型）。
*   **充分性评价**：实验在核心数据集上证明了方法的有效性，但**缺乏与其他 SOTA（先进）模型（如 VisualBERT, ViLBERT 等）的直接横向对比数据**，且未进行广泛的消融实验来验证投影头维度或对比损失权重的影响。

#### 6. 主要结论与发现
*   **性能提升**：GatedCLIP 达到了 **0.66 的 AUROC**，相比基线（0.49）实现了 **35% 的相对提升**。
*   **门控有效性**：实验发现，对于视觉仇恨明显的模因，门控值 $g$ 均值为 0.68（偏向图像）；对于文本仇恨明显的模因，$g$ 均值为 0.35（偏向文本）。
*   **轻量化可行性**：证明了无需全参数微调，仅通过轻量级适配层即可显著提升基础模型在特定下游任务上的表现。

#### 7. 优点
*   **参数高效**：仅需极少的可训练参数，适合资源受限的部署场景。
*   **动态融合**：相比固定权重的融合，门控机制提供了更好的灵活性和一定的可解释性。
*   **对齐保持**：引入对比学习损失，防止了在微调分类任务时丢失 CLIP 原有的跨模态对齐能力。

#### 8. 不足与局限
*   **性能差距**：虽然远超基线，但 0.66 的 AUROC 距离该挑战赛的顶尖水平（>0.80）仍有较大差距。
*   **泛化性验证不足**：仅在一个数据集上进行了测试，未验证在其他多模态任务或不同语言环境下的表现。
*   **文化偏见**：模型依赖于 CLIP 的预训练数据，可能在处理非西方文化背景或非英语的仇恨模因时存在局限。
*   **对比实验缺失**：未提供与其他参数高效微调方法（如 Adapter, LoRA）的对比。

（完）
