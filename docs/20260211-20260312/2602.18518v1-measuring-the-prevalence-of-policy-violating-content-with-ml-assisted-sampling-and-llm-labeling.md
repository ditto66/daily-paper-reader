---
title: Measuring the Prevalence of Policy Violating Content with ML Assisted Sampling and LLM Labeling
title_zh: 利用机器学习辅助采样和大型语言模型标注衡量违规内容的流行率
authors: "Attila Dobi, Aravindh Manickavasagam, Benjamin Thompson, Xiaohan Yang, Faisal Farooq"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.18518v1"
tags: ["query:ai-mmd"]
score: 9.0
evidence: 用于标记违规内容的多模态大模型
tldr: 本研究针对内容安全团队难以准确衡量违规内容流行度（Prevalence）的问题，提出了一种结合机器学习辅助采样与大语言模型（LLM）自动标注的测量系统。该系统通过ML权重优化采样效率，聚焦高风险高曝光内容，并利用多模态LLM进行策略一致性标注。该方法不仅降低了人工成本，还支持多维度（如地理位置、内容年龄）的无偏估计，为平台治理提供了高效、可扩展的量化监控方案。
selection_source: fresh_fetch
motivation: 传统的违规内容流行度测量受限于违规样本稀疏和人工标注成本高昂，难以实现高频且具有代表性的全平台监控。
method: 采用基于设计的测量系统，利用ML辅助权重进行概率采样，并结合多模态LLM与策略提示词进行自动化标注。
result: 该系统能够产生具有置信区间的无偏流行度估计，并支持通过后分层估计实现多维度的看板下钻分析。
conclusion: 结合ML采样与LLM标注的工程化流程，为大规模、多策略的内容安全合规性测量提供了一种高效且统计严谨的解决方案。
---

## 摘要
内容安全团队需要能够反映用户实际体验的指标，而不仅仅是举报的内容。我们研究流行率（prevalence）：即在特定日期内，违规内容在用户总浏览量（展示量）中所占的比例。准确衡量流行率具有挑战性，因为违规行为通常很少见，且人工标注成本高昂，导致频繁的、具有平台代表性的研究进展缓慢。我们提出了一种基于设计的测量系统，该系统：(i) 利用机器学习辅助权重从展示流中提取每日概率样本，在保持无偏性的同时，将标注预算集中在高曝光和高风险内容上；(ii) 使用受政策提示词和黄金标准集验证约束的多模态大语言模型（LLM）对采样项目进行标注；(iii) 生成具有置信区间和仪表板下钻功能的、设计一致的流行率估计值。一个关键的设计目标是“一个全局样本，多个透视维度”：通过后分层估计，同一个每日样本可以支持按展示面、观众地理位置、内容时长以及其他细分领域计算流行率。我们描述了统计估计量、方差和置信区间的构建、标注质量监控，以及使该系统能够跨政策配置的工程工作流。

## Abstract
Content safety teams need metrics that reflect what users actually experience, not only what is reported. We study prevalence: the fraction of user views (impressions) that went to content violating a given policy on a given day. Accurate prevalence measurement is challenging because violations are often rare and human labeling is costly, making frequent, platform-representative studies slow. We present a design-based measurement system that (i) draws daily probability samples from the impression stream using ML-assisted weights to concentrate label budget on high-exposure and high-risk content while preserving unbiasedness, (ii) labels sampled items with a multimodal LLM governed by policy prompts and gold-set validation, and (iii) produces design-consistent prevalence estimates with confidence intervals and dashboard drilldowns. A key design goal is one global sample with many pivots: the same daily sample supports prevalence by surface, viewer geography, content age, and other segments through post-stratified estimation. We describe the statistical estimators, variance and confidence interval construction, label-quality monitoring, and an engineering workflow that makes the system configurable across policies.

---

## 论文详细总结（自动生成）

这是一份关于论文《Measuring the Prevalence of Policy-Violating Content with ML-Assisted Sampling and LLM Labeling》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：内容安全团队需要衡量“流行率”（Prevalence），即用户实际看到的违规内容占总展示量的比例。
*   **核心挑战**：
    1.  **稀疏性**：违规内容在海量数据中极度稀疏，随机采样效率极低。
    2.  **成本与延迟**：传统的人工标注成本高昂且耗时长（数周），无法支持每日级别的监控和快速决策。
*   **整体含义**：本文提出了一套结合机器学习（ML）辅助采样与大语言模型（LLM）标注的系统，旨在实现高频（每日）、低成本、统计严谨的违规内容流行率测量。

### 2. 方法论
该系统由三个核心环节组成：
*   **ML 辅助概率采样**：
    *   **核心思想**：利用生产环境中的风险模型评分（Auxiliary Scores）来调整采样权重，使样本集中在高风险和高曝光内容上。
    *   **技术细节**：采用**加权水库采样（Weighted Reservoir Sampling）**算法。采样权重公式为 $w_{jd} = C_{jd}^\nu (s_{jd}^\gamma + \epsilon)$，其中 $C$ 是展示量，$s$ 是风险分。通过调节参数 $\gamma$ 和 $\nu$，在采样效率和权重方差之间取得平衡。
*   **LLM 自动化标注**：
    *   使用多模态 LLM（如 GPT-4 级别）对采样内容（图像、视频帧、文本元数据）进行标注。
    *   通过领域专家（SME）编写的政策提示词（Prompts）驱动，并输出结构化的违规判定及理由。
*   **设计一致性估计（Design-Consistent Estimation）**：
    *   使用 **Hansen-Hurwitz 比率估计量**。关键在于：虽然采样是有偏的（偏向高风险），但通过采样概率的倒数进行重加权，确保最终得到的流行率估计值是无偏的。
    *   支持**单样本多维度下钻**：同一个全局样本可以根据地理位置、内容年龄等维度进行后分层估计。

### 3. 实验设计
*   **应用场景**：Pinterest 生产环境，处理每日数十亿次的展示流。
*   **对比方法（Benchmark）**：
    *   **采样对比**：对比了“纯展示量权重采样（PPS）”与“ML 辅助采样（ML-PPS）”。
    *   **标注对比**：将 LLM 标注结果与人工专家标注的“黄金数据集（Gold Set）”进行对比。
*   **评估指标**：置信区间（CI）宽度、有效样本量（ESS）、正样本提升率、准确率（Accuracy）和 F1 分数。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **推理资源**：提到使用了 GPT-4 级别的多模态模型，并通过 Batch API 处理和提示词缓存（Prompt Caching）来优化成本。系统每日处理约 100 万个单位的内容标注。

### 5. 实验数量与充分性
*   **实验规模**：系统在 Pinterest 生产环境已运行超过一年，涵盖了多个不同的政策领域（如 A、B、C 三类政策）。
*   **充分性验证**：
    *   **模拟实验**：通过合成数据模拟了长尾分布下的采样效率，验证了统计估计量的稳定性。
    *   **生产验证**：展示了在不同政策下，ML 辅助采样相比普通采样能将正样本比例提升 6-11 倍。
    *   **一致性测试**：LLM 与人工标注的准确率差异在 3.2% 以内，F1 差异在 0.5% 以内。
*   **评价**：实验基于大规模真实工业环境，且包含了统计学模拟验证，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **效率飞跃**：相比纯人工流程，标注延迟缩短了约 **15 倍**，运营成本降低了 **10 倍以上**。
*   **采样增益**：ML 辅助采样显著提高了稀疏违规内容的检出率，从而在相同样本量下大幅收窄了置信区间。
*   **质量可靠**：经过 SME 优化的 LLM 提示词在违规判定上达到了与人工审核员相当的水平。
*   **互补性**：流行率指标与用户举报量仅呈弱相关，证明了该系统提供了举报数据之外的补充视野。

### 7. 优点
*   **统计严谨性**：通过设计一致性估计解决了采样偏差问题，确保指标的科学性。
*   **工程灵活性**：一套样本支持多种维度（Pivot）的分析，无需为每个细分领域重新采样。
*   **自动化闭环**：实现了从日志提取、采样、标注到告警的全自动化流程，支持每日 OKR 追踪。

### 8. 不足与局限
*   **模型依赖性**：虽然估计是无偏的，但采样效率高度依赖于辅助风险评分模型的质量；如果模型失效，置信区间会迅速扩大。
*   **漂移风险**：LLM 模型的更新或政策解读的微小变化可能导致指标波动，需要持续的黄金集监控。
*   **极端稀疏限制**：对于发生率极低的违规行为，即使有 ML 辅助，日级统计仍可能面临方差过大的问题，需依赖周级聚合。
*   **黑盒属性**：LLM 的判定逻辑虽然有理由输出，但本质上仍具有一定的不可解释性。

（完）
