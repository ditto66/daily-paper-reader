---
title: Agentic Mixed-Source Multi-Modal Misinformation Detection with Adaptive Test-Time Scaling
title_zh: 基于自适应测试时缩放的智能体化混合来源多模态虚假信息检测
authors: "Wei Jiang, Tong Chen, Wei Yuan, Quoc Viet Hung Nguyen, Hongzhi Yin"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02519v1"
tags: ["query:q6"]
score: 10.0
evidence: 具有自适应缩放的智能体多模态虚假信息检测
tldr: 针对社交平台中复杂的混合来源多模态虚假信息检测（M3D），本文提出AgentM3D框架。该框架通过多智能体协作，引入自适应测试时缩放范式，利用Best-of-N机制和评论智能体增强推理能力。通过规划智能体动态调整推理路径并结合级联决策链，在减少计算开销的同时显著提升了零样本检测性能，在多个基准测试上达到SOTA。
selection_source: fresh_fetch
motivation: 单一视觉语言模型在处理由文本、图像或跨模态不匹配导致的复杂虚假信息时能力有限，且现有智能体系统缺乏深度推理路径的探索。
method: 提出AgentM3D框架，采用自适应测试时缩放、Best-of-N机制、级联决策链以及根据样本难度动态调整推理路径的规划智能体。
result: 在两个M3D基准测试上的实验表明，AgentM3D在零样本检测性能上显著优于现有的VLM和智能体基准模型。
conclusion: 通过引入自适应推理缩放和多智能体协同机制，AgentM3D有效提升了多模态虚假信息检测的准确性与计算效率。
---

## 摘要
视觉语言模型 (VLM) 已被证明在社交平台上检测多模态虚假信息方面非常有效，特别是在标注不可用或延迟的零样本设置下。然而，单个 VLM 的能力在更复杂的混合来源多模态虚假信息检测 (M3D) 任务中显得力不从心。以带有标题的图像为例，在 M3D 中，虚假信息可能源于不真实的文本、伪造的图像或两种模态之间的不匹配。尽管最近的智能体系统可以通过连接特定模态的 VLM 智能体来处理零样本 M3D，但其有效性仍受限于其架构。在现有的智能体 M3D 解决方案中，对于任何输入样本，每个智能体仅执行一次前向推理过程，这使得决策在具有挑战性的案例中容易受到模型随机性和推理错误的影响。此外，缺乏对替代推理路径的探索阻碍了现代 VLM 充分利用其推理能力。在这项工作中，我们提出了 AgentM3D，这是一个用于零样本 M3D 的多智能体框架。为了增强 VLM 的推理能力，我们引入了一种自适应测试时缩放范式，其中每个特定模态的 VLM 智能体应用 Best-of-N 机制，并结合一个评论智能体进行任务对齐评分。这些智能体被组织在一个级联的、特定模态的决策链中，以减少不必要的计算并限制错误传播。为了确保可扩展性，规划智能体根据样本难度动态确定最大推理路径数量，并且自适应停止机制防止每个智能体内部的过度推理。在两个 M3D 基准测试上的广泛实验表明，与各种基于 VLM 和智能体的基准模型相比，AgentM3D 实现了最先进的零样本检测性能。

## Abstract
Vision-language models (VLMs) have been proven effective for detecting multi-modal misinformation on social platforms, especially in zero-shot settings with unavailable or delayed annotations. However, a single VLM's capacity falls short in the more complex mixed-source multi-modal misinformation detection (M3D) task. Taking captioned images as an example, in M3D, false information can originate from untruthful texts, forged images, or mismatches between the two modalities. Although recent agentic systems can handle zero-shot M3D by connecting modality-specific VLM agents, their effectiveness is still bottlenecked by their architecture. In existing agentic M3D solutions, for any input sample, each agent performs only one forward reasoning pass, making decisions prone to model randomness and reasoning errors in challenging cases. Moreover, the lack of exploration over alternative reasoning paths prevents modern VLMs from fully utilizing their reasoning capacity. In this work, we present AgentM3D, a multi-agent framework for zero-shot M3D. To amplify the reasoning capability of VLMs, we introduce an adaptive test-time scaling paradigm in which each modality-specific VLM agent applies a Best-of-N mechanism, coupled with a critic agent for task-aligned scoring. The agents are organized in a cascading, modality-specific decision chain to reduce unnecessary computation and limit error propagation. To ensure scalability, a planning agent dynamically determines the maximum number of reasoning paths based on sample difficulty, and an adaptive stopping mechanism prevents excessive reasoning within each agent. Extensive experiments on two M3D benchmarks demonstrate that AgentM3D achieves state-of-the-art zero-shot detection performance compared with various VLM-based and agentic baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Agentic Mixed-Source Multi-Modal Misinformation Detection with Adaptive Test-Time Scaling》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：社交媒体上的虚假信息日益复杂，呈现出“混合来源”特征（M3D），即虚假内容可能单独来自文本、图像，或两者虽真实但组合不当（跨模态不匹配）。
*   **核心问题**：现有的视觉语言模型（VLM）在零样本（Zero-shot）检测中，通常采用单次前向推理。这种方式容易受到模型随机性的影响，且在复杂逻辑推理时容易产生“幻觉”或错误。
*   **研究动机**：现有的智能体（Agentic）系统缺乏对推理路径的深度探索。论文旨在通过“测试时缩放”（Test-Time Scaling, TTS）技术，让模型在推理阶段通过多次尝试和自我修正来提高检测的鲁棒性和准确性。

### 2. 论文提出的方法论：AgentM3D
AgentM3D 是一个多智能体协作框架，核心思想是将检测任务分解并引入自适应的推理增强机制：
*   **分层模态特定检测（Hierarchical Cascade）**：
    *   系统按顺序调用：**文本真实性智能体** $\rightarrow$ **图像真实性智能体** $\rightarrow$ **跨模态一致性智能体**。
    *   采用级联决策：一旦前一级检测到异常，立即终止并报错，减少后续不必要的计算。
*   **带评论的 Best-of-N 推理（Critique-aware BoN）**：
    *   对每个智能体，生成 $N$ 个推理候选路径。
    *   引入**奖励模型（Reward Model）**评估语义连贯性，并结合**评论智能体（Critique Agent）**利用外部工具（如维基百科、图像取证工具）对候选路径进行事实核查评分。
*   **自适应测试时缩放（Adaptive TTS）**：
    *   **规划智能体（Planning Agent）**：预先判断样本难度，简单样本走单次推理，复杂样本激活 BoN 增强推理。
    *   **自适应早停机制（Early-Stopping）**：基于 Top-m 候选者的得分差距（Top-m Average Gap）动态决定是否停止采样，平衡精度与效率。

### 3. 实验设计
*   **数据集**：
    1.  **MMFakeBench**：包含 11,000 多个图文对，涵盖文本扭曲、图像扭曲和跨模态不匹配。
    2.  **Combined Benchmark**：作者整合了 Mocheg（文本中心）、Fakeddit-M（图像操纵）和 VERITE（真实世界平衡数据）三个数据集，构建了更具挑战性的混合来源基准。
*   **对比方法**：
    *   **基础 VLM**：Standard（单次推理）、BoN（标准 Best-of-N）。
    *   **智能体框架**：MMD-Agent（分层检测基准）、T2Agent（基于蒙特卡洛树搜索的工具增强智能体）。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA RTX 4090 GPU** 上完成。
*   **模型后端**：使用了 **Qwen3-VL-4B-Instruct** 和 **Qwen3-VL-8B-Instruct** 作为核心推理引擎。
*   **训练时长**：由于是**零样本（Zero-shot）**设置，该方法不需要针对特定任务进行训练，主要开销在于推理阶段。

### 5. 实验数量与充分性
*   **实验规模**：在两个大型基准上进行了测试，每个基准包含约 1000 个平衡样本。
*   **消融实验**：详细分析了早停机制（ESR）、评论智能体（ACA/CCA）和规划智能体（PA）的作用。
*   **参数敏感性**：探讨了早停阈值 $\tau$ 和采样数量 $N$ 对性能的影响。
*   **充分性评价**：实验设计较为全面，不仅对比了准确率，还重点分析了推理延迟（Latency），证明了该方法在提升精度的同时控制了计算成本，对比实验具有较高的客观性。

### 6. 论文的主要结论与发现
*   **性能领先**：AgentM3D 在两个基准上均显著优于现有的 SOTA 方法（如 MMD-Agent 和 T2Agent）。
*   **TTS 的必要性**：单纯增加采样（BoN）而不引入评论机制（Critique）有时会降低性能，因为奖励模型可能被错误的推理路径误导。
*   **效率优化**：通过规划智能体和早停机制，AgentM3D 避免了对简单样本的过度计算，实现了优于其他多智能体系统的精度-效率权衡。

### 7. 优点（亮点）
*   **系统性解决 TTS 挑战**：首次系统探讨了在 M3D 任务中“何时缩放”、“如何评分”以及“缩放多少”的问题。
*   **任务对齐的评分**：引入特定模态的评论智能体，结合外部工具（逻辑检查、图像取证），解决了通用奖励模型在虚假信息检测中判断力不足的问题。
*   **级联架构**：有效地限制了错误在不同模态间的传播。

### 8. 不足与局限
*   **工具依赖性**：系统的表现高度依赖外部工具（如图像取证模型、搜索 API）的准确性，若工具失效，评论智能体可能给出错误反馈。
*   **模型范围**：实验主要集中在 Qwen 系列模型上，尚未验证在其他主流 VLM（如 GPT-4o, Claude 3.5）上的泛化表现。
*   **实时性挑战**：尽管有早停机制，但相比单次推理，TTS 仍然增加了推理延迟，对于极高并发的实时检测场景可能仍显沉重。

（完）
