---
title: "MemoPhishAgent: Memory-Augmented Multi-Modal LLM Agent for Phishing URL Detection"
title_zh: MemoPhishAgent：用于钓鱼 URL 检测的记忆增强型多模态大语言模型智能体
authors: "Xuan Chen, Hao Liu, Tao Yuan, Mehran Kafai, Piotr Habas, Xiangyu Zhang"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.21394v2"
tags: ["query:ai-mmd"]
score: 7.0
evidence: 用于检测恶意网络钓鱼内容的多模态大模型智能体
tldr: "针对传统钓鱼网站检测滞后及现有LLM方法推理不足的问题，本文提出MemoPhishAgent (MPA)。这是一种增强记忆的多模态LLM智能体，通过动态编排钓鱼检测工具并利用过往推理轨迹的片段记忆来识别威胁。实验显示其在公开和真实社交媒体数据集上显著优于现有SOTA模型，召回率提升高达20%，且已在生产环境中部署，为数百万用户提供主动防护。"
selection_source: fresh_fetch
motivation: 传统检测方法滞后且现有LLM方案缺乏动态推理能力，难以应对快速演变的钓鱼攻击。
method: 提出一种多模态LLM智能体，结合钓鱼专用工具集和片段记忆机制，通过回顾历史推理轨迹辅助决策。
result: "在公开数据集和真实社交媒体场景下，召回率分别提升了13.6%和20%，片段记忆贡献了高达27%的性能增益。"
conclusion: 结合多模态推理与片段记忆的智能体架构能有效提升真实场景下的钓鱼检测鲁棒性与准确性。
---

## 摘要
传统的钓鱼网站检测依赖于静态启发式方法或参考列表，这滞后于快速演变的攻击。虽然最近的系统引入了大语言模型 (LLMs)，但它们仍是基于提示词的确定性流水线，未能充分利用推理能力。我们提出了 MemoPhishAgent (MPA)，这是一种记忆增强型多模态 LLM 智能体，它能动态编排钓鱼检测专用工具，并利用过去推理轨迹的情节记忆来指导对重复出现和新型威胁的决策。在两个公开数据集上，MPA 的表现优于三个最先进的 (SOTA) 基准模型，召回率提高了 13.6%。为了更好地反映面向用户的真实钓鱼检测性能，我们进一步在从五个社交媒体平台主动抓取的真实可疑 URL 基准上评估了 MPA，其召回率提高了 20%。详细分析表明，情节记忆在不引入额外计算开销的情况下，贡献了高达 27% 的召回率增益。消融研究证实了与基于提示词的基准相比，基于智能体的方法的必要性，并验证了我们工具设计的有效性。最后，MPA 已部署到生产环境中，每周处理 6 万个针对性的高风险 URL，并实现了 91.44% 的召回率，为数百万客户提供主动保护。总之，我们的结果表明，将多模态推理与情节记忆相结合，可以在真实的各种用户暴露场景中实现稳健的钓鱼检测。

## 速览
**TLDR**：针对传统钓鱼网站检测滞后及现有LLM方法推理不足的问题，本文提出MemoPhishAgent (MPA)。这是一种增强记忆的多模态LLM智能体，通过动态编排钓鱼检测工具并利用过往推理轨迹的片段记忆来识别威胁。实验显示其在公开和真实社交媒体数据集上显著优于现有SOTA模型，召回率提升高达20%，且已在生产环境中部署，为数百万用户提供主动防护。 \
**Motivation**：传统检测方法滞后且现有LLM方案缺乏动态推理能力，难以应对快速演变的钓鱼攻击。 \
**Method**：提出一种结合多模态LLM与片段记忆的智能体架构，通过动态调用专用工具和检索历史推理经验进行决策。 \
**Result**：在公开数据集和真实社交媒体场景下，召回率分别提升了13.6%和20%，片段记忆贡献了显著的性能增益。 \
**Conclusion**：结合多模态推理与片段记忆的智能体方法能有效提升真实场景下的钓鱼检测鲁棒性与准确性。

---

## Abstract
Traditional phishing website detection relies on static heuristics or reference lists, which lag behind rapidly evolving attacks. While recent systems incorporate large language models (LLMs), they are still prompt-based, deterministic pipelines that underutilize reasoning capability. We present MemoPhishAgent (MPA), a memory-augmented multi-modal LLM agent that dynamically orchestrates phishing-specific tools and leverages episodic memories of past reasoning trajectories to guide decisions on recurring and novel threats. On two public datasets, MPA outperforms three state-of-the-art (SOTA) baselines, improving recall by 13.6%. To better reflect realistic, user-facing phishing detection performance, we further evaluate MPA on a benchmark of real-world suspicious URLs actively crawled from five social media platforms, where it improves recall by 20%. Detailed analysis shows episodic memory contributes up to 27% recall gain without introducing additional computational overhead. The ablation study confirms the necessity of the agent-based approach compared to prompt-based baselines and validates the effectiveness of our tool design. Finally, MPA is deployed in production, processing 60K targeted high-risk URLs weekly, and achieving 91.44% recall, providing proactive protection for millions of customers. Together, our results show that combining multi-modal reasoning with episodic memory yields robust phishing detection in realistic user-exposure settings.

---

## 论文详细总结（自动生成）

以下是对论文《MemoPhishAgent: Memory-Augmented Multi-Modal LLM Agent for Phishing URL Detection》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：网络钓鱼攻击演变极快，传统的防御手段（如静态黑名单、启发式规则或基于参考列表的方法）在面对新域名、内容混淆和基础设施快速变动时存在明显的滞后性。
*   **核心挑战**：现有的基于大语言模型（LLM）的检测系统大多采用单次提示（Prompt-based）或确定性的固定流水线，缺乏动态推理能力，且未能有效利用历史调查经验（即“记忆”），导致在处理复杂或重复出现的威胁时效率和准确率受限。
*   **研究目标**：开发一个能够像人类专家一样进行多步推理、动态调用工具并具备“学习经验”能力的智能体（Agent），以实现高召回率的实时钓鱼检测。

### 2. 方法论：MemoPhishAgent (MPA)
*   **核心思想**：将多模态 LLM 智能体与**情节记忆（Episodic Memory）**相结合。智能体不再遵循固定步骤，而是根据当前证据动态决定下一步行动，并从过去的推理轨迹中提取经验。
*   **关键技术细节**：
    *   **多模态工具集**：设计了五个专用工具：
        1.  **Crawl Content**：将 HTML 转换为 LLM 友好的 Markdown。
        2.  **Check Screenshot**：全页截图摘要与可疑度评估。
        3.  **Check Image**：对特定图形元素（如 Logo）进行细粒度分析。
        4.  **Intelligent Search**：通过搜索引擎获取外部实时知识。
        5.  **Extract Targets**：识别重定向、嵌套链接或子页面。
    *   **情节记忆系统**：
        *   **构建**：提取页面关键词作为索引，存储“工具调用序列 + 最终判定”作为值。
        *   **检索与决策策略**：采用三层架构：
            *   *无匹配*：执行完整的 ReAct 推理循环。
            *   *部分匹配*：将历史轨迹作为 In-context 示例引导当前推理。
            *   *完全匹配*：通过对存储结果的“多数投票”快速达成共识，显著降低计算开销。
    *   **推理框架**：基于 ReAct 模式，实现“观察-思考-行动”的闭环。

### 3. 实验设计
*   **数据集**：
    *   **TR-OP**：包含 5,000 个钓鱼 URL 和 5,000 个良性 URL。
    *   **DynaPD**：包含 6,075 个钓鱼网站和 6,075 个良性网站。
    *   **SocPhish（本文贡献）**：从 Instagram、TikTok、Twitter 等 5 个社交平台实时抓取的 2,765 个真实可疑 URL，反映了最前线的攻击趋势。
*   **对比基准（Baselines）**：
    *   **PhishLLM**：基于 LLM 的参考列表验证方法。
    *   **PhishIntention**：通过视觉和意图推断钓鱼行为。
    *   **MLLM**：结合视觉信号与 URL 特征的多模态模型。
*   **评估指标**：重点关注**召回率（Recall）**、F1 分数和准确率（ACC），因为在实际部署中，漏掉钓鱼网站的代价远高于误报。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现背景**：由于 MPA 是基于 LLM Agent 的推理框架，其核心开销在于 API 调用或模型推理。文中提到该系统已在 Amazon 生产环境中部署，每周处理约 6 万个高风险 URL，这暗示了其具备良好的工程可扩展性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个不同性质的数据集上进行了性能对比。
    *   进行了详尽的**消融实验**（逐一移除五个工具及记忆模块）。
    *   进行了**敏感性测试**（针对记忆缓存大小 $k$ 和相似度阈值 $\tau$）。
    *   包含**鲁棒性测试**：针对提示词注入攻击（Prompt Injection）、畸形 URL、错误的 JSON 输出以及不同的 HTML 表示格式。
*   **充分性评价**：实验设计非常充分且具有高度的客观性。特别是引入了真实世界的社交媒体数据集（SocPhish）和生产环境部署数据，证明了方法在非实验室环境下的有效性。

### 6 主要结论与发现
*   **性能领先**：MPA 在所有数据集上均优于 SOTA 基准，在真实社交媒体数据集上召回率提升了 20%。
*   **记忆模块的价值**：情节记忆贡献了高达 27% 的召回率增益，且在“完全匹配”路径下显著降低了推理延迟。
*   **动态优于固定**：实验证明，动态选择工具的智能体架构比固定工作流（Deterministic Workflow）更具鲁棒性，能更好地处理隐藏在重定向后的深层威胁。
*   **生产验证**：在实际部署中实现了 91.44% 的召回率，证明了其作为主动防御工具的实用性。

### 7. 优点
*   **动态性**：打破了预定义检测逻辑的局限，能够根据实时证据调整调查策略。
*   **经验复用**：创新的情节记忆机制让模型能够“吃一堑长一智”，在处理变体攻击时更加高效。
*   **多模态融合**：有机结合了文本语义、视觉特征和外部搜索知识，覆盖了钓鱼检测的多个维度。
*   **工程落地**：不仅有理论创新，还有大规模工业级部署的验证。

### 8. 不足与局限
*   **依赖性风险**：系统高度依赖底层多模态 LLM 的推理能力和工具输出的质量，若底层模型发生幻觉，可能影响最终判定。
*   **延迟问题**：尽管记忆模块能加速，但对于“无匹配”的新型 URL，多步推理和多模态工具调用（如截图、搜索）可能带来较高的实时检测延迟。
*   **对抗演进**：虽然测试了提示词注入，但高级攻击者可能会针对“关键词提取”或“Markdown 转换”逻辑设计专门的混淆手段来绕过检测。

（完）
