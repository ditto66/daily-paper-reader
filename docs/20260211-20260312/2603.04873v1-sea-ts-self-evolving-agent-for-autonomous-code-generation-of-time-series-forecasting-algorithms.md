---
title: "SEA-TS: Self-Evolving Agent for Autonomous Code Generation of Time Series Forecasting Algorithms"
title_zh: SEA-TS：用于时间序列预测算法自主代码生成的自进化智能体
authors: "Longkun Xu, Xiaochun Zhang, Qiantu Tuo, Rui Li"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04873v1"
tags: ["query:semma"]
score: 9.0
evidence: 具有迭代自进化循环的自进化代码生成智能体
tldr: SEA-TS是一个自主生成、验证和优化时间序列预测算法代码的自进化智能体框架。针对传统机器学习开发中数据稀缺、适应性差和人工迭代效率低的问题，该框架引入了MA-MCTS搜索、代码审查提示词优化及全局引导推理机制。实验表明，SEA-TS在公开和私有数据集上均显著优于现有SOTA模型，并能自主发现如物理约束单调衰减头等创新架构模式。
selection_source: fresh_fetch
motivation: 传统时间序列预测算法开发面临数据稀缺、分布偏移及人工迭代效率低下的挑战。
method: 提出包含MA-MCTS搜索、自动化代码审查与提示词精炼、以及全局引导推理的自进化闭环框架。
result: "在Solar-Energy基准上MAE降低40%，在私有电力预测任务中也显著优于TimeMixer等SOTA模型。"
conclusion: SEA-TS证明了自主机器学习工程能够超越人工设计，发现具有物理意义且高效的创新算法架构。
---

## 摘要
准确的时间序列预测是各领域决策的基础，但传统的机器学习开发在面对新部署时存在数据稀缺、分布偏移下适应性差以及手动迭代收益递减等问题。我们提出了时间序列算法自进化智能体（SEA-TS），这是一个通过迭代自进化循环自主生成、验证和优化预测代码的框架。我们的框架引入了三项关键创新：(1) 指标优势蒙特卡洛树搜索（MA-MCTS），它用归一化的优势得分取代固定奖励，以提供判别性的搜索引导；(2) 带有运行提示词细化的代码审查，每个执行的方案都会经过自动审查，随后更新编码了纠错模式的提示词，防止类似错误再次发生；(3) 全局可控推理，通过将每个节点与全局最优和最差方案进行比较，实现跨轨迹的知识迁移。我们采用 MAP-Elites 存档来保证架构的多样性。在公开的 Solar-Energy 基准测试中，SEA-TS 生成的代码相对于 TimeMixer 实现了 40% 的 MAE 降低，超越了现有最先进的方法。在私有数据集上，与人工设计的基准相比，SEA-TS 生成的代码在太阳能光伏预测中将 WAPE 降低了 8.6%，在居民负荷预测中降低了 7.7%；在负荷预测中实现了 26.17% 的 MAPE，而 TimeMixer 为 29.34%。值得注意的是，进化出的模型发现了新颖的架构模式——包括编码了太阳辐照度约束的物理启发式单调衰减头、每个站点的学习昼夜周期特征以及可学习的每小时偏差修正——这证明了自主机器学习工程可以产生超越手动设计的真正新颖的算法构思。

## Abstract
Accurate time series forecasting underpins decision-making across domains, yet conventional ML development suffers from data scarcity in new deployments, poor adaptability under distribution shift, and diminishing returns from manual iteration. We propose Self-Evolving Agent for Time Series Algorithms (SEA-TS), a framework that autonomously generates, validates, and optimizes forecasting code via an iterative self-evolution loop. Our framework introduces three key innovations: (1) Metric-Advantage Monte Carlo Tree Search (MA-MCTS), which replaces fixed rewards with a normalized advantage score for discriminative search guidance; (2) Code Review with running prompt refinement, where each executed solution undergoes automated review followed by prompt updates that encode corrective patterns, preventing recurrence of similar errors; and (3) Global Steerable Reasoning, which compares each node against global best and worst solutions, enabling cross-trajectory knowledge transfer. We adopt a MAP-Elites archive for architectural diversity. On the public Solar-Energy benchmark, SEA-TS generated code achieves a 40% MAE reduction relative to TimeMixer, surpassing state-of-the-art methods. On proprietary datasets, SEA-TS generated code reduces WAPE by 8.6% on solar PV forecasting and 7.7% on residential load forecasting compared to human-engineered baselines, and achieves 26.17% MAPE on load forecasting versus 29.34% by TimeMixer. Notably, the evolved models discover novel architectural patterns--including physics-informed monotonic decay heads encoding solar irradiance constraints, per-station learned diurnal cycle profiles, and learnable hourly bias correction--demonstrating that autonomous ML engineering can generate genuinely novel algorithmic ideas beyond manual design.

---

## 论文详细总结（自动生成）

这是一份关于论文《SEA-TS: Self-Evolving Agent for Autonomous Code Generation of Time Series Forecasting Algorithms》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对时间序列预测（TSF）领域中传统机器学习开发范式的三大痛点：**数据稀缺**（新场景冷启动）、**分布偏移**（环境变化导致模型退化）以及**人工迭代收益递减**（专家调优成本极高）。
虽然现有的 LLM 智能体（如 AIDE, ML-Master）已尝试自动化建模，但仍面临以下挑战：
*   **奖励欺骗（Reward Hacking）：** 智能体可能生成逻辑错误（如时间序列中的数据泄露）的代码，虽指标虚高但无法实战。
*   **奖励信号单一：** 简单的二元奖励（+1/0）无法区分微小改进与重大突破。
*   **推理视野狭窄：** 搜索过程通常只参考局部父节点，缺乏全局最优/最差方案的对比。
*   **提示词静态：** 系统提示词在搜索过程中保持不变，无法从失败中实时学习。

### 2. 方法论：核心思想与关键技术
SEA-TS 提出了一个闭环的自进化框架，通过以下核心技术实现算法代码的自主生成与优化：
*   **指标优势蒙特卡洛树搜索 (MA-MCTS)：** 引入“指标优势（Metric Advantage）”概念。它不使用固定奖励，而是计算当前指标相对于历史分布的 **Z-score**（即 $A_j = \frac{\mu(M) - M_j}{\sigma(M)}$）。这种统计归一化的信号能提供更具判别性的搜索引导。
*   **代码审查与运行提示词精炼 (Running Prompt Refinement)：** 
    *   **自动审查：** 对每个成功运行的代码进行逻辑审查，专门检测数据泄露、逆归一化错误等。
    *   **动态更新：** 将审查发现的错误模式和成功经验实时编码进“运行提示词（Running Prompt）”中，确保后续迭代不再犯错。
*   **全局引导推理 (Global Steerable Reasoning)：** 在生成新代码前，强制 LLM 对比当前节点与全局最优（Best）及全局最差（Worst）方案，实现跨轨迹的知识迁移。
*   **MAP-Elites 质量-多样性存档：** 维护一个多维度的精英池（如架构类型、特征工程复杂度、训练策略），确保搜索不会陷入单一的局部最优，保持算法架构的多样性。

### 3. 实验设计
*   **数据集：**
    *   **公开基准：** Solar-Energy（阿拉巴马州137个光伏电站的10分钟间隔数据）。
    *   **工业私有数据：** 包含光伏发电预测（Proprietary PV）和居民电力负荷预测（Proprietary Load）。
*   **Benchmark 与对比方法：**
    *   **TimeMixer：** 2024年提出的多尺度混合 SOTA 模型。
    *   **Timer：** 大规模预训练的时间序列 Transformer 模型。
    *   **人类基准：** 经验丰富的工程师手动设计的模型。
*   **评价指标：** MAE（平均绝对误差）、WAPE（加权平均百分比误差）、MAPE（平均绝对百分比误差）。

### 4. 资源与算力
*   **模型使用：** 采用 **GPT-5**（高推理努力版本）进行核心代码生成与审查，**Qwen3-coder-plus** 作为备选模型。
*   **算力环境：** 部署在 Microsoft Azure 云基础设施上。
*   **耗时：** 搜索预算设为 $T=500$ 次迭代。论文指出，总时长受沙盒执行（模型训练）支配。在并行执行模式下，完成一个完整实验通常需要 **一周以内**。
*   **具体硬件：** 文中未明确给出具体的 GPU 型号（如 A100/H100）或具体卡数，但强调了沙盒执行是主要的计算瓶颈。

### 5. 实验数量与充分性
*   **实验规模：** 针对三个不同性质的数据集（公开光伏、私有光伏、私有负荷）进行了完整测试，每个任务运行 500 次迭代。
*   **消融与验证：** 论文通过展示进化出的具体架构（如“单调衰减头”）来证明各组件的有效性。
*   **客观性：** 实验对比了最新的 SOTA 模型（2024/2025年的模型），并使用了官方代码库进行公平对比。通过引入自动代码审查，排除了因“数据泄露”导致的虚假高性能，增强了结果的可信度。

### 6. 主要结论与发现
*   **性能突破：** 在 Solar-Energy 公开数据集上，SEA-TS 相比 TimeMixer 实现了 **40% 的 MAE 降低**。在私有负荷预测中，MAPE 从 29.34% 降至 26.17%。
*   **自主创新发现：** 智能体自主发现了人类文献中未见或少见的架构模式：
    *   **物理启发式单调衰减头：** 自动编码了太阳光照在午后单调下降的物理约束。
    *   **站点特异性昼夜特征：** 为每个电站学习独特的日周期曲线。
    *   **可学习的小时偏差修正：** 发现了一种基于预测幅值的比例偏差校准技术。
*   **鲁棒性：** 智能体在多个任务中自发选择了 MAD（绝对中位差）标准化而非标准差归一化，表现出对传感器异常值的鲁棒性。

### 7. 优点
*   **闭环进化：** 真正实现了从“代码生成 -> 运行 -> 逻辑审查 -> 知识沉淀 -> 提示词更新”的完整闭环。
*   **解决核心痛点：** 针对时间序列特有的“数据泄露”问题设计了专门的审查机制，避免了 LLM 常见的奖励欺骗。
*   **超越人类设计：** 实验证明了 AI 能够发现具有物理意义的新颖架构，而不仅仅是堆叠现有模块。
*   **搜索效率：** MA-MCTS 配合全局推理，显著提升了在广阔代码空间中的搜索效率。

### 8. 不足与局限
*   **成本问题：** 频繁调用高性能 LLM API（如 GPT-5）产生的高昂 Token 费用是实际应用的一大障碍。
*   **能力边界：** 生成代码的质量仍受限于底层 LLM 的编码上限，对于极度复杂的算法逻辑，LLM 仍可能力有不逮。
*   **维度依赖：** MAP-Elites 的多样性维度（如架构类型、特征复杂度）目前仍需人工预定义，尚未实现完全的自动化维度发现。
*   **执行时长：** 500 次迭代且包含模型训练，对于急需快速部署的场景来说，一周的进化周期可能过长。

（完）
