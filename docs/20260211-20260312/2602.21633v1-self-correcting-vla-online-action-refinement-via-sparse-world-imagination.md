---
title: "Self-Correcting VLA: Online Action Refinement via Sparse World Imagination"
title_zh: 自纠错 VLA：基于稀疏世界想象的在线动作精炼
authors: "Chenyv Liu, Wentao Tan, Lei Zhu, Fengling Li, Jingjing Li, Guoli Yang, Heng Tao Shen"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21633v1"
tags: ["query:semma"]
score: 10.0
evidence: 通过动作细化实现多模态智能体的自我改进
tldr: 针对视觉-语言-动作（VLA）模型缺乏物理动力学理解和自我改进机制的问题，本文提出SC-VLA。该方法通过集成辅助预测头实现“稀疏世界想象”，预测任务进度和未来轨迹趋势，并引入在线动作细化模块，利用预测的未来状态动态调整动作轨迹。实验表明，SC-VLA在模拟和真实机器人任务中均达到SOTA性能，显著提升了成功率并减少了执行步数，展现了强大的物理感知与自我优化能力。
selection_source: fresh_fetch
motivation: 传统VLA模型过度依赖统计先验而缺乏对物理动力学的深刻理解，且现有的世界模型缺乏显式的自我改进机制。
method: 提出SC-VLA框架，通过稀疏世界想象预测任务进度与未来趋势，并结合在线动作细化模块根据预测状态动态调整策略。
result: "在机器人操作任务中，SC-VLA相比基准模型成功率提升了9%，执行步数减少了16%，并在真实世界实验中获得14%的性能增益。"
conclusion: 通过将稀疏想象与在线动作修正相结合，SC-VLA有效增强了VLA模型的物理感知能力和任务执行效率。
---

## 摘要
标准的视觉-语言-动作（VLA）模型依赖于拟合统计数据先验，这限制了它们对底层物理动力学的鲁棒理解。强化学习通过探索增强了物理接地，但通常依赖于与智能体内部状态隔离的外部奖励信号。世界动作模型已成为一种极具前景的范式，它整合了想象与控制以实现预测性规划。然而，它们依赖于隐式上下文建模，缺乏显式的自改进机制。为了解决这些问题，我们提出了自纠错 VLA（SC-VLA），通过稀疏想象内在引导动作精炼，从而实现自改进。我们首先通过集成辅助预测头来设计稀疏世界想象，以预测当前任务进度和未来轨迹趋势，从而约束策略编码短期物理演化。随后，我们引入了在线动作精炼模块来重塑依赖于进度的密集奖励，并根据预测的稀疏未来状态调整轨迹方向。在模拟基准测试和真实世界设置中具有挑战性的机器人操作任务评估表明，SC-VLA 达到了最先进的性能，与表现最佳的基准模型相比，其任务吞吐量最高，步数减少了 16%，成功率提高了 9%，同时在真实世界实验中获得了 14% 的提升。代码可在 https://github.com/Kisaragi0/SC-VLA 获取。

## Abstract
Standard vision-language-action (VLA) models rely on fitting statistical data priors, limiting their robust understanding of underlying physical dynamics. Reinforcement learning enhances physical grounding through exploration yet typically relies on external reward signals that remain isolated from the agent's internal states. World action models have emerged as a promising paradigm that integrates imagination and control to enable predictive planning. However, they rely on implicit context modeling, lacking explicit mechanisms for self-improvement. To solve these problems, we propose Self-Correcting VLA (SC-VLA), which achieve self-improvement by intrinsically guiding action refinement through sparse imagination. We first design sparse world imagination by integrating auxiliary predictive heads to forecast current task progress and future trajectory trends, thereby constraining the policy to encode short-term physical evolution. Then we introduce the online action refinement module to reshape progress-dependent dense rewards, adjusting trajectory orientation based on the predicted sparse future states. Evaluations on challenging robot manipulation tasks from simulation benchmarks and real-world settings demonstrate that SC-VLA achieve state-of-the-art performance, yielding the highest task throughput with 16% fewer steps and a 9% higher success rate than the best-performing baselines, alongside a 14% gain in real-world experiments. Code is available at https://github.com/Kisaragi0/SC-VLA.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SC-VLA（Self-Correcting VLA）** 的新型机器人视觉-语言-动作模型框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的 VLA 模型（如 RT-2, OpenVLA）主要依赖于大规模模仿学习来拟合统计先验，缺乏对底层**物理动力学**的深刻理解。
*   **研究动机**：
    *   **外部奖励依赖**：现有的强化学习（RL）微调方法通常依赖外部定义的奖励函数，这些信号往往与智能体的内部状态脱节。
    *   **世界模型局限**：现有的“世界动作模型”虽能预测未来，但多为隐式建模，缺乏显式的动作纠错和自我改进机制。
*   **整体含义**：SC-VLA 旨在通过“稀疏世界想象”赋予模型预测未来状态的能力，并利用这种内在预测作为奖励信号，实现在线动作的自我精炼。

### 2. 方法论：核心思想与关键技术
SC-VLA 采用两阶段框架，结合了**流匹配（Flow Matching）**策略和**残差强化学习**：

*   **核心思想**：将动作生成与未来状态预测解耦。模型不仅生成动作，还预测任务进度和短期物理演化（位移/旋转），并以此引导动作修正。
*   **关键技术细节**：
    1.  **稀疏世界想象 (SPI)**：在 DiT（Diffusion Transformer）骨干网络的中间层加入辅助预测头，预测当前任务进度 $p_t$ 和未来状态增量 $\Delta s_t$（包括末端执行器的位置、旋转和夹爪状态）。
    2.  **在线动作精炼 (OAR)**：引入一个轻量级的残差模块（基于 SAC 算法）。最终动作 $a_t = a_{base} + \lambda a_{res}$。
    3.  **内在奖励机制**：
        *   **引导奖励 (Guidance Reward)**：计算实际执行的位移方向与 SPI 预测的 $\Delta s_t$ 方向的一致性。
        *   **动态权重调度**：根据预测的任务进度 $p_t$ 动态调整奖励权重，前期依赖预测引导探索，后期转向自主微调。
*   **算法流程**：先通过离线数据训练具备想象能力的基座模型，再在仿真/真实环境中通过残差 RL 进行在线精炼，利用 SPI 生成的内在奖励进行策略优化。

### 3. 实验设计
*   **场景与任务**：
    *   **仿真环境**：使用 ManiSkill3 平台，包含四个高精度挑战任务：StackCube（叠方块）、PlaceSphere（放球）、LiftPegUpright（抬起销钉）、PegInsertion（插销钉）。
    *   **真实世界**：使用 ARX5 机械臂，执行叠方块、放球、插销钉和推方块任务。
*   **Benchmark 与对比方法**：
    *   对比了主流的模仿学习和 VLA 范式：**Diffusion Policy (DP)**、**ACT**、**π0** 以及基座模型 **GR00T N1.5**。
    *   评估指标包括：成功率（Success Rate）和任务吞吐量（通过平均执行步数衡量）。

### 4. 资源与算力
论文明确提到了实验所使用的硬件资源：
*   **GPU 型号与数量**：
    *   **DP/ACT 训练**：单张 NVIDIA RTX 5090。
    *   **π0 训练**：单张 NVIDIA RTX PRO 6000。
    *   **GR00T/SC-VLA 训练**：单张 NVIDIA L40。
*   **训练时长/规模**：基座模型训练 50,000 次迭代，残差 RL 训练根据任务不同在 50万至 300万步之间。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验：每个任务评估 50 个 Episode。
    *   真实世界：每个任务进行 20 次试验。
    *   消融实验：针对进度预测（$p_t$）、状态预测（$\Delta s_t$）、内在奖励和动态权重分别做了对比实验。
*   **充分性与公平性**：实验设计较为充分。所有方法在相同的演示数据量（100 条/任务）下训练，确保了数据层面的公平性。通过消融实验验证了 SPI 各个组件的必要性，证明了性能提升并非单纯来自残差结构，而是来自“想象”的引导。

### 6. 主要结论与发现
*   **性能领先**：SC-VLA 在所有任务中均达到 SOTA，平均成功率比最强基准提升了 9%。
*   **高效率**：SC-VLA 的执行步数比基准减少了 16%，显著提升了机器人作业的吞吐量。
*   **物理感知**：显式建模短期物理演化（$\Delta s_t$）比单纯的语义对齐更能提升接触密集型任务（如插销钉）的稳定性。
*   **自改进能力**：即使在没有外部密集奖励的情况下，利用内在想象奖励也能有效引导模型完成复杂的长程任务。

### 7. 优点（亮点）
*   **内在奖励设计**：巧妙地将世界模型的预测转化为 RL 的密集奖励，解决了稀疏奖励环境下探索难的问题，且无需人工设计复杂的奖励函数。
*   **稀疏想象的效率**：相比于预测高维像素（视频生成），预测稀疏的物理状态增量计算开销极低，更适合实时控制。
*   **动态调节机制**：引入任务进度感知的权重调度，平衡了先验知识与自主探索。

### 8. 不足与局限
*   **残差缩放系数（$\lambda$）的敏感性**：实验中提到不同任务需要不同的残差缩放系数（如 PegInsertion 需要 0.1，而其他任务需要 0.01），这表明模型对超参数仍有一定依赖，缺乏完全的自适应性。
*   **短期预测限制**：目前的 SPI 主要关注短期（Short-horizon）物理演化，对于需要极长程逻辑推理的任务，其引导效果可能受限。
*   **对基座模型的依赖**：如果基座模型的 SPI 预测出现严重偏差，可能会通过内在奖励误导残差策略的收敛。

（完）
