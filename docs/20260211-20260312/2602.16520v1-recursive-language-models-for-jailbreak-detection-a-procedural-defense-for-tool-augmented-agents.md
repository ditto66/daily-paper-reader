---
title: "Recursive language models for jailbreak detection: a procedural defense for tool-augmented agents"
title_zh: 递归语言模型用于越狱检测：一种针对工具增强型智能体的程序化防御方案
authors: Doron Shavit
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16520v1"
tags: ["query:ai-mmd"]
score: 7.0
evidence: 针对工具增强型智能体越狱攻击的程序化防御
tldr: 针对大语言模型特别是工具增强型智能体面临的越狱攻击威胁，本文提出了RLM-JB框架。该框架利用递归语言模型将越狱检测从单次分类转变为程序化分析，通过输入规范化、分块并行筛选及跨块信号聚合，有效应对长上下文隐藏和语义伪装等攻击手段。实验表明，该方法在保持极高精确度的同时，显著提升了对复杂越狱指令的检测召回率，为智能体安全提供了可审计的防御方案。
selection_source: fresh_fetch
motivation: 传统的单次检测方法难以应对利用长上下文隐藏、语义伪装和轻量级混淆手段的演进式越狱攻击。
method: 提出RLM-JB框架，通过根模型协调分析程序，对输入进行去混淆、分块处理及并行筛选，并聚合证据生成决策。
result: "在AutoDAN等对抗性输入测试中，该框架实现了92.5%-98.0%的召回率，同时保持了98.99%-100%的高精确度。"
conclusion: 将越狱检测视为一种递归的程序化过程而非简单的分类任务，能有效提升工具增强型智能体防御复杂攻击的鲁棒性。
---

## 摘要
越狱提示词（Jailbreak prompts）是大语言模型（LLMs）面临的一种实际且不断演变的威胁，特别是在对不可信内容执行工具的智能体系统中。许多攻击利用长上下文隐藏、语义伪装和轻量级混淆手段，从而规避单次通过的护栏（guardrails）机制。我们提出了 RLM-JB，这是一个基于递归语言模型（RLMs）构建的端到端越狱检测框架。在该框架中，根模型编排一个有界的分析程序，对输入进行转换，在覆盖的分段上查询工作模型，并将证据汇总为可审计的决策。RLM-JB 将检测视为一个过程而非单次分类：它对可疑输入进行归一化和去混淆处理，对文本进行分块以减少上下文稀释并保证覆盖范围，执行并行分块筛选，并组合跨分块信号以恢复拆分有效载荷（split-payload）攻击。在 AutoDAN 风格的对抗性输入上，RLM-JB 在三种 LLM 后端上均实现了极高的检测效能（ASR/召回率达 92.5-98.0%），同时保持了极高的精确率（98.99-100%）和较低的误报率（0.0-2.0%），突显了随着筛选后端变化而产生的实际灵敏度与特异性之间的权衡。

## 速览
**TLDR**：针对大语言模型特别是工具增强型智能体面临的越狱攻击威胁，本文提出了RLM-JB框架。该框架利用递归语言模型将越狱检测从单次分类转变为程序化分析，通过输入规范化、分块并行筛选及跨块信号聚合，有效应对长上下文隐藏和语义伪装等攻击手段。实验表明，该方法在保持极高精确度的同时，显著提升了对复杂越狱指令的检测召回率，为智能体安全提供了可审计的防御方案。 \
**Motivation**：传统的单次检测方法难以应对利用长上下文隐藏、语义伪装和轻量级混淆手段的演进式越狱攻击。 \
**Method**：提出RLM-JB框架，通过根模型协调分析程序，对输入进行去混淆、分块处理及并行筛选，并聚合证据生成决策。 \
**Result**：在AutoDAN等对抗性输入测试中，该框架实现了92.5%-98.0%的召回率，同时保持了98.99%-100%的高精确度。 \
**Conclusion**：将越狱检测视为一种递归的程序化过程而非简单的分类任务，能有效提升工具增强型智能体防御复杂攻击的鲁棒性。

---

## Abstract
Jailbreak prompts are a practical and evolving threat to large language models (LLMs), particularly in agentic systems that execute tools over untrusted content. Many attacks exploit long-context hiding, semantic camouflage, and lightweight obfuscations that can evade single-pass guardrails. We present RLM-JB, an end-to-end jailbreak detection framework built on Recursive Language Models (RLMs), in which a root model orchestrates a bounded analysis program that transforms the input, queries worker models over covered segments, and aggregates evidence into an auditable decision. RLM-JB treats detection as a procedure rather than a one-shot classification: it normalizes and de-obfuscates suspicious inputs, chunks text to reduce context dilution and guarantee coverage, performs parallel chunk screening, and composes cross-chunk signals to recover split-payload attacks. On AutoDAN-style adversarial inputs, RLM-JB achieves high detection effectiveness across three LLM backends (ASR/Recall 92.5-98.0%) while maintaining very high precision (98.99-100%) and low false positive rates (0.0-2.0%), highlighting a practical sensitivity-specificity trade-off as the screening backend changes.

---

## 论文详细总结（自动生成）

以下是对论文《Recursive language models for jailbreak detection: a procedural defense for tool-augmented agents》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）在工具增强型智能体（Agent）中的广泛应用，越狱攻击（Jailbreak）变得愈发复杂。攻击者利用长上下文隐藏、语义伪装、编码混淆和拆分有效载荷（split-payload）等手段，能够轻易规避传统的单次（single-pass）静态过滤器或分类器。
*   **核心问题**：如何在动态、状态化的智能体环境中，针对不断演变的对抗性提示词提供一种鲁棒、可审计且覆盖全面的防御机制？
*   **整体含义**：论文提出了 **RLM-JB** 框架，将越狱检测从简单的“一次性分类任务”转变为一种“程序化的递归分析过程”。

### 2. 方法论
RLM-JB 基于递归语言模型（RLM）架构，其核心思想是将输入文本外化到计算环境中，由一个“根模型”（Root Model）编排一系列分析步骤：
*   **规范化与去混淆**：首先对输入进行格式清洗，并自动识别 Base64 等编码模式进行解码，确保后续分析能处理原始语义。
*   **保证覆盖的分块处理（Chunking）**：将长文本切分为重叠的片段。这一步是为了解决 LLM 的“中间迷失”（lost in the middle）问题，确保攻击指令不会因上下文过长而被稀释。
*   **并行分块筛选**：调用多个工作模型（Worker Models）并行分析每个分块，提取恶意意图、置信度及具体的攻击向量（如角色扮演、指令劫持等）。
*   **证据聚合与推理**：根模型收集所有分块的信号。如果攻击被拆分在不同块中，聚合逻辑能够通过跨块关联恢复攻击假设，最终输出结构化的判定结果及审计理由。

### 3. 实验设计
*   **场景与数据集**：
    *   使用 **AutoDAN** 风格的基准测试，模拟复杂的工具调用和多步任务环境。
    *   引入 **InjectPrompt** 语料库，测试最新的提示词注入攻击及其变体。
*   **Benchmark 设置**：每个后端测试 400 个对抗性案例和 200 个良性提示词（用于评估误报率）。
*   **对比方法**：
    *   **基准对比**：直接使用 GPT-5.2 进行单次检测（无 RLM-JB 流程）。
    *   **横向对比**：与 Granite Guardian 3.0、Llama-Guard 2 和 JBShield 等现有护栏模型进行性能指标对比。
*   **后端模型**：测试了 DeepSeek-V3.2、GPT-4o 和 GPT-5.2 三种不同的筛选后端。

### 4. 资源与算力
*   **算力说明**：文中未明确提及具体的 GPU 型号或训练时长。由于该框架主要基于现有的 LLM API（如 GPT 和 DeepSeek 系列）进行编排，其核心开销在于推理阶段。
*   **开销反馈**：作者指出 RLM-JB 增加了延迟和成本。在实验中，使用 GPT-5 后端的 RLM-JB 处理时间比基准单次调用增加了约 **3 倍**。

### 5. 实验数量与充分性
*   **实验规模**：针对每个后端进行了 600 组实验（共计 1800+ 组核心实验），并补充了 InjectPrompt 的专项测试。
*   **充分性评价**：实验覆盖了主流的先进模型后端，并对比了业界知名的防御模型，数据具有说服力。
*   **局限性**：良性样本（用于计算 FPR）是由 LLM 生成的合成数据，而非真实世界的用户流量，这可能导致误报率的估计存在一定偏差。

### 6. 主要结论与发现
*   **程序化防御的优越性**：RLM-JB 在 AutoDAN 攻击下的召回率（ASR）达到 **92.5%–98.0%**，远高于 GPT-5.2 基准的 59.57%。
*   **高精确度**：在所有测试后端中，精确率保持在 **98.99%–100%**，误报率极低（0.0%–2.0%）。
*   **性能权衡**：随着后端模型能力的增强（从 DeepSeek 到 GPT-5.2），检测灵敏度（Recall）提升，但误报率（FPR）也随之轻微上升，体现了安全防御中的灵敏度-特异性权衡。
*   **F1 分数提升**：相比 Llama-Guard 2 等模型，RLM-JB 在 F1 指标上实现了 20%–30% 的相对提升。

### 7. 优点
*   **结构化与可审计**：不仅给出“是/否”的判断，还提供分块证据和攻击向量分析，便于安全人员回溯。
*   **应对复杂攻击**：通过去混淆和跨块聚合，有效破解了针对单次检测器的“语义伪装”和“载荷拆分”策略。
*   **后端无关性**：可以灵活更换底层的筛选模型，具有良好的扩展性。

### 8. 不足与局限
*   **延迟与成本**：由于涉及多次模型调用和分块处理，实时性较差，不适合对延迟极度敏感的交互场景。
*   **自适应攻击风险**：尚未充分测试针对该“程序化流程”本身进行优化的自适应攻击者（Adaptive Adversaries）。
*   **依赖代码执行**：RLM 架构依赖沙箱环境执行编排代码，对于不支持代码执行环境的部署场景存在限制。

（完）
