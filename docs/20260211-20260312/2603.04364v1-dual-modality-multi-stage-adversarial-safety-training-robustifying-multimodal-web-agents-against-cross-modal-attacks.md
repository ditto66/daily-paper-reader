---
title: "Dual-Modality Multi-Stage Adversarial Safety Training: Robustifying Multimodal Web Agents Against Cross-Modal Attacks"
title_zh: 双模态多阶段对抗安全训练：增强多模态 Web 智能体抵御跨模态攻击的鲁棒性
authors: "Haoyu Liu, Dingcheng Li, Lukas Rutishauser, Zeyu Zheng"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04364v1"
tags: ["query:ai-mmd"]
score: 9.0
evidence: 增强多模态网络智能体对抗跨模态欺骗性内容的能力
tldr: 针对多模态网络智能体易受跨模态攻击（同时干扰截图和辅助功能树）的问题，本文提出DMAST框架。该框架将智能体与攻击者的交互建模为零和博弈，通过模仿学习、引导式微调及对抗性强化学习三个阶段进行协同训练。实验表明，DMAST在提升安全性的同时，显著提高了任务完成效率，在分布外任务上表现出极强的鲁棒性。
selection_source: fresh_fetch
motivation: 现有多模态网络智能体在面对同时操纵视觉和文本通道的跨模态注入攻击时存在严重安全漏洞，且视觉攻击比纯文本攻击更具威胁。
method: 提出DMAST框架，通过模仿学习、零确认策略引导的监督微调以及基于GRPO的对抗性强化学习自博弈来增强智能体的防御能力。
result: 在分布外任务上，DMAST在大幅降低对抗风险的同时，将任务完成效率提高了一倍，显著优于现有的防御方案。
conclusion: 该研究证明了通过双模态协同进化训练可以有效提升多模态智能体的安全性，并使其在复杂未知环境中具备稳健的泛化能力。
---

## 摘要
处理屏幕截图和可访问性树（accessibility trees）的多模态 Web 智能体正越来越多地被部署用于与 Web 界面交互，然而其双流架构开启了一个尚未被充分探索的攻击面：攻击者向网页 DOM 注入内容，可同时利用一致的欺骗性叙述破坏两个观测通道。我们在 MiniWob++ 上的漏洞分析表明，包含视觉组件的攻击效果远优于纯文本注入，这暴露了以文本为中心的视觉语言模型（VLM）安全训练中的关键缺陷。受此发现启发，我们提出了双模态多阶段对抗安全训练（DMAST），该框架将智能体与攻击者的交互形式化为两人零和马尔可夫博弈，并通过三阶段流水线对双方进行协同训练：(1) 从强教师模型进行模仿学习；(2) 采用一种新颖的“零确认”（zero-acknowledgment）策略进行预言机引导的有监督微调，以在对抗噪声下培养专注于任务的推理能力；(3) 通过群体相对策略优化（GRPO）自博弈进行对抗性强化学习。在分布外任务上，DMAST 在大幅降低对抗风险的同时，将任务完成效率提高了一倍。我们的方法显著优于现有的基于训练和基于提示词的防御手段，展示了真正的协同进化进展以及对复杂、未知环境的鲁棒泛化能力。

## Abstract
Multimodal web agents that process both screenshots and accessibility trees are increasingly deployed to interact with web interfaces, yet their dual-stream architecture opens an underexplored attack surface: an adversary who injects content into the webpage DOM simultaneously corrupts both observation channels with a consistent deceptive narrative. Our vulnerability analysis on MiniWob++ reveals that attacks including a visual component far outperform text-only injections, exposing critical gaps in text-centric VLM safety training. Motivated by this finding, we propose Dual-Modality Multi-Stage Adversarial Safety Training (DMAST), a framework that formalizes the agent-attacker interaction as a two-player zero-sum Markov game and co-trains both players through a three-stage pipeline: (1) imitation learning from a strong teacher model, (2) oracle-guided supervised fine-tuning that uses a novel zero-acknowledgment strategy to instill task-focused reasoning under adversarial noise, and (3) adversarial reinforcement learning via Group Relative Policy Optimization (GRPO) self-play. On out-of-distribution tasks, DMAST substantially mitigates adversarial risks while simultaneously doubling task completion efficiency. Our approach significantly outperforms established training-based and prompt-based defenses, demonstrating genuine co-evolutionary progress and robust generalization to complex, unseen environments.

---

## 论文详细总结（自动生成）

这篇论文针对多模态 Web 智能体的安全性提出了创新的防御框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代 Web 智能体通常同时处理**屏幕截图（视觉）**和**辅助功能树（文本/结构）**。研究发现，攻击者通过向网页 DOM 注入恶意 HTML，可以同时在视觉和文本通道制造一致的欺骗信息（跨模态攻击）。
*   **研究动机**：
    *   **现有防御的局限性**：目前的视觉语言模型（VLM）安全训练多侧重于文本，对视觉欺骗（如伪造的系统弹窗、排版覆盖）防御力薄弱。
    *   **攻击有效性**：实验证明，包含视觉组件的攻击（成功率 ~35%）远比纯文本注入（成功率 ~24%）更有效。
    *   **安全与效能的权衡**：简单的提示词防御（Prompt Defense）会导致智能体过度保守，出现“拒绝执行”现象，从而降低任务完成率。

### 2. 方法论：DMAST 框架
论文将智能体与攻击者的交互建模为**两人零和马尔可夫博弈**，并提出了**双模态多阶段对抗安全训练（DMAST）**：

*   **核心思想**：让智能体与攻击者在同一模型权重下协同进化，通过模拟真实网页注入机制（HTML/CSS 注入）来增强鲁棒性。
*   **关键技术阶段**：
    1.  **阶段 1：模仿学习（IL）**：从强大的教师模型（Gemma-3-27B）中蒸馏专家轨迹，为学生模型（Gemma-3-12B）提供稳定的初始策略。
    2.  **阶段 2：预言机引导的 SFT（Oracle-Guided SFT）**：
        *   **零确认策略（Zero-Acknowledgment）**：利用预言机生成“无视攻击”的思维链（CoT）。即使观测到攻击内容，推理过程也只关注与任务相关的合法元素，不提及或承认攻击的存在。
        *   **去噪训练**：强制智能体在受到干扰的观测下保持目标不变。
    3.  **阶段 3：对抗性强化学习（Self-play RL）**：
        *   使用**群体相对策略优化（GRPO）**算法。
        *   智能体和攻击者进行自博弈，攻击者学习如何绕过防御，智能体学习如何在更复杂的攻击下生存。

### 3. 实验设计
*   **数据集/场景**：
    *   **MiniWob++**：用于训练和基础评估（125 个任务，预留 28 个用于测试）。
    *   **VisualWebArena (VWA)**：用于**分布外（OOD）**评估，包含 100 个更复杂的真实网页任务。
*   **Benchmark 与对比方法**：
    *   **对比方法**：SPAG（自博弈 RL）、ART（自动红队测试）、Online SFT（在线微调）、纯提示词防御（Prompt Defense）以及基座模型。
    *   **评估指标**：任务成功率（TSR ↑）和攻击成功率（ASR ↓）。

### 4. 资源与算力
*   **模型使用**：学生模型为 **Gemma-3-12B-IT**，教师模型为 **Gemma-3-27B-IT**。
*   **算力细节**：
    *   使用了 **vLLM** 推理引擎和 **LoRA** 微调技术（Rank=128）。
    *   RL 阶段使用了 **64 个并行工作节点（workers）**进行数据采集。
    *   **未明确说明**具体的 GPU 型号（如 H100/A100）数量及总训练时长（小时/天）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   漏洞分析阶段进行了 1,000 个回合的评估。
    *   RL 训练进行了 10 个迭代，每个迭代包含 48 个任务，每个任务 16 个回合。
    *   **充分性**：实验涵盖了从简单（MiniWob++）到复杂（VWA）的跨度，并进行了详细的**消融实验**（验证阶段 2 的必要性）和**交叉评估**（验证攻击者与防御者的协同进化）。
*   **客观性**：通过固定教师模型作为评估攻击者，确保了不同防御方法在同一强度攻击下的公平对比。

### 6. 主要结论与发现
*   **防御效果**：DMAST 在 VWA 任务上将攻击成功率从 41.2% 降至 21.4%，同时将任务成功率从 6.2% 提升至 10.2%。
*   **协同进化**：随着训练进行，攻击者生成的 HTML 注入变得更加多样化（词汇多样性和策略复杂性均有提升），不再依赖简单模板。
*   **阶段贡献**：SFT 阶段主要提升任务效能，而 RL 阶段显著增强了安全性。

### 7. 优点
*   **跨模态一致性**：通过 HTML 注入机制，确保了截图和辅助功能树的攻击内容完全同步，比单纯的图像扰动更符合真实威胁。
*   **零确认策略**：这是一种新颖的防御哲学，通过“视而不见”来避免智能体被诱导进入防御性解释的陷阱。
*   **高效性**：智能体与攻击者共享权重，减少了内存占用并促进了内部特征的对抗学习。

### 8. 不足与局限
*   **绝对性能受限**：虽然相对提升显著，但在复杂任务（VWA）上的绝对成功率（10.2%）仍然较低，这可能受限于 12B 学生模型的固有能力。
*   **攻击类型单一**：目前主要针对**敏感数据泄露**，尚未广泛测试控制流劫持或虚假信息传播等其他攻击目标。
*   **算力透明度**：缺乏具体的训练资源消耗数据，难以评估该方案的训练成本。

（完）
