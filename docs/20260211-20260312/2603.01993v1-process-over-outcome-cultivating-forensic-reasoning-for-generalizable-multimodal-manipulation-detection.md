---
title: "Process Over Outcome: Cultivating Forensic Reasoning for Generalizable Multimodal Manipulation Detection"
title_zh: 过程重于结果：培养用于可泛化多模态篡改检测的取证推理
authors: "Yuchen Zhang, Yaxiong Wang, Kecheng Han, Yujiao Wu, Lianwei Wu, Li Zhu, Zhedong Zheng"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01993v1"
tags: ["query:q4"]
score: 10.0
evidence: 使用取证推理的可泛化多模态操纵检测
tldr: 针对生成式AI带来的多模态篡改检测挑战，本文提出REFORM框架，强调“过程优于结果”。不同于传统仅关注分类的方法，REFORM通过三阶段课程学习（诱导取证逻辑、对齐推理与判断、强化学习优化逻辑一致性）培养模型的取证推理能力。配合新发布的带有丰富推理标注的ROM数据集，该方法在多个基准测试中显著提升了检测的泛化性和可解释性。
selection_source: fresh_fetch
motivation: 现有的篡改检测方法过度依赖结果导向的监督，导致模型容易过拟合表面伪影且缺乏可解释性，难以应对未见的篡改模式。
method: 提出了REFORM框架，通过诱导取证理由、对齐推理与判断以及利用强化学习精炼逻辑一致性的三阶段课程进行建模。
result: 在ROM、DGM4和MMFakeBench等多个数据集上达到了SOTA性能，展现出卓越的泛化能力。
conclusion: 引入显式取证推理过程是提升多模态篡改检测泛化性和可靠性的关键路径。
---

## 摘要
生成式人工智能的最新进展显著增强了多模态媒体篡改的真实性，从而对篡改检测提出了巨大挑战。现有的篡改检测和定位方法主要集中在结果导向监督下的篡改类型分类，这不仅缺乏可解释性，而且往往容易过拟合于表层伪影。在本文中，我们认为可泛化的检测需要引入显式的取证推理，而不仅仅是分类有限的篡改类型，因为后者无法泛化到未见的篡改模式。为此，我们提出了 REFORM，这是一个推理驱动的框架，将学习重点从结果拟合转向过程建模。REFORM 采用了一个三阶段课程，首先诱导取证依据，然后将推理与最终判断对齐，最后通过强化学习优化逻辑一致性。为了支持这一范式，我们引入了 ROM，这是一个具有丰富推理标注的大规模数据集。广泛的实验表明，REFORM 建立了新的最先进性能，并具有卓越的泛化能力，在 ROM 上达到 81.52% 的准确率，在 DGM4 上达到 76.65% 的准确率，在 MMFakeBench 上达到 74.9 的 F1 分数。

## Abstract
Recent advances in generative AI have significantly enhanced the realism of multimodal media manipulation, thereby posing substantial challenges to manipulation detection. Existing manipulation detection and grounding approaches predominantly focus on manipulation type classification under result-oriented supervision, which not only lacks interpretability but also tends to overfit superficial artifacts. In this paper, we argue that generalizable detection requires incorporating explicit forensic reasoning, rather than merely classifying a limited set of manipulation types, which fails to generalize to unseen manipulation patterns. To this end, we propose REFORM, a reasoning-driven framework that shifts learning from outcome fitting to process modeling. REFORM adopts a three-stage curriculum that first induces forensic rationales, then aligns reasoning with final judgments, and finally refines logical consistency via reinforcement learning. To support this paradigm, we introduce ROM, a large-scale dataset with rich reasoning annotations. Extensive experiments show that REFORM establishes new state-of-the-art performance with superior generalization, achieving 81.52% ACC on ROM, 76.65% ACC on DGM4, and 74.9 F1 on MMFakeBench.

---

## 论文详细总结（自动生成）

这是一份关于论文《Process Over Outcome: Cultivating Forensic Reasoning for Generalizable Multimodal Manipulation Detection》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：随着生成式AI（如Stable Diffusion, FLUX）的发展，多模态媒体篡改（如换脸、全场景合成）变得极其逼真。
*   **核心痛点**：现有的检测方法大多是“结果导向”的，即模型仅学习将输入映射到“真/假”标签。这种方式导致模型倾向于拟合训练集中的特定统计伪影（快捷学习），在面对未见的篡改模式时泛化性极差，且缺乏可解释性。
*   **核心思想**：论文主张“过程重于结果”，认为模型应像人类取证专家一样，通过逻辑推理链（Forensic Reasoning）来识别不一致性，从而实现更强的泛化能力。

### 2. 方法论：REFORM 框架
REFORM（Reasoning-Enhanced Forensic Optimization via Reinforcement Modeling）是一个推理驱动的框架，核心技术包括：
*   **架构设计**：采用“编码器-双解码器”结构。
    *   **认知启动编码器（Ep）**：利用可学习的“推理 Token”从冻结的语义上下文中提取取证信号。
    *   **双解码器（Dr & Da）**：推理解码器（Dr）生成详细的取证理由，答案解码器（Da）输出分类标签和篡改区域坐标。
*   **三阶段课程学习流程**：
    1.  **认知推理预热**：通过数据蒸馏获得的取证理由（Rationales）对模型进行初步监督，使其学会表达取证逻辑。
    2.  **推理赋能联合微调**：同时优化推理和答案生成。引入**推理-答案一致性损失（$L_{RAC}$）**，通过铰链损失（Hinge Loss）强制推理链的语义表示与最终答案对齐。
    3.  **约束感知策略精炼（RL阶段）**：采用 **GRPO（组相对策略优化）** 强化学习算法。
        *   **奖励函数设计**：包含一致性奖励（利用预训练的 TinyBERT 验证推理是否支持答案）、准确性奖励、定位奖励（IoU）和格式奖励。

### 3. 实验设计
*   **数据集**：
    *   **ROM（自建）**：包含 70.4 万个样本，涵盖 9 种篡改类型（从人脸编辑到全场景生成），并附带由 InternVL3.5 蒸馏出的详细推理标注。
    *   **通用基准**：DGM4（多模态篡改定位）、MMFakeBench（混合来源误导信息）。
    *   **泛化性测试**：在 MDSM、MiRAGe、AIGUARD（电商风险）、COSMOS、DiffFace 等多个未见数据集上进行零样本测试。
*   **对比方法**：
    *   传统检测器：ViLT, HAMMER, HAMMER++。
    *   SOTA 专用模型：FKA-Owl, AMD。
    *   通用大模型：GPT-4o, Gemini 2.5, Qwen2.5-VL (72B), LLaVA-v1.6 (34B) 等。

### 4. 资源与算力
*   **硬件环境**：使用 **NVIDIA GeForce RTX 4090 GPU** 进行分布式训练（DDP）。
*   **模型规模**：REFORM 基于 Florence-2-B，总参数量约为 **376M**。
*   **效率表现**：在 4090 上，解释模式（生成推理）速度为 1.03 pairs/sec；快速模式（仅输出标签）速度达 **13.17 pairs/sec**，具备实时处理潜力。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括跨域测试（在 NYT 训练，在 BBC/Wash. 等测试）、零样本跨任务测试、以及针对 9 种不同篡改类型的细粒度评估。
*   **消融实验**：针对三阶段课程的每个组件、推理 Token 的长度、强化学习的奖励项配置、以及 RL 训练中的组大小（G）进行了深入分析。
*   **客观性**：引入了**人类评估**（18名志愿者），对模型生成的推理理由进行“说服力”评分，证明了其可解释性的有效性。整体实验设计严谨，对比基准涵盖了从轻量级模型到超大规模闭源模型，非常充分。

### 6. 主要结论
*   **推理驱动的优越性**：显式建模推理过程能显著提升模型在未见领域和未见篡改模式下的泛化能力。
*   **性能突破**：REFORM 在 ROM 上达到 81.52% ACC，在 DGM4 上达到 76.65% ACC，显著超过了参数量大得多的通用大模型（如 GPT-4o）。
*   **强化学习的作用**：GRPO 能够有效纠正监督学习中的“暴露偏差”，通过自我探索优化逻辑一致性。

### 7. 优点与亮点
*   **范式创新**：从简单的“结果拟合”转向“逻辑建模”，为篡改检测提供了新的研究路径。
*   **高质量资源**：发布的 ROM 数据集规模大、标注细（含推理链），对社区贡献巨大。
*   **高效且灵活**：双解码器设计允许模型在“解释模式”和“快速模式”间切换，兼顾了可解释性和工业部署的推理效率。

### 8. 不足与局限
*   **教师模型依赖**：推理标注源自 InternVL 的蒸馏，如果教师模型存在知识错误或幻觉，可能会传递给学生模型。
*   **推理延迟**：在需要生成详细理由的“解释模式”下，自回归生成过程会导致较高的延迟。
*   **极端环境挑战**：在图像质量极低（高噪声、严重像素化）的情况下，模型仍可能出现感知失效。

（完）
