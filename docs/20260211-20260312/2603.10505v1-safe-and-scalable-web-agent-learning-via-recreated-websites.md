---
title: Safe and Scalable Web Agent Learning via Recreated Websites
title_zh: 通过重建网站实现安全且可扩展的 Web 智能体学习
authors: "Hyungjoo Chae, Jungsoo Park, Alan Ritter"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10505v1"
tags: ["query:semma"]
score: 9.0
evidence: 通过合成环境实现 Web 智能体的大规模自进化
tldr: 针对真实网站在训练Web智能体时存在的不安全、难重置及缺乏验证反馈等问题，本文提出VeriEnv框架。该框架利用大模型将真实网站克隆为可执行且可验证的合成环境，并通过Python SDK提供内部访问权限，使智能体能自主生成任务并获得确定性奖励。实验证明，该方法能有效提升智能体的泛化能力、特定站点精通度及扩展性，实现了安全且可扩展的自我进化学习。
selection_source: fresh_fetch
motivation: 真实网站环境存在安全风险、重置困难且难以提供程序化验证反馈，严重限制了Web智能体的规模化训练与评估。
method: 提出VeriEnv框架，利用LLM自动克隆真实网站并构建带Python SDK的合成环境，支持任务自生成与确定性奖励验证。
result: 实验表明，使用VeriEnv训练的智能体在未知网站上具有强泛化性，并能通过增加训练环境数量实现性能的持续扩展。
conclusion: VeriEnv通过将智能体学习与不安全的真实环境交互解耦，为构建安全、可验证且可大规模扩展的Web智能体提供了新范式。
---

## 摘要
训练自主 Web 智能体从根本上受到其学习环境的限制：现实世界的网站探索起来不安全，难以重置，且很少提供可验证的反馈。我们提出了 VeriEnv，这是一个将语言模型视为环境创建者的框架，能够自动将现实世界的网站克隆为完全可执行、可验证的合成环境。通过 Python SDK 提供受控的内部访问，VeriEnv 使智能体能够自主生成具有确定性、程序化可验证奖励的任务，从而消除了对启发式或基于 LLM 的评判器的依赖。这种设计将智能体学习与不安全的现实世界交互解耦，同时通过环境扩展实现可扩展的自我进化。通过在 Web 智能体基准测试上的实验，我们证明了使用 VeriEnv 训练的智能体能够泛化到未见过的网站，通过自我进化训练实现特定站点的精通，并从训练环境数量的扩展中获益。代码和资源将在论文接收后发布于 https://github.com/kyle8581/VeriEnv。

## Abstract
Training autonomous web agents is fundamentally limited by the environments they learn from: real-world websites are unsafe to explore, hard to reset, and rarely provide verifiable feedback. We propose VeriEnv, a framework that treats language models as environment creators, automatically cloning real-world websites into fully executable, verifiable synthetic environments. By exposing controlled internal access via a Python SDK, VeriEnv enables agents to self-generate tasks with deterministic, programmatically verifiable rewards, eliminating reliance on heuristic or LLM-based judges. This design decouples agent learning from unsafe real-world interaction while enabling scalable self-evolution through environment expansion. Through experiments on web agent benchmarks, we show that agents trained with VeriEnv generalize to unseen websites, achieve site-specific mastery through self-evolving training, and benefit from scaling the number of training environments. Code and resources will be released at https://github.com/kyle8581/VeriEnv upon acceptance.

---

## 论文详细总结（自动生成）

### 论文总结：Safe and Scalable Web Agent Learning via Recreated Websites

#### 1. 核心问题与整体含义
训练自主 Web 智能体（Web Agents）目前面临三大瓶颈：
*   **安全性与限制**：在真实网站上进行强化学习探索可能导致误操作（如意外下单）、违反平台政策或被防火墙（如 CAPTCHA）拦截。
*   **环境不可控**：真实网站难以重置到初始状态，导致实验不可重复。
*   **反馈不透明**：缺乏程序化的验证机制，现有研究多依赖于不可靠的“LLM 评判器”或启发式规则来评估任务是否完成。

本文提出 **VeriEnv** 框架，其核心含义是**将语言模型（LLM）作为环境创建者**。通过自动克隆真实网站并构建全栈合成环境，将智能体的训练与不安全的现实交互解耦，从而实现安全、可验证且可大规模扩展的自进化学习。

#### 2. 方法论
VeriEnv 的工作流程分为三个关键阶段：
*   **网站重建（Recreating Websites）**：利用具备文件系统和终端访问权限的 Coding Agent（如 GPT-5.2），根据真实网站的截图自动编写前端代码、后端逻辑和数据库架构。通过迭代调试（使用 Playwright MCP 捕获 Bug 并修复），生成功能和视觉上高度接近真实的合成环境。
*   **可验证任务与评判器生成**：在合成环境中，LLM 不仅生成自然语言任务指令，还利用环境自带的 **Python SDK** 编写验证程序。该程序能直接查询数据库状态（例如：检查订单是否已存入数据库），从而提供确定性的、非启发式的奖励信号。
*   **自进化学习循环**：智能体在合成环境中执行任务，系统通过 SDK 自动验证轨迹。有效的成功轨迹被保留，用于对智能体进行拒绝采样微调（Rejection Fine-tuning），使其在不断扩展的环境中持续进化。

#### 3. 实验设计
*   **数据集与场景**：
    *   **跨域泛化实验**：在 97 个重建网站上训练，在 **WebArena-Lite** 和 **Mind2Web-Online** 基准测试上评估（确保测试网站在训练集中未出现）。
    *   **特定站点精通实验**：针对特定网站进行反复训练，观察智能体在单一领域内的性能提升。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, GPT-4o-mini, Claude-3.5-Sonnet。
    *   **开源基座与专用协议**：Qwen3-4B, LLaMA-3.2-3B-Instruct，以及 Synatra（合成轨迹方法）和 ADP（代理数据协议）。
    *   **自进化基准**：与 PAE（依赖真实网站和 LLM 评判器的自进化方法）进行对比。

#### 4. 资源与算力
*   **环境构建成本**：平均每个网站的克隆过程耗时 **83.5 分钟**，成本约为 **3.6 美元**（含调试和任务生成）。
*   **训练硬件**：使用了 **2 张 NVIDIA A40 GPU**。
*   **训练配置**：采用 DeepSpeed ZeRO-3 优化，学习率为 1e-5，训练 2 个 Epoch，最大序列长度为 8000 tokens。

#### 5. 实验数量与充分性
*   **规模**：研究者共构建了 **149 个** 合成网站环境，生成了 **7400 个** 涵盖 Easy、Medium、Hard 三种难度的任务。
*   **评估维度**：除了自动化指标，还引入了 **人类评估**（4 名计算机专业研究生），对环境的功能正确性（90%）、视觉质量（4.7/5）和任务验证器准确性（76%）进行了双盲标注。
*   **充分性**：实验涵盖了从模型泛化性到站点精通度的多维度分析，并进行了环境数量的扩展性分析（Scaling Analysis），证明了实验结果的客观性和统计意义。

#### 6. 主要结论与发现
*   **性能显著提升**：在 WebArena 上，VeriEnv 使 Qwen3-4B 的成功率提升了 **6.06%**，使 LLaMA-3.2-3B 提升了 **9.70%**，显著优于其他合成数据方法。
*   **验证的重要性**：相比于 PAE 等依赖 LLM 评分的方法，VeriEnv 提供的确定性奖励信号使训练更加稳定，减少了“假阳性”反馈。
*   **环境扩展效应**：实验显示，随着训练所用的合成环境数量增加，智能体的性能呈线性增长趋势，验证了该框架的可扩展性。

#### 7. 优点
*   **安全性**：完全避免了与真实生产环境的交互风险。
*   **确定性奖励**：通过 Python SDK 直接访问后端状态，解决了 Web 任务难以自动评估的顽疾。
*   **自动化程度高**：从网站克隆到任务生成、轨迹验证、模型微调，全流程基本无需人工干预。

#### 8. 不足与局限
*   **多媒体处理能力有限**：对于依赖复杂多媒体（如视频流、大型 PDF 渲染）的网站，Coding Agent 的重建质量会有所下降。
*   **基础设施依赖**：大规模部署合成网站（如文中提到的 100+ 站点）对服务器端口管理和资源隔离（如 Docker）有较高要求，否则易出现端口冲突。
*   **验证器误差**：虽然是程序化验证，但如果数据库重置逻辑不严谨（如随机种子未固定），仍可能导致 24% 左右的验证错误。

（完）
