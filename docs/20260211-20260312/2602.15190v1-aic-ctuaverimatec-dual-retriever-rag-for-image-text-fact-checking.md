---
title: "AIC CTU@AVerImaTeC: dual-retriever RAG for image-text fact checking"
title_zh: "AIC CTU@AVerImaTeC：用于图文事实核查的双检索器 RAG"
authors: "Herbert Ullrich, Jan Drchal"
date: 2026-02-16
pdf: "https://arxiv.org/pdf/2602.15190v1"
tags: ["query:ai-mmd"]
score: 9.0
evidence: 用于图文事实核查的多模态大模型
tldr: 本研究介绍了在AVerImaTeC挑战赛中获得第三名的双检索RAG系统。该系统针对图文事实核查任务，将传统的文本检索增强生成（RAG）与反向图像搜索（RIS）模块相结合。系统由文本检索、RIS API和多模态大模型生成三个解耦模块组成，具有结构简单、易于复现的特点。通过OpenAI Batch API，单次核查成本仅约0.013美元，在保持高性能的同时实现了极高的经济性，为多模态事实核查提供了高效且易于上手的基准方案。
selection_source: fresh_fetch
motivation: 旨在通过结合文本检索和反向图像搜索，提升多模态事实核查的准确性并显著降低处理成本。
method: 构建了一个由文本相似度检索、反向图像搜索API和多模态大模型生成模块组成的双检索RAG框架。
result: 该系统在AVerImaTeC任务中排名第三，单次事实核查的平均成本仅为0.013美元，表现出极高的性价比。
conclusion: 实验证明了简单且解耦的模块化设计在多模态事实核查中的有效性，并为后续研究提供了可复现的基准。
---

## 摘要
在本文中，我们介绍了在 AVerImaTeC 共享任务中获得第三名的系统，该系统将我们去年的检索增强生成（RAG）流水线与反向图像搜索（RIS）模块相结合。尽管该系统结构简单，但通过 OpenAI Batch API 使用 GPT5.1，每次事实核查仅需调用一次多模态大语言模型（LLM），平均成本仅为 0.013 美元，且表现出极具竞争力的性能。我们的系统也易于复现和调整，仅由三个解耦模块组成：基于相似度搜索的文本检索模块、基于 API 访问的 RIS 图像检索模块以及使用 GPT5.1 的生成模块。因此，我们建议将其作为进一步实验的一个易于上手的起点。我们发布了该系统的代码和提示词（prompts），以及我们的向量库，并分享了关于该方案运行成本的见解和进一步改进的方向。

## Abstract
In this paper, we present our 3rd place system in the AVerImaTeC shared task, which combines our last year's retrieval-augmented generation (RAG) pipeline with a reverse image search (RIS) module. Despite its simplicity, our system delivers competitive performance with a single multimodal LLM call per fact-check at just $0.013 on average using GPT5.1 via OpenAI Batch API. Our system is also easy to reproduce and tweak, consisting of only three decoupled modules - a textual retrieval module based on similarity search, an image retrieval module based on API-accessed RIS, and a generation module using GPT5.1 - which is why we suggest it as an accesible starting point for further experimentation. We publish its code and prompts, as well as our vector stores and insights into the scheme's running costs and directions for further improvement.

---

## 论文详细总结（自动生成）

以下是对论文《AIC CTU@AVerImaTeC: dual-retriever RAG for image-text fact checking》的结构化总结：

### 1. 论文的核心问题与整体含义
随着社交媒体成为公共议程的主要平台，事实核查任务已从纯文本演变为**图文结合（Image-Text）**的形式。核查不仅取决于文字陈述，还取决于配图的真实性、是否经过篡改以及是否在正确的语境下使用。
本文的研究动机是为 AVerImaTeC 共享任务开发一个**高性能、低成本且易于复现**的基准系统。作者旨在证明，通过简单的解耦模块和单次多模态大模型（MLLM）调用，即可在复杂的多模态事实核查任务中取得竞争性表现。

### 2. 方法论
该系统采用了一种**双检索器 RAG（检索增强生成）**架构，主要包含三个解耦模块：

*   **文本检索模块（离线）：**
    *   将 AVerImaTeC 提供的知识库切分为 2048 字符的片段。
    *   使用 `mxbai-embed-large-v1` 模型生成嵌入向量。
    *   利用 FAISS 库进行精确 k-NN 搜索（k=20），随后应用最大边际相关性（MMR）重排序，保留 7 个最具代表性的结果。
*   **图像检索模块（在线）：**
    *   **反向图像搜索（RIS）：** 通过 Serper API 调用 Google Lens，针对声明中的每张图片获取最多 30 个结果。
    *   **网页抓取：** 使用 Firecrawl API 将 RIS 结果的 URL 转换为 LLM 友好的 Markdown 格式。
    *   **过滤：** 使用 `Htmldate` 库排除发布日期晚于声明日期的证据，并剔除受抓取保护（如 Facebook/Instagram）的空结果，最终保留前 9 个结果。
*   **生成模块：**
    *   **提示词工程：** 构建包含系统指令、检索到的文本/图像来源、以及通过 BM25 从训练集中检索到的 Few-shot 示例的提示词。
    *   **多模态推理：** 将声明文本和 Base64 编码的图片输入 **GPT-5.1**。
    *   **格式转换：** 将 LLM 生成的问答对（QA pairs）转换为 AVerImaTeC 要求的陈述性证据格式，并附带图像指针。

### 3. 实验设计
*   **数据集：** 使用 **AVerImaTeC 数据集**，包含数百个由人类标注的参考图文事实核查案例。
*   **Benchmark：** AVerImaTeC 官方排行榜，对比了包括官方基线（迭代代理式系统）在内的多个参赛系统。
*   **评估指标：** 采用 Ev2R 召回率分数（由 LLM 担任裁判），包括问题分数（Question Score）、证据分数（Evidence Score）、判决准确率（Verdict Accuracy）和理由分数（Justification Score）。
*   **对比方法：** 与 ADA-AGGR、XxP、REVEAL 等系统以及官方 Baseline 进行对比。

### 4. 资源与算力
*   **算力消耗：** 论文未提及具体的本地 GPU 训练，因为该系统主要基于 API 调用。
*   **运行成本（核心亮点）：**
    *   **LLM 成本：** 通过 OpenAI Batch API 使用 GPT-5.1，平均每个案例成本仅为 **0.013 美元**。
    *   **检索成本：** Serper API 每次搜索约 0.003 美元；Firecrawl 抓取在最坏情况下约 0.05 美元（但建议使用免费工具如 Trafilatura 替代）。
    *   **总计：** 单次核查的平均成本极低，体现了极高的经济性。

### 5. 实验数量与充分性
*   **实验规模：** 在 AVerImaTeC 的开发集（Dev-split）和测试集（Test-split）上进行了完整评估。
*   **消融实验：** 针对**证据生成格式**做了三组对比实验（仅答案、问题+答案、陈述性证据），探讨了不同输出形式对最终得分的影响。
*   **充分性评价：** 实验设计较为客观，直接参与了公开挑战赛的排名。虽然消融实验主要集中在输出格式上，但对于理解系统瓶颈（证据分数）提供了有效数据支持。

### 6. 主要结论与发现
*   **排名：** 该系统在 AVerImaTeC 挑战赛中获得**第三名**。
*   **性能：** 系统在“问题分数”上表现优异（0.81），接近 SOTA，但在“证据分数”上（0.35）存在瓶颈，这直接限制了最终的判决准确率。
*   **格式影响：** 实验发现，将证据表述为“陈述性文本”比传统的“问答对”能提升约 2% 的证据分数，说明评估指标对输出格式较为敏感。

### 7. 优点
*   **简洁高效：** 相比于复杂的迭代代理（Agentic）系统，该系统结构简单，仅需单次 LLM 调用。
*   **低成本：** 极低的运行成本使其在大规模事实核查应用中具有极高的实用价值。
*   **易复现性：** 模块解耦清晰，且作者公开了代码、提示词和向量库。
*   **双检索优势：** 成功将传统的文本 RAG 与反向图像搜索结合，有效处理了多模态证据。

### 8. 不足与局限
*   **证据分数瓶颈：** 系统在提取和呈现图像相关证据方面仍有提升空间，未能完全匹配人类标注的风格。
*   **RIS 引擎限制：** 依赖 Google Lens，对于久远的历史声明或包含敏感内容的图片，检索结果可能为空。
*   **依赖闭源模型：** 核心生成能力依赖 GPT-5.1 等闭源模型，存在黑盒效应和可重复性风险（模型版本更新）。
*   **数据偏差：** AVerImaTeC 数据集中“Refuted（反驳）”类别占比过高（约 78%-95%），可能导致系统在真实环境中的泛化能力受限。

（完）
