---
title: Hybrid Self-evolving Structured Memory for GUI Agents
title_zh: 面向 GUI 智能体的混合自演化结构化记忆
authors: "Sibo Zhu, Wenyi Wu, Kun Zhou, Stephen Wang, Biwei Huang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10291v1"
tags: ["query:semma"]
score: 9.0
evidence: 视觉语言智能体的混合自进化结构化记忆
tldr: 针对GUI智能体在处理长流程、复杂界面及错误修复时的局限性，本文受人脑启发提出了HyMEM（混合自进化结构化记忆）。HyMEM采用图结构组织记忆，结合了离散的高层符号节点与连续的轨迹嵌入，支持多跳检索、节点自进化更新及推理时的实时工作记忆刷新。实验证明，HyMEM显著提升了开源模型性能，使7B/8B模型在GUI任务上超越了GPT-4o等闭源模型。
selection_source: fresh_fetch
motivation: 现有的GUI智能体外部记忆多采用扁平化检索，缺乏人类记忆的结构化组织和自进化能力，难以应对复杂的真实世界任务。
method: 提出一种基于图的混合记忆框架HyMEM，通过符号节点与轨迹嵌入的耦合，实现多跳检索、节点动态更新及推理中的工作记忆刷新。
result: "HyMEM显著提升了开源GUI智能体的表现，其中Qwen2.5-VL-7B性能提升22.5%，甚至超越了Gemini 1.5 Pro和GPT-4o。"
conclusion: 结构化且可自进化的记忆机制是提升GUI智能体处理复杂、长程任务能力的关键路径。
---

## 摘要
视觉语言模型（VLM）的显著进展使得 GUI 智能体能够以类人的方式与计算机进行交互。然而，由于长程工作流、多样化的界面以及频繁的中间错误，现实世界的计算机使用任务仍然具有挑战性。先前的研究通过大量轨迹集合为智能体构建外部记忆，但依赖于对离散摘要或连续嵌入的扁平化检索，缺乏人类记忆的结构化组织和自演化特性。受大脑启发，我们提出了混合自演化结构化记忆（HyMEM），这是一种基于图的记忆，将离散的高层符号节点与连续的轨迹嵌入相结合。HyMEM 维持一种图结构，以支持多跳检索、通过节点更新操作实现的自演化，以及推理过程中的即时工作记忆刷新。大量实验表明，HyMEM 持续提升了开源 GUI 智能体的性能，使 7B/8B 参数规模的主干网络能够媲美或超越强大的闭源模型；值得注意的是，它将 Qwen2.5-VL-7B 的性能提升了 22.5%，并优于 Gemini2.5-Pro-Vision 和 GPT-4o。

## Abstract
The remarkable progress of vision-language models (VLMs) has enabled GUI agents to interact with computers in a human-like manner. Yet real-world computer-use tasks remain difficult due to long-horizon workflows, diverse interfaces, and frequent intermediate errors. Prior work equips agents with external memory built from large collections of trajectories, but relies on flat retrieval over discrete summaries or continuous embeddings, falling short of the structured organization and self-evolving characteristics of human memory. Inspired by the brain, we propose Hybrid Self-evolving Structured Memory (HyMEM), a graph-based memory that couples discrete high-level symbolic nodes with continuous trajectory embeddings. HyMEM maintains a graph structure to support multi-hop retrieval, self-evolution via node update operations, and on-the-fly working-memory refreshing during inference. Extensive experiments show that HyMEM consistently improves open-source GUI agents, enabling 7B/8B backbones to match or surpass strong closed-source models; notably, it boosts Qwen2.5-VL-7B by +22.5% and outperforms Gemini2.5-Pro-Vision and GPT-4o.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **HyMEM（Hybrid Self-evolving Structured Memory）** 的新型 GUI 智能体记忆框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 GUI 智能体在处理长流程、多样化界面和中间错误时表现不佳。虽然引入外部记忆（存储历史轨迹）有所帮助，但现有方法多采用“扁平化”检索（仅依赖简单的文本摘要或向量相似度），缺乏人类大脑那种**结构化组织**和**持续自演化**的能力。
*   **研究背景**：受神经生物学启发，人类记忆是混合的：海马体编码细粒度的多模态经验（连续路径），而新皮层提取统计规律形成高层符号（离散路径）。论文旨在为 GUI 智能体构建一个既能保留视觉细节又能提取高层策略的图结构记忆系统。

### 2. 方法论：核心思想与关键技术
HyMEM 的核心是一个**基于图的混合自演化记忆系统**，主要包含以下三个维度：
*   **混合结构化图模式**：
    *   **节点定义**：每个节点包含高层策略（离散启发式总结）、中层属性（语义标签如 #search）和底层轨迹嵌入（连续的多模态表示）。
    *   **图连接**：共享相同中层属性的节点之间建立边，支持多跳关联检索。
*   **自演化构建机制**：
    *   当新轨迹进入时，通过“检索-冗余检查-结构化更新”三步走：
        *   **ADD**：发现全新策略时添加新节点。
        *   **MERGE**：策略相似但有补充信息（如新 UI 变体）时合并。
        *   **REPLACE**：新轨迹更高效（步数更少、成功率更高）时替换旧节点。
*   **推理时的动态利用**：
    *   **结构化检索**：采用“种子+扩展”策略，先找相似节点，再通过图连接扩展到相关但视觉不一定相似的策略节点。
    *   **即时工作记忆刷新**：在执行过程中，VLM 会检测“阶段转换”（如从搜索转向结账），一旦检测到偏移，立即触发重新检索和刷新工作记忆，确保上下文始终相关。

### 3. 实验设计
*   **数据集/场景**：
    *   **WebVoyager**：涵盖 Amazon、Google Maps 等真实网站的动态推理。
    *   **Multimodal-Mind2Web**：跨域、跨网站的通用网页任务。
    *   **MMInA**：多步、多跳的互联网任务。
*   **Benchmark 与对比方法**：
    *   **闭源模型**：GPT-4o, Gemini 2.5 Pro Vision, Claude 4。
    *   **开源基座**：Qwen2.5-VL-7B/32B, Qwen3-VL-8B, UI-TARS-1.5-7B 等。
    *   **记忆增强方法**：ReasoningBank（离散文本）、AWM（工作流）、CoMEM（连续嵌入）。

### 4. 资源与算力
*   **算力消耗**：论文明确指出，使用 **1 张 NVIDIA H100 GPU** 即可在 **20 小时内**完成训练。
*   **训练细节**：采用了参数高效微调（PEFT），仅更新了 Q-Former 和 LoRA 层，约占总参数量的 **1.2%**。
*   **数据规模**：初始记忆库包含 2,883 条成功轨迹，消融实验中扩展到了 8,000 条。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流 Benchmark 的多个子域（如购物、旅游、地图、百科）进行了广泛测试。
*   **消融实验**：
    *   验证了**全局演化**（长期记忆积累）与**局部演化**（推理时刷新）的各自贡献。
    *   分析了**记忆库规模**（从 500 到 8,000 条轨迹）对性能的缩放效应。
    *   探讨了检索策略中**相似性与多样性**的权衡。
*   **充分性评价**：实验设计非常全面，不仅对比了不同规模的模型，还对比了不同类型的记忆机制，证明了混合路径的优越性。

### 6. 主要结论与发现
*   **性能飞跃**：HyMEM 显著提升了开源模型表现。Qwen2.5-VL-7B 在挂载 HyMEM 后，性能从 12.5% 提升至 **35.0%（+22.5%）**。
*   **以小博大**：搭载 HyMEM 的 7B/8B 规模模型在多个指标上**超越了 GPT-4o 和 Gemini 2.5 Pro**。
*   **图压缩优势**：随着轨迹增加，图节点数量呈亚线性增长，证明了该结构能有效压缩冗余信息。
*   **动态刷新的必要性**：在长程任务中，固定上下文会随步骤推移失效，即时刷新机制是保持任务同步的关键。

### 7. 优点（亮点）
*   **脑启发式设计**：将离散符号的逻辑性与连续嵌入的感知细节完美结合。
*   **自演化能力**：解决了记忆库无限膨胀的问题，通过“合并/替换”实现了知识的提纯。
*   **高效性**：训练成本极低（单卡 20 小时），且能显著增强轻量级模型的竞争力。
*   **多跳检索**：通过图结构挖掘出“视觉不相似但逻辑相关”的经验，增强了泛化能力。

### 8. 不足与局限
*   **更新策略依赖启发式**：目前的节点更新（ADD/MERGE/REPLACE）依赖于 VLM 的判断逻辑，而非通过强化学习等更系统的方法进行优化。
*   **模型规模覆盖**：实验主要集中在 7B/8B 模型，尚未在 32B 或 70B 等更大规模模型上验证其增益是否会边际递减。
*   **安全性与风险**：自主 GUI 智能体在真实环境下的误操作风险仍需更严格的安全护栏。

（完）
