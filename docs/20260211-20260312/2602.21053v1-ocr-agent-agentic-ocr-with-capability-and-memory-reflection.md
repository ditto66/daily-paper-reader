---
title: "OCR-Agent: Agentic OCR with Capability and Memory Reflection"
title_zh: OCR-Agent：具有能力与记忆反思的智能体 OCR
authors: "Shimin Wen, Zeyu Zhang, Xingdou Bian, Hongjie Zhu, Lulu He, Layi Shama, Daji Ergu, Ying Cai"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.21053v1"
tags: ["query:semma"]
score: 10.0
evidence: 具备能力与记忆反思的迭代自纠正框架
tldr: 针对大视觉语言模型（VLM）在复杂视觉任务中缺乏有效纠错机制、易陷入重复错误的问题，本文提出 OCR-Agent 框架。该框架通过“能力反思”诊断错误并制定计划，结合“记忆反思”回顾历史尝试以避免重复，实现结构化的自我迭代优化。在 OCRBench v2 评测中，该方法在无需额外训练的情况下显著提升了模型在视觉理解与推理上的表现，超越了现有开源 SOTA 模型。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在多轮修正中缺乏有效的自我纠错能力，容易陷入无效的重复尝试。
method: 提出一种包含能力反思（诊断与规划）和记忆反思（回顾历史避免重复）的迭代自纠错框架。
result: 在 OCRBench v2 上，OCR-Agent 在中英文子集及视觉理解、推理任务中均取得了 SOTA 性能，优于 InternVL3-8B。
conclusion: 结构化且具备自我意识的反思机制能在不增加训练成本的前提下，显著增强视觉语言模型的推理鲁棒性。
---

## 摘要
大视觉语言模型（VLMs）通过迭代优化方法在复杂的视觉理解任务中展现出巨大潜力。然而，这些模型通常缺乏有效的自我修正机制，使其难以独立纠正认知偏差。因此，在多轮修订过程中，它们往往会陷入重复且无效的尝试，无法实现答案质量的稳定提升。为了解决这一问题，我们提出了一种新颖的迭代自我修正框架，赋予模型两项关键能力：能力反思（Capability Reflection）和记忆反思（Memory Reflection）。该框架引导模型首先通过能力反思诊断错误并生成修正计划，然后利用记忆反思回顾过去的尝试以避免重复并探索新方案，最后通过严谨的重新推理来优化答案。在具有挑战性的 OCRBench v2 基准测试上的实验表明，OCR-Agent 在英文子集上比当前的开源 SOTA 模型 InternVL3-8B 高出 +2.0，在中文子集上高出 +1.2，同时在视觉理解（79.9）和推理（66.5）方面达到了最先进的结果——甚至超越了更大规模的微调模型。我们的方法证明，结构化的、具有自我意识的反思可以在无需额外训练的情况下显著增强 VLM 的推理鲁棒性。代码：https://github.com/AIGeeksGroup/OCR-Agent。

## Abstract
Large Vision-Language Models (VLMs) have demonstrated significant potential on complex visual understanding tasks through iterative optimization methods.However, these models generally lack effective self-correction mechanisms, making it difficult for them to independently rectify cognitive biases. Consequently, during multi-turn revisions, they often fall into repetitive and ineffective attempts, failing to achieve stable improvements in answer quality.To address this issue, we propose a novel iterative self-correction framework that endows models with two key capabilities: Capability Reflection and Memory Reflection. This framework guides the model to first diagnose errors and generate a correction plan via Capability Reflection, then leverage Memory Reflection to review past attempts to avoid repetition and explore new solutions, and finally, optimize the answer through rigorous re-reasoning. Experiments on the challenging OCRBench v2 benchmark show that OCR-Agent outperforms the current open-source SOTA model InternVL3-8B by +2.0 on English and +1.2 on Chinese subsets, while achieving state-of-the-art results in Visual Understanding (79.9) and Reasoning (66.5) - surpassing even larger fine-tuned models. Our method demonstrates that structured, self-aware reflection can significantly enhance VLMs' reasoning robustness without additional training. Code: https://github.com/AIGeeksGroup/OCR-Agent.

---

## 论文详细总结（自动生成）

这是一份关于论文《OCR-Agent: Agentic OCR with Capability and Memory Reflection》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
尽管大型视觉语言模型（VLMs）在 OCR 任务中表现出色，但在面对复杂场景时仍存在两个核心痛点：
*   **能力幻觉（Capability Hallucination）**：模型在自我修正时常提出其无法执行的操作，例如建议“增强图像对比度”或“人工校对”，导致修正计划脱离实际。
*   **精炼停滞（Refinement Stagnation）**：在多轮迭代中，模型容易陷入无效的循环，重复之前的错误尝试，无法通过自我反思实现性能的稳定提升。
**研究动机**：开发一种无需额外训练、能够感知自身能力边界并具备历史记忆的智能体框架，以实现稳定、鲁棒的 OCR 自我修正。

### 2. 方法论：核心思想与技术细节
OCR-Agent 提出了一个“反思-精炼”的迭代循环，核心包含两个机制：
*   **能力反思（Capability Reflection）**：
    *   模型在生成修正计划 $P$ 后，通过一个可行性指示函数 $\phi(a)$ 进行过滤。
    *   如果动作 $a$ 是模型无法执行的（如图像处理），则将其剔除；仅保留模型可执行的文本操作（如重新审视特定区域、逻辑重推理）。
    *   这确保了精炼步骤是基于模型实际能力的。
*   **记忆反思（Memory Reflection）**：
    *   引入反思存储器 $M_i$，记录从初始答案到当前轮次的所有历史反思轨迹。
    *   在每一轮迭代中，模型不仅参考当前图像和问题，还要审视过去的失败经验，从而避免重复错误路径，探索新的解决方案。
*   **算法流程**：
    1.  生成初始答案 $A_0$。
    2.  **反思阶段**：结合图像、问题、前一轮答案和历史记忆，生成新的反思 $R_i$。
    3.  **过滤阶段**：从 $R_i$ 中提取计划并过滤掉不可行操作。
    4.  **精炼阶段**：基于过滤后的可行计划和完整的记忆链条，生成改进后的答案 $A_i$。
    5.  重复上述步骤（实验中固定为 3 轮）。

### 3. 实验设计
*   **数据集与 Benchmark**：使用了 **OCRBench v2**，这是一个包含超过 10,000 个手动验证 QA 对的高质量基准，涵盖中英文数据。
*   **任务类型**：包括文本识别、目标定位、信息提取、结构化解析、数学计算、视觉理解和复杂推理等 8 个子任务。
*   **对比方法**：
    *   **开源模型**：InternVL3-8B（之前的 SOTA）、LLaVA-Next/OV、MiniCPM-V、Deepseek-VL2 等。
    *   **闭源模型**：GPT-4o、GPT-4o-mini、Gemini-Pro、Claude 3.5-Sonnet 等。
    *   **消融变体**：Naive（直接回答）、CoT（思维链）、Self-Refine（标准自精炼）。

### 4. 资源与算力
*   **硬件环境**：实验在 **4 张 NVIDIA 3090 GPU** 上执行。
*   **训练时长**：由于该框架是 **Training-free（无需训练）** 的，因此不存在训练时长。所有的性能提升均通过推理阶段的提示工程和智能体逻辑实现。

### 5. 实验数量与充分性
*   **实验规模**：在 OCRBench v2 的所有子集上进行了全面测试，涵盖了从基础识别到高阶推理的多种维度。
*   **消融实验**：详细对比了“仅能力反思”、“仅记忆反思”以及“两者结合”的效果，验证了各组件的贡献。
*   **客观性**：通过与多种参数量更大（如 12B、16B）的模型以及顶尖闭源模型对比，证明了该方法在 7B 基础模型（RolmOCR-7B）上即可实现超越大模型的性能，实验设计较为公平且具有说服力。

### 6. 主要结论与发现
*   **性能突破**：OCR-Agent 在英文子集上达到 51.0 分，超越了 InternVL3-8B（49.0），逼近 Gemini-Pro（51.9）。
*   **任务优势**：在**视觉理解（79.9）**和**推理（66.5）**任务上表现尤为突出，显著优于所有对比模型。
*   **中文增强**：在中文任务上，将基础模型 RolmOCR-7B 的分数从 37.7 提升至 54.7，提升幅度接近 16 分。
*   **迭代稳定性**：相比于 CoT 和 Self-Refine 在 1-2 轮后性能趋于平缓或下降，OCR-Agent 在 3 轮迭代中表现出持续、稳定的性能增长。

### 7. 优点
*   **无需训练**：通过纯推理侧的逻辑优化提升性能，部署成本低。
*   **解决核心痛点**：通过能力过滤和记忆机制，有效解决了 VLM 在自纠错中的“胡言乱语”和“原地打转”问题。
*   **强泛化性**：实验证明该框架可以显著提升不同基础模型的表现，具有普适性。

### 8. 不足与局限
*   **计算开销**：由于需要多轮迭代（每轮包含反思和精炼两次调用），推理延迟和计算成本显著高于单次推理模型，不适合实时性要求极高的场景。
*   **基础模型依赖**：如果基础模型对图像的底层视觉感知存在根本性错误（如完全看不见某个字符），反思机制可能无法从错误的上下文中恢复。
*   **迭代策略固定**：目前采用固定的 3 轮迭代，对于简单问题可能存在冗余，对于极难问题可能轮次不足。

（完）
