---
title: Retrospective In-Context Learning for Temporal Credit Assignment with Large Language Models
title_zh: 基于大语言模型的回溯式上下文学习用于时间信用分配
authors: "Wen-Tse Chen, Jiayu Chen, Fahim Tajwar, Hao Zhu, Xintong Duan, Ruslan Salakhutdinov, Jeff Schneider"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17497v1"
tags: ["query:q1"]
score: 8.0
evidence: 使用大语言模型和回顾式上下文学习训练自进化智能体
tldr: 针对自进化智能体在稀疏反馈下学习效率低的问题，本文提出利用大语言模型（LLM）的预训练知识进行时序信用分配。通过回顾式上下文学习（RICL），将稀疏奖励转化为稠密的优势函数信号，并构建了在线学习框架RICOL。实验证明该方法在BabyAI任务中显著提升了采样效率，为开发高效、通用的强化学习范式提供了新思路。
selection_source: carryover_cache
motivation: 传统的时序信用分配依赖于任务特定的价值函数，存在采样效率低和泛化性差的问题。
method: 提出回顾式上下文学习（RICL）利用LLM生成稠密训练信号，并结合在线学习框架RICOL迭代优化策略。
result: RICL能准确估计优势函数并识别关键状态，RICOL在BabyAI场景下以极高的采样效率达到了传统强化学习算法的性能。
conclusion: 利用LLM进行时序信用分配是提升强化学习采样效率和泛化能力的有效途径。
---

## 摘要
从自采样数据和稀疏环境反馈中学习仍然是训练自我进化智能体的一个基本挑战。时间信用分配通过将稀疏反馈转化为密集监督信号来缓解这一问题。然而，以往的方法通常依赖于学习特定任务的价值函数来进行信用分配，这面临着样本效率低和泛化能力有限的问题。在这项工作中，我们提出利用大语言模型（LLMs）的预训练知识，通过回溯式上下文学习（RICL）将稀疏奖励转化为密集训练信号（即优势函数）。我们进一步提出了一个在线学习框架 RICOL，它根据 RICL 的信用分配结果迭代地优化策略。我们通过实验证明，RICL 可以在有限样本下准确估计优势函数，并有效识别环境中用于时间信用分配的关键状态。在四个 BabyAI 场景上的扩展评估表明，RICOL 达到了与传统在线强化学习算法相当的收敛性能，且样本效率显著提高。我们的研究结果强调了利用 LLMs 进行时间信用分配的潜力，为更具样本效率和泛化能力的强化学习范式铺平了道路。

## 速览
**TLDR**：针对自进化智能体在稀疏反馈下学习效率低的问题，本文提出利用大语言模型（LLM）的预训练知识进行时序信用分配。通过回顾式上下文学习（RICL），将稀疏奖励转化为稠密的优势函数信号，并构建了在线学习框架RICOL。实验证明该方法在BabyAI任务中显著提升了采样效率，为利用LLM优化强化学习提供了新思路。 \
**Motivation**：传统的时序信用分配依赖于任务特定的价值函数，存在采样效率低和泛化性差的问题。 \
**Method**：提出回顾式上下文学习（RICL）利用LLM生成稠密监督信号，并结合在线学习框架RICOL迭代优化策略。 \
**Result**：RICL能准确估计优势函数并识别关键状态，RICOL在保持性能的同时显著提高了采样效率。 \
**Conclusion**：本研究展示了利用LLM进行时序信用分配的潜力，为开发高效且通用的强化学习范式奠定了基础。

---

## Abstract
Learning from self-sampled data and sparse environmental feedback remains a fundamental challenge in training self-evolving agents. Temporal credit assignment mitigates this issue by transforming sparse feedback into dense supervision signals. However, previous approaches typically depend on learning task-specific value functions for credit assignment, which suffer from poor sample efficiency and limited generalization. In this work, we propose to leverage pretrained knowledge from large language models (LLMs) to transform sparse rewards into dense training signals (i.e., the advantage function) through retrospective in-context learning (RICL). We further propose an online learning framework, RICOL, which iteratively refines the policy based on the credit assignment results from RICL. We empirically demonstrate that RICL can accurately estimate the advantage function with limited samples and effectively identify critical states in the environment for temporal credit assignment. Extended evaluation on four BabyAI scenarios show that RICOL achieves comparable convergent performance with traditional online RL algorithms with significantly higher sample efficiency. Our findings highlight the potential of leveraging LLMs for temporal credit assignment, paving the way for more sample-efficient and generalizable RL paradigms.

---

## 论文详细总结（自动生成）

这篇论文由卡内基梅隆大学、香港大学和斯坦福大学的研究人员合作完成，发表于 NeurIPS 2025。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在训练自我进化（Self-evolving）的智能体时，环境反馈往往是**稀疏的**（Sparse），且存在**时间信用分配**（Temporal Credit Assignment, TCA）难题。即智能体很难在长序列任务中识别出哪些具体动作导致了最终的成功或失败。
*   **研究背景**：传统的强化学习（RL）依赖于学习特定任务的价值函数（Value Function）来进行信用分配，但这通常需要海量的交互样本，且泛化性差。
*   **整体含义**：本文旨在利用大语言模型（LLM）内置的预训练知识，将稀疏的奖励信号转化为稠密的监督信号（优势函数），从而提升 LLM 智能体在复杂多轮任务中的在线学习效率。

### 2. 核心方法论
论文提出了两个相互关联的组件：**RICL** 和 **RICOL**。

*   **RICL (Retrospective In-Context Learning, 回溯式上下文学习)**：
    *   **核心思想**：利用一个“反射器”LLM（Reflector）对已发生的轨迹进行事后分析。
    *   **技术细节**：对于轨迹中的每个状态 $s_t$，反射器根据后续的轨迹和奖励生成言语反馈 $f_t$。然后，将 $f_t$ 加入 Prompt，得到一个上下文更新后的策略 $\pi'$。
    *   **优势估计**：通过比较原始策略 $\pi_0$ 和更新策略 $\pi'$ 的对数概率（Log-probabilities）来估计优势函数 $A(s, a)$。论文在理论上证明了（定理 4.1）这种对数概率之差与优势函数成正比。
*   **RICOL (Retrospective In-Context Online Learning, 回溯式上下文在线学习)**：
    *   **算法流程**：
        1.  **轨迹收集**：当前策略 $\pi_k$ 与环境交互收集数据。
        2.  **策略评估 (RICL)**：对收集到的状态进行回溯分析，生成言语反馈并计算 $\pi'$ 的分布。
        3.  **策略改进**：基于优势加权回归（Advantage-Weighted Regression, AWR）的思想，通过最小化 KL 散度损失函数来更新 LLM 的参数，使新策略 $\pi_{k+1}$ 靠近 $\pi'$，同时保持在 $\pi_k$ 的信任区域内。

### 3. 实验设计
*   **实验场景**：
    1.  **1D Key-Door 环境**：用于验证优势函数估计的准确性。
    2.  **BabyAI 场景**：包括 `goto`（前往）、`pickup`（捡起）、`pick_up_seq_go_to`（序列任务）和 `open`（开门）四个具有挑战性的 2D 网格世界任务。
*   **对比方法 (Baselines)**：
    *   **GPT-4o mini**：零样本（Zero-shot）表现。
    *   **Reflexion**：仅使用上下文学习和自我反思，不更新参数。
    *   **PPO (3B)**：使用 LLM 作为主干的传统 PPO 算法。
    *   **PPO (10M)**：从头训练的小型 MLP 模型。
    *   **RWR (Reward-Weighted Regression)**：不进行信用分配，直接使用轨迹奖励。

### 4. 资源与算力
论文在附录 D 中明确列出了算力资源：
*   **GPU 型号与数量**：
    *   RICOL 和 RWR 训练：使用 2 张 **NVIDIA A40** GPU。
    *   PPO (3B) 训练：使用 4 张 **NVIDIA A40** GPU。
    *   PPO (10M) 训练：使用 1 张 **GeForce RTX 2080 Ti**。
*   **训练时长**：
    *   简单任务（如 `goto`）约 72-96 分钟。
    *   复杂任务（如 `open`）在 RICOL 下需要约 585-594 分钟（约 10 小时）。
    *   PPO (3B) 在所有任务上均设定为 1440 分钟（24 小时）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础理论验证（1D 环境）到复杂多轮决策（BabyAI）的多个维度。
*   **消融与鲁棒性实验**：
    *   对比了 RICL 与传统蒙特卡洛（MC）采样的效率。
    *   测试了在不同准确率（噪声）的言语反馈下 RICOL 的稳定性。
    *   验证了回溯式设计（Retrospective）相对于标准 ICL 的优越性。
*   **充分性评价**：实验设计较为充分，每个实验均使用了 **3 个随机种子**并报告了均值和标准差。对比基准涵盖了从零样本到最先进的在线 RL 算法，评估标准客观、公平。

### 6. 主要结论与发现
*   **极高的采样效率**：在估计优势函数时，RICL 比蒙特卡洛方法效率高出约 **100 倍**。
*   **在线学习优势**：在 BabyAI 任务中，RICOL 达到相同性能所需的样本量比 PPO (10M) 少 **50 倍**，比 PPO (3B) 少 **10 倍**。
*   **超越强模型**：通过在线交互学习，使用 3B 参数模型的 RICOL 最终在成功率上超过了作为“反射器”的 GPT-4o mini。
*   **信用分配能力**：RICL 能够准确识别出任务中的关键状态（如“捡起钥匙”的那一刻）。

### 7. 优点与亮点
*   **理论支撑**：为 LLM 的对数概率变化与强化学习中的优势函数建立了理论联系。
*   **回溯式设计**：通过针对已发生的特定状态生成反馈，降低了 LLM 泛化的压力，使反馈更精准。
*   **稠密信号生成**：成功将 LLM 的推理能力转化为 RL 训练所需的稠密梯度信号，解决了稀疏奖励难题。

### 8. 不足与局限
*   **动作空间限制**：目前仅支持**离散且有限**的动作空间，因为计算 KL 散度时需要遍历所有可能的动作。
*   **依赖反射器质量**：算法的效果在很大程度上取决于“反射器”LLM 生成反馈的质量。
*   **计算开销**：虽然采样效率高，但每次更新需要多次调用 LLM 计算对数概率，推理成本较高。
*   **任务复杂度**：虽然在 BabyAI 上表现良好，但在更复杂、状态空间更大的真实世界任务或长文本推理任务中的表现尚待验证。

（完）
