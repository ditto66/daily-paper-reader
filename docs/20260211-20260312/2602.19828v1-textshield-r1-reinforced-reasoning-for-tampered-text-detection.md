---
title: "TextShield-R1: Reinforced Reasoning for Tampered Text Detection"
title_zh: TextShield-R1：用于篡改文本检测的强化推理
authors: "Chenfan Qu, Yiwu Zhong, Jian Liu, Xuekang Zhu, Bohan Yu, Lianwen Jin"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.19828v1"
tags: ["query:ai-mmd"]
score: 9.0
evidence: 用于篡改文本检测的多模态大语言模型
tldr: 针对篡改文本检测中微观伪造难识别、定位精度低及解释标注成本高的问题，本文提出首个基于强化学习的多模态大模型方案 TextShield-R1。该方案通过法证持续预训练、基于 GRPO 的强化学习微调及 OCR 纠偏推理，显著提升了模型的推理与定位能力。同时，发布了包含 16 种语言、4.5 万张图像的 TFR 基准测试集，为可解释篡改检测提供了全面评估。
selection_source: fresh_fetch
motivation: 现有多模态大模型在处理篡改文本时难以捕捉微观伪造痕迹，且面临定位精度低和高质量解释标注稀缺的挑战。
method: 提出包含法证持续预训练、基于组相对策略优化（GRPO）的强化学习微调以及推理阶段 OCR 纠偏的完整框架。
result: 在涵盖 16 种语言和 10 种篡改技术的 TFR 基准测试中，TextShield-R1 在检测精度和推理能力上均显著超越了现有最先进模型。
conclusion: TextShield-R1 证明了强化学习在提升多模态模型法证推理能力方面的有效性，为可解释的篡改文本检测树立了新标杆。
---

## 摘要
篡改图像的日益盛行带来了严重的安全威胁，凸显了对可靠检测方法的迫切需求。多模态大语言模型（MLLMs）在分析篡改图像和生成解释方面展现出强大潜力。然而，它们在识别微观伪影方面仍面临困难，在定位篡改文本区域时准确率较低，且在伪造解释方面严重依赖昂贵的标注。为此，我们推出了 TextShield-R1，这是首个基于强化学习的 MLLM 方案，用于篡改文本的检测与推理。具体而言，我们的方法引入了取证持续预训练（Forensic Continual Pre-training），这是一种由易到难的课程学习，通过利用来自自然图像取证和 OCR 任务的大规模廉价数据，使 MLLM 为篡改文本检测做好充分准备。在微调阶段，我们采用带有新型奖励函数的群体相对策略优化（Group Relative Policy Optimization），以减少对标注的依赖并提升推理能力。在推理阶段，我们通过 OCR 修正（OCR Rectification）增强定位准确性，该方法利用 MLLM 强大的文本识别能力来优化其预测结果。此外，为了支持严谨的评估，我们引入了文本取证推理（TFR）基准测试，包含超过 4.5 万张涵盖 16 种语言、10 种篡改技术和不同领域的真实及篡改图像。其中包含丰富的推理式标注，可进行全面评估。我们的 TFR 基准测试同时解决了现有基准的七大局限性，并支持在跨风格、跨方法和跨语言条件下的鲁棒评估。大量实验表明，TextShield-R1 显著提升了可解释篡改文本检测的最先进水平。

## Abstract
The growing prevalence of tampered images poses serious security threats, highlighting the urgent need for reliable detection methods. Multimodal large language models (MLLMs) demonstrate strong potential in analyzing tampered images and generating interpretations. However, they still struggle with identifying micro-level artifacts, exhibit low accuracy in localizing tampered text regions, and heavily rely on expensive annotations for forgery interpretation. To this end, we introduce TextShield-R1, the first reinforcement learning based MLLM solution for tampered text detection and reasoning. Specifically, our approach introduces Forensic Continual Pre-training, an easy-to-hard curriculum that well prepares the MLLM for tampered text detection by harnessing the large-scale cheap data from natural image forensic and OCR tasks. During fine-tuning, we perform Group Relative Policy Optimization with novel reward functions to reduce annotation dependency and improve reasoning capabilities. At inference time, we enhance localization accuracy via OCR Rectification, a method that leverages the MLLM's strong text recognition abilities to refine its predictions. Furthermore, to support rigorous evaluation, we introduce the Text Forensics Reasoning (TFR) benchmark, comprising over 45k real and tampered images across 16 languages, 10 tampering techniques, and diverse domains. Rich reasoning-style annotations are included, allowing for comprehensive assessment. Our TFR benchmark simultaneously addresses seven major limitations of existing benchmarks and enables robust evaluation under cross-style, cross-method, and cross-language conditions. Extensive experiments demonstrate that TextShield-R1 significantly advances the state of the art in interpretable tampered text detection.

---

## 论文详细总结（自动生成）

以下是对论文《TextShield-R1: Reinforced Reasoning for Tampered Text Detection》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态大语言模型（MLLM）在处理**篡改文本检测（TTD）**任务时存在三大瓶颈：
    1.  **任务对齐不足**：MLLM 通常在宏观语义任务上预训练，难以捕捉篡改检测所需的微观伪影（如边缘异常、纹理不一致）。
    2.  **标注依赖严重**：高质量的伪造解释（Reasoning）标注成本极高，且涉及隐私的证件图像难以通过闭源模型（如 GPT-4o）自动标注。
    3.  **定位精度低**：MLLM 在预测精确坐标（Bounding Box）方面表现较弱，尤其是在文本密集的场景下。
*   **研究背景**：随着生成式 AI（AIGC）的发展，篡改文本图像（如伪造合同、证件、新闻）的门槛降低，亟需一种具备高精度定位和可解释推理能力的检测方案。

### 2. 方法论：TextShield-R1 框架
论文提出了一个包含预训练、微调和推理三个阶段的完整体系：
*   **法证持续预训练（Forensic Continual Pre-training）**：
    *   采用“由易到难”的课程学习，先利用大规模廉价的自然图像取证数据（如物体篡改）训练模型识别通用伪影。
    *   **3D 法证学习**：要求模型同时输出篡改分类、区域描述、边界框和掩码字符串（Mask String），强化空间感知。
    *   **OCR 参考接地（Grounding）**：在预训练中交替进行文本识别任务，防止模型在学习取证知识时丢失原有的 OCR 能力。
*   **基于 GRPO 的强化学习微调**：
    *   引入**群体相对策略优化（GRPO）**，通过设计五种奖励函数（Reward Functions）引导模型：
        1.  **分类奖励**：判断真/假/全生成的准确性。
        2.  **方法检测奖励**：识别篡改手段（如复制粘贴或 AI 生成）。
        3.  **定位奖励**：基于预测框与真值的 IoU。
        4.  **OCR 奖励**：基于篡改文本识别的编辑距离。
        5.  **格式奖励**：强制模型遵循 `<think>` 和 `<answer>` 的思考链格式。
    *   该方法显著降低了对详尽解释标注的依赖，仅需少量冷启动数据即可通过强化学习提升推理能力。
*   **推理阶段的 OCR 纠偏（OCR Rectification）**：
    *   利用 MLLM 识别出的文本内容，在外部 OCR 引擎的结果中匹配最接近的文本块，并用 OCR 引擎的高精度坐标替换 MLLM 的预测坐标，解决定位不准的问题。

### 3. 实验设计
*   **数据集与基准（TFR Benchmark）**：
    *   发布了 **Text Forensics Reasoning (TFR)** 基准，包含 **4.5 万张伪造图像**和同等数量的真实图像。
    *   覆盖 16 种语言、10 种篡改技术（包括最新的 GPT-4o 生成和扩散模型）、3 大领域（文档、场景文本、证件卡片）。
    *   设置了三个 **OOD（分布外）测试集**：跨图像风格（CIS）、跨篡改方法（CTM）、跨语言（CL）。
*   **对比方法**：
    *   对比了 GPT-4o、MiniCPM-V2.6、InternVL3、Qwen2.5-VL 等主流 MLLM 的原始版本及微调版本。
    *   对比了专门的取证模型如 FakeShield 和 SIDA。

### 4. 资源与算力
*   **模型基础**：以 **Qwen2.5-VL-7B** 作为基础模型。
*   **训练细节**：使用 LoRA（Rank=64）进行微调，采用 AdamW 优化器，学习率为 1e-4。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量及总训练时长。但提到预训练阶段使用了约 36 万张图像，微调阶段使用了 TFR 训练集，属于中大规模训练规模。

### 5. 实验数量与充分性
*   **实验规模**：进行了主对比实验、跨领域/跨方法/跨语言的鲁棒性实验，以及针对 FCP、GRPO 和 OCR 纠偏的详细消融实验。
*   **充分性评价**：实验设计非常充分。通过 4 个不同的测试维度（分类、OCR、定位、推理）全面评估模型。
*   **客观性**：引入了 OOD 测试集，证明了模型不是简单的“背诵”训练数据，而是学习到了通用的取证特征，评估过程较为客观公平。

### 6. 主要结论与发现
*   **TextShield-R1 达到 SOTA**：在所有测试指标上均显著优于现有模型，尤其是在定位精度（IoU）和推理质量上。
*   **强化学习的有效性**：GRPO 能够使模型在仅有 25% 解释标注的情况下，达到甚至超过全标注监督微调的效果。
*   **预训练的重要性**：法证持续预训练是模型收敛的关键，直接微调会导致模型在复杂的微观伪影面前出现过拟合或混淆。

### 7. 优点与亮点
*   **首创性**：首次将强化学习（GRPO）引入篡改文本检测领域，解决了标注成本高的痛点。
*   **基准贡献**：TFR 基准解决了现有数据集领域单一、缺乏真实样本、篡改手段过时等 7 大缺陷，极具实用价值。
*   **即插即用**：OCR 纠偏机制不改变模型架构，显著提升了 MLLM 在工业级应用中的定位可靠性。

### 8. 不足与局限
*   **外部依赖**：OCR 纠偏依赖于外部 OCR 引擎的质量，若 OCR 引擎无法检测到极度模糊的篡改文本，该机制将失效。
*   **推理延迟**：引入 `<think>` 思考链和外部 OCR 匹配会增加单次推理的时间开销，在实时检测场景下可能受限。
*   **算力细节缺失**：未披露具体训练资源消耗，不便于其他研究者评估复现成本。

（完）
