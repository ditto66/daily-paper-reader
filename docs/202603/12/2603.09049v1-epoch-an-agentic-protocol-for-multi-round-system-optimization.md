---
title: "EPOCH: An Agentic Protocol for Multi-Round System Optimization"
title_zh: EPOCH：一种用于多轮系统优化的智能体协议
authors: "Zhanlin Liu, Yitao Li, Munirathnam Srikanth"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09049v1"
tags: ["query:semma"]
score: 9.0
evidence: 智能体多轮自我改进协议
tldr: 针对当前自主智能体在系统优化中缺乏统一协议且多为特定任务设计的问题，本文提出了EPOCH。这是一个面向多轮系统优化的工程协议，通过基准构建和迭代自我改进两个阶段，结合角色受限的阶段划分（规划、执行、评估）以及标准化的执行接口，实现了对提示词、代码及模型配置的协同优化，显著提升了生产环境下的稳定性、可追溯性和评估完整性。
selection_source: fresh_fetch
motivation: 现有的自主优化方法通常针对特定任务设计，缺乏一个统一的协议来管理多轮自我改进过程并确保评估的严谨性。
method: EPOCH将优化过程分为基准构建和迭代改进两个阶段，并通过角色受限的阶段划分和标准化命令接口来规范执行。
result: 该协议实现了跨提示词、模型配置和代码的协调优化，同时保证了优化过程的稳定性、可重复性和可追溯性。
conclusion: EPOCH为生产导向的自主改进工作流提供了一个实用且标准化的框架，有效提升了异构环境下系统优化的效率。
---

## 摘要
自主智能体越来越多地被用于通过迭代执行和反馈来改进提示词（prompts）、代码和机器学习系统。然而，现有的方法通常被设计为特定任务的优化循环，而不是用于建立基准和管理受追踪的多轮自我改进的统一协议。我们介绍了 EPOCH，这是一种用于异构环境下多轮系统优化的工程协议。EPOCH 将优化组织为两个阶段：基准构建和迭代自我改进。它通过角色约束阶段进一步结构化每一轮，将规划、实施和评估分离，并通过规范的命令接口和轮次级追踪来标准化执行。这种设计实现了提示词、模型配置、代码和基于规则的组件之间的协同优化，同时保持了评估的稳定性、可复现性、可追溯性和完整性。在各种任务中的实证研究说明了 EPOCH 在面向生产的自主改进工作流中的实用性。

## Abstract
Autonomous agents are increasingly used to improve prompts, code, and machine learning systems through iterative execution and feedback. Yet existing approaches are usually designed as task-specific optimization loops rather than as a unified protocol for establishing baselines and managing tracked multi-round self-improvement. We introduce EPOCH, an engineering protocol for multi-round system optimization in heterogeneous environments. EPOCH organizes optimization into two phases: baseline construction and iterative self-improvement. It further structures each round through role-constrained stages that separate planning, implementation, and evaluation, and standardizes execution through canonical command interfaces and round-level tracking. This design enables coordinated optimization across prompts, model configurations, code, and rule-based components while preserving stability, reproducibility, traceability, and integrity of evaluation. Empirical studies in various tasks illustrate the practicality of EPOCH for production-oriented autonomous improvement workflows.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **EPOCH** 的智能体协议，旨在为异构环境下的多轮系统优化提供一套标准化的工程框架。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的自主智能体优化工具（如 DSPy、Promptbreeder 等）通常针对特定任务（如仅优化提示词或仅修复代码），缺乏一个统一的、可用于生产环境的协议来管理从“建立基准”到“多轮自我改进”的全生命周期。
*   **研究背景**：在实际生产中，系统性能往往由代码、配置、提示词和规则共同决定。缺乏统一协议会导致优化过程不可追溯、评估标准不一致以及难以在不同组件间协同优化。
*   **整体含义**：EPOCH 类似于决策领域的 OODA（观察-调整-决策-行动）循环，它将优化过程协议化，确保每一步改进都是可复现、可审计且具有评估完整性的。

### 2. 方法论：核心思想与流程
EPOCH 将优化任务抽象为两个阶段和六个受限角色：

*   **核心思想**：通过**角色分离**（将提议改进的人与评估改进的人分开）和**标准化接口**，确保优化过程的严谨性。
*   **第一阶段：基准构建 (Phase I)**
    *   **种子规划者 (Seed Planner)**：分析任务需求，设计初始系统架构和评估接口。
    *   **基准执行者 (Baseline Executor)**：将设计转化为可运行的代码，生成初始性能指标。
*   **第二阶段：多轮自我改进 (Phase II)**
    *   **编排者 (Orchestrator)**：管理轮次流转、预算控制和状态切换。
    *   **调查者 (Investigator)**：分析失败案例，提出改进假设（仅限访问训练数据）。
    *   **执行者 (Executor)**：在允许的动作空间内实施修改（如改代码、调参）。
    *   **评审者 (Reviewer)**：在标准评估接口上验证改进，决定接受或拒绝该轮修改。
*   **关键技术细节**：
    *   **轮次级追踪**：记录每一轮的变更（Delta）、证据、决策理由和指标。
    *   **评估完整性**：强制执行训练/测试集分离、泄露检查和过拟合约束。

### 3. 实验设计
论文通过四个不同领域的任务验证了协议的通用性：
*   **代码优化 (Fibonacci Calculator)**：目标是优化大数计算性能。从正确性测试过渡到性能测试（5% 改进阈值）。
*   **超参数微调 (MNIST 分类)**：使用 MobileNetV2，优化器在 Adam/AdamW/SGD 间切换，调整学习率。
*   **提示词微调 (SST-2 情感分析)**：使用 `gpt-4.1-nano` 模型，通过分析训练失败案例迭代优化 System Prompt 和 Few-shot 示例。
*   **基于规则的优化 (Iris 鸢尾花分类)**：优化符号化的决策阈值和优先级规则。
*   **对比基准**：实验主要对比了各任务从 Baseline 到多轮优化后的性能提升轨迹，重点展示了协议处理“拒绝修改”和“自动终止”的能力。

### 4. 资源与算力
*   **模型使用**：提示词优化实验明确使用了 `gpt-4.1-nano`。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长。由于该协议侧重于逻辑编排而非大规模模型训练，其算力消耗主要取决于底层调用的 LLM API 或具体的任务执行器（如 MNIST 训练）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了代码、深度学习、自然语言处理和符号逻辑四个维度，展示了极强的跨领域能力。
*   **充分性评价**：
    *   **定性分析充分**：详细展示了每一轮的决策逻辑（如为何拒绝某次调参、何时触发自动终止）。
    *   **定量规模较小**：为了保证可解释性，实验使用了较小的数据子集（如 SST-2 仅用 20 个训练样本，MNIST 仅用 200 个样本）。虽然证明了协议的有效性，但在大规模工业数据上的鲁棒性仍有待进一步验证。

### 6. 主要结论与发现
*   **协议有效性**：EPOCH 能够稳定地将问题描述转化为可执行基准，并通过多轮迭代显著提升性能。
*   **鲁棒的控制流**：协议能有效识别无效改进并予以拒绝（如 MNIST 实验中的不稳定学习率），并在达到收益递减点时自动终止。
*   **透明度**：通过轮次追踪，系统进化的每一步都变得清晰可查，解决了自主智能体“黑盒优化”的问题。

### 7. 优点
*   **角色约束机制**：强制分离调查、执行和评审角色，有效防止了数据泄露和评估偏差。
*   **高度通用性**：不绑定特定优化算法，可同时管理提示词、代码和配置的混合优化。
*   **生产导向**：强调可追溯性和可审计性，非常适合需要高可靠性的工业级 AI 工作流。

### 8. 不足与局限
*   **实验样本量**：实验多在小型数据集上进行，缺乏在大规模、高噪声生产数据下的压力测试。
*   **单目标限制**：目前主要针对单一目标的迭代优化，尚未深入探讨多智能体在复杂分布式系统中的并行协作优化。
*   **延迟开销**：多角色分离和标准化的评估流程虽然保证了质量，但相比于简单的优化循环，可能会增加单轮优化的时间开销。

（完）
