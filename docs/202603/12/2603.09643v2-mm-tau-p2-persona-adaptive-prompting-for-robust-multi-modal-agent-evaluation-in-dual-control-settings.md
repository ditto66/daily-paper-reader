---
title: "MM-tau-p$^2$: Persona-Adaptive Prompting for Robust Multi-Modal Agent Evaluation in Dual-Control Settings"
title_zh: MM-tau-p$^2$：双重控制设置下鲁棒多模态智能体评估的人格自适应提示
authors: "Anupam Purwar, Aditya Choudhary"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09643v2"
tags: ["query:semma"]
score: 9.0
evidence: 基于用户画像评估行为进化的多模态智能体基准
tldr: 本研究针对现有LLM智能体评估框架忽略用户人格且仅限于文本交互的问题，提出了MM-tau-p$^2$基准。该基准专门用于在双重控制设置下评估多模态智能体的人格自适应能力，并引入了包括多模态鲁棒性和轮次开销在内的12项新指标。通过在电信和零售领域的实验，利用LLM-as-judge方法验证了前沿模型在处理多模态输入时的性能表现，为构建更具鲁棒性的个性化智能体提供了自动化评估方案。
selection_source: fresh_fetch
motivation: 现有LLM智能体评估框架多为文本驱动且忽略用户人格特征，无法满足多模态及个性化客户体验管理的需求。
method: 提出MM-tau-p$^2$基准，通过双重控制设置和人格自适应提示词，引入12项新指标来评估多模态智能体的鲁棒性。
result: 实验表明即使是GPT-4等前沿模型，在引入多模态后仍面临多模态鲁棒性和轮次开销等方面的挑战。
conclusion: MM-tau-p$^2$为自动化评估多模态智能体提供了一套全面的衡量标准，有效填补了电信和零售等领域个性化评估的空白。
---

## 摘要
当前针对大语言模型（LLM）驱动的智能体的评估框架和基准主要集中在文本对话驱动的智能体上，这些框架并未向智能体展示用户的人格（persona），因此是在一种与用户无关的环境中运行。重要的是，在客户体验管理领域，智能体的行为会随着其对用户性格的了解而演变。随着实时语音合成（TTS）和多模态语言模型的普及，基于 LLM 的智能体正逐渐向多模态化发展。为此，我们提出了 MM-tau-p$^2$ 基准，其中包含用于评估多模态智能体在双重控制设置下（无论是否进行用户人格自适应）的鲁棒性的指标，同时在规划过程中考虑用户输入以解决用户查询。特别是，我们的研究表明，即使是像 GPT-5、GPT 4.1 这样的最先进前沿 LLM，在将多模态引入基于 LLM 的智能体时，仍需通过多模态鲁棒性、轮次开销（turn overhead）等指标来衡量额外的考量因素。总的来说，MM-tau-p$^2$ 基于我们之前的工作 FOCAL，通过引入 12 个新颖指标，提供了一种自动评估多模态智能体的全面方法。我们还通过 LLM-as-judge 方法，利用精心设计的提示和定义明确的评分标准（rubrics）来评估每场对话，从而提供了这些指标在电信和零售领域的估算值。

## Abstract
Current evaluation frameworks and benchmarks for LLM powered agents focus on text chat driven agents, these frameworks do not expose the persona of user to the agent, thus operating in a user agnostic environment. Importantly, in customer experience management domain, the agent's behaviour evolves as the agent learns about user personality. With proliferation of real time TTS and multi-modal language models, LLM based agents are gradually going to become multi-modal. Towards this, we propose the MM-tau-p$^2$ benchmark with metrics for evaluating the robustness of multi-modal agents in dual control setting with and without persona adaption of user, while also taking user inputs in the planning process to resolve a user query. In particular, our work shows that even with state of-the-art frontier LLMs like GPT-5, GPT 4.1, there are additional considerations measured using metrics viz. multi-modal robustness, turn overhead while introducing multi-modality into LLM based agents. Overall, MM-tau-p$^2$ builds on our prior work FOCAL and provides a holistic way of evaluating multi-modal agents in an automated way by introducing 12 novel metrics. We also provide estimates of these metrics on the telecom and retail domains by using the LLM-as-judge approach using carefully crafted prompts with well defined rubrics for evaluating each conversation.

---

## 论文详细总结（自动生成）

这是一份关于论文《MM-tau-p$^2$: Persona-Adaptive Prompting for Robust Multi-Modal Agent Evaluation in Dual-Control Settings》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的 LLM 智能体（Agent）评估框架（如 tau-bench）主要集中在纯文本对话，且通常处于“用户不可知”的环境中，忽略了用户的人格特征（Persona）。
*   **核心问题**：在真实的客户体验管理（CX）场景中，智能体的行为应随用户的人格、专业程度和情绪而演变。此外，随着实时语音技术的发展，智能体正向多模态（语音+文本）转型。
*   **整体含义**：论文提出了 **MM-tau-p$^2$** 基准，旨在评估多模态智能体在“双重控制”（智能体与用户共同影响对话走向）设置下，对用户人格的自适应能力及其在语音交互中的鲁棒性。

### 2. 论文提出的方法论
*   **核心思想（p$^2$ 概念）**：
    *   第一个 **p**：优化智能体和人类模拟器的提示词（Prompts），包含防止过早升级（Escalation）的护栏。
    *   第二个 **p**：注入详细的用户人格（Personas），控制用户的专业水平、模糊性、冗余度和对澄清的容忍度。
*   **关键技术细节**：
    *   **双重控制协议**：用户不再是静态的信息提供者，而是会提供不完整信息、纠正智能体或在中途引入新约束。
    *   **多模态流水线**：集成 ASR（语音转文本）、LLM 决策、TTS（文本转语音）的模块化架构。
    *   **人格建模与上下文注入**：除了静态人格，还通过滑动窗口（最近 16 条消息）动态推断用户性格（如技术水平、挫败感），并每 3 轮更新一次 Agent 的系统提示词。
*   **12 项评估指标**：涵盖目标达成（如关键字段准确率 CFA）、效率（如轮次开销 TO）、恢复能力（如错误恢复率 ERR）和安全性（如不可逆操作安全性 IAS）。

### 3. 实验设计
*   **数据集与场景**：选择了**电信（Telecom）**和**零售（Retail）**两个领域。电信任务复杂度更高（涉及 SIM 卡锁定、漫游等），零售任务相对简单。
*   **Benchmark 设置**：
    *   **模态对比**：纯文本 vs. 纯语音。
    *   **人格设置**：无（None）、简单（Easy，专业度高）、困难（Hard，表达模糊）。
    *   **注入条件**：无注入、人格注入、动态上下文注入。
*   **对比方法**：使用前沿模型 **GPT-4.1** 和 **GPT-5** 作为智能体骨干，并采用 **LLM-as-judge**（同样使用 GPT-4.1/5）进行自动化评分。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **资源依赖**：由于研究基于前沿闭源模型，主要依赖于 OpenAI 和 Microsoft 的 API 服务（GPT-4.1, GPT-5）以及 ElevenLabs 的 TTS 服务。实验侧重于推理和评估框架的构建，而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：实验覆盖了两个领域、两种模态、三种人格等级以及三种注入策略，并对比了两个不同版本的 LLM 裁判。
*   **充分性与客观性**：
    *   **充分性**：通过多维度指标（12项）而非单一的成功率进行评估，较为全面。
    *   **客观性**：作者意识到了 LLM-as-judge 的偏见，通过迭代优化裁判提示词（见附录 A.3）来减少对“人工转接”任务的误判。
    *   **公平性**：在相同任务和约束下对比了不同模型和模态的表现。

### 6. 论文的主要结论与发现
*   **多模态代价**：从文本转向语音时，即使是顶级模型也会出现性能下降，表现为轮次开销增加和鲁棒性降低。
*   **人格自适应的权衡**：注入详细的人格信息能提高关键字段的准确率，但会显著降低安全性指标（Agent 在高压或复杂人格下更容易跳过确认步骤执行高危操作）。
*   **裁判模型差异**：GPT-5 作为裁判比 GPT-4.1 更“乐观”，尤其在处理电信领域的语音交互时，评分差异高达 17%。
*   **动态注入的优势**：对于缺乏领域知识的“困难”用户，静态人格注入效果不佳，而动态上下文注入能更好地适应用户行为的变化。

### 7. 优点
*   **指标创新**：引入了“多模态鲁棒性得分（MRS）”和“ASR 鲁棒目标达成（ARGA）”等针对性指标，填补了语音 Agent 评估的空白。
*   **现实性强**：双重控制和“专家-新手差距”的建模非常贴近真实的客服场景。
*   **自动化程度高**：提供了一套完整的 LLM-as-judge 流程，能够大规模评估复杂的对话质量。

### 8. 不足与局限
*   **裁判一致性问题**：尽管进行了优化，LLM 裁判在处理“必要的人工转接”与“Agent 失败”的边界时仍存在不一致性。
*   **语音特征缺失**：目前的基准尚未模拟语音交互中特有的**中断（Interruption）**、**重叠说话（Overtalk）**和**延迟（Latency）**对用户放弃率的影响。
*   **领域覆盖有限**：仅测试了电信和零售，尚未扩展到医疗、金融等对安全性要求更高的领域。
*   **黑盒依赖**：高度依赖闭源 API，对于开源模型在相同框架下的表现缺乏探讨。

（完）
