---
title: "Image-based Prompt Injection: Hijacking Multimodal LLMs through Visually Embedded Adversarial Instructions"
title_zh: 基于图像的提示注入：通过视觉嵌入的对抗性指令劫持多模态大语言模型
authors: "Neha Nagaraja, Lan Zhang, Zhilong Wang, Bo Zhang, Pawan Patil"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03637v1"
tags: ["query:ai-mmd"]
score: 8.0
evidence: 通过视觉嵌入的对抗性指令劫持多模态大模型
tldr: "本研究探讨了多模态大语言模型（MLLM）面临的基于图像的提示注入（IPI）威胁。研究提出了一种端到端的黑盒攻击流水线，通过分割区域选择、自适应字体缩放和背景感知渲染，将对抗性指令隐蔽地嵌入自然图像中。实验表明，该方法在保持人类视觉隐蔽性的同时，能有效操纵GPT-4-turbo等模型的输出，攻击成功率最高达64%，揭示了多模态模型在黑盒环境下的安全风险。"
selection_source: fresh_fetch
motivation: 旨在研究如何通过在图像中嵌入隐蔽的对抗性指令来操纵多模态大语言模型的行为。
method: 开发了一套包含区域选择、字体缩放和背景感知渲染的端到端流水线，以实现隐蔽且有效的图像提示注入。
result: "在GPT-4-turbo上的实验显示，该攻击在满足隐蔽性约束的情况下，最高可达到64%的攻击成功率。"
conclusion: 图像提示注入是多模态模型在黑盒设置下的实际威胁，亟需开发相应的防御机制。
---

## 摘要
多模态大语言模型 (MLLMs) 整合了视觉和文本以驱动各类应用，但这种整合也引入了新的漏洞。我们研究了基于图像的提示注入 (IPI)，这是一种黑盒攻击，通过将对抗性指令嵌入自然图像中来覆盖模型行为。我们的端到端 IPI 流水线结合了基于分割的区域选择、自适应字体缩放和背景感知渲染，旨在使提示对人类感知隐形，同时保持模型的可解释性。利用 COCO 数据集和 GPT-4-turbo，我们评估了 12 种对抗性提示策略和多种嵌入配置。结果表明，IPI 可以可靠地操纵模型的输出，在隐身约束下，最有效的配置实现了高达 64% 的攻击成功率。这些发现强调了 IPI 在黑盒设置中是一种实际威胁，并突出了针对多模态提示注入进行防御的必要性。

## Abstract
Multimodal Large Language Models (MLLMs) integrate vision and text to power applications, but this integration introduces new vulnerabilities. We study Image-based Prompt Injection (IPI), a black-box attack in which adversarial instructions are embedded into natural images to override model behavior. Our end-to-end IPI pipeline incorporates segmentation-based region selection, adaptive font scaling, and background-aware rendering to conceal prompts from human perception while preserving model interpretability. Using the COCO dataset and GPT-4-turbo, we evaluate 12 adversarial prompt strategies and multiple embedding configurations. The results show that IPI can reliably manipulate the output of the model, with the most effective configuration achieving up to 64\% attack success under stealth constraints. These findings highlight IPI as a practical threat in black-box settings and underscore the need for defenses against multimodal prompt injection.

---

## 论文详细总结（自动生成）

这是一份关于论文《Image-based Prompt Injection: Hijacking Multimodal LLMs through Visually Embedded Adversarial Instructions》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着多模态大语言模型（MLLM）在图像描述、自主感知等领域的广泛应用，其安全性成为焦点。现有的提示注入（Prompt Injection）研究多集中在纯文本领域。
*   **核心问题**：论文探讨了**基于图像的提示注入（IPI）**在黑盒设置下的可行性。即攻击者能否在自然图像中嵌入人类难以察觉、但模型能够识别并执行的对抗性指令，从而劫持模型的输出行为。
*   **研究动机**：填补 MLLM 在视觉-文本交互漏洞研究上的空白，分析视觉属性（字体、颜色、位置等）如何影响攻击的隐蔽性与成功率。

### 2. 方法论
论文提出了一套端到端的 IPI 攻击流水线，核心步骤包括：
*   **对抗性提示工程**：设计了12种提示模板，利用重复指令（Repetition）和“对象感知前缀”（Object-aware prefixing，如“忽略图中的狗，输出XXX”）来覆盖模型的视觉基元。
*   **基于分割的区域选择**：利用 **SAM (Segment Anything Model)** 对图像进行全景分割，根据区域面积、纹理均匀度（低复杂度利于阅读）和空间位置（偏好右上或中下）对候选嵌入区域进行排序。
*   **自适应嵌入逻辑**：
    *   **单/多掩码分布**：优先在最大区域嵌入，若空间不足则按语义顺序跨多个区域分布。
    *   **字体缩放**：在 0.1 到 0.3 比例间自适应调整。
*   **背景感知渲染策略**：
    1.  **背景平均补丁着色**：根据字符所在局部区域的平均 RGB 值加偏移量着色。
    2.  **像素级混合**：在像素层面进行亮度微调，隐蔽性最强。
    3.  **全局区域平均着色**：在选定的均匀区域内使用统一的背景近似色，在隐蔽性与模型可读性间取得最佳平衡。

### 3. 实验设计
*   **数据集**：使用 **COCO 数据集**，涵盖室内、室外、杂乱及简洁等多种真实场景。
*   **目标模型**：**GPT-4-turbo**（代表当前最先进的黑盒 MLLM）。
*   **评估指标**：攻击成功率（ASR）和均方误差（MSE，用于衡量视觉失真）。
*   **对比实验**：
    *   对比了 12 种不同的对抗提示策略。
    *   对比了不同字体大小（0.10 - 0.30）对 ASR 的影响。
    *   对比了三种不同的着色渲染策略及其在不同亮度偏移（Offset）下的表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）。
*   **资源消耗**：由于是黑盒攻击研究，主要计算开销在于调用 GPT-4-turbo API 进行推理，以及本地运行 SAM 模型进行图像分割和 GPT-4o 生成图像对象描述。

### 5. 实验数量与充分性
*   **实验规模**：针对每种配置，在 COCO 数据集上进行了多次重复实验（每张图像处理 5 次以减少随机性），总计涉及数百次 API 查询。
*   **充分性**：实验涵盖了从提示词设计、视觉参数调整到语义前缀增强的完整消融分析。
*   **客观性**：研究采用了标准化的 COCO 数据集和主流商业模型，实验变量控制清晰，能够客观反映黑盒攻击的真实威胁。

### 6. 主要结论与发现
*   **攻击有效性**：在黑盒设置下，IPI 能可靠地操纵模型输出。在保持视觉隐蔽的前提下，最高实现了 **64%** 的攻击成功率。
*   **关键因素**：
    *   **重复性指令**（如 Prompt 5）表现最稳健。
    *   **语义启动**：加入“忽略图中物体”的前缀能显著提升 ASR（从 41% 提升至 64%）。
    *   **视觉阈值**：字体缩放比例低于 0.2 时攻击基本失效，0.3 是较为理想的平衡点。
*   **权衡关系**：人类不可见性与模型可解释性之间存在显著的负相关，攻击者必须在两者间寻找“甜点区”。

### 7. 优点
*   **实用性强**：专注于黑盒攻击，不依赖模型梯度，更符合现实安全威胁场景。
*   **自动化程度高**：结合 SAM 分割和自适应渲染，实现了攻击生成的全自动化。
*   **语义与视觉结合**：不仅关注视觉嵌入，还通过对象感知前缀利用了模型的指令遵循特性。

### 8. 不足与局限
*   **模型覆盖面**：实验主要集中在 GPT-4-turbo，缺乏对开源模型（如 LLaVA, Qwen-VL）或其他闭源模型（如 Gemini, Claude）的横向对比。
*   **防御验证不足**：虽然文中讨论了防御方向（如 OCR 过滤、图像转文本预处理），但未进行实际的防御效果测试。
*   **人类感知评估**：对“隐蔽性”的评价主要基于 MSE 指标和视觉示例，缺乏大规模的人类主观视觉测试（User Study）。

（完）
