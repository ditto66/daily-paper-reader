---
title: "AgentSentry: Mitigating Indirect Prompt Injection in LLM Agents via Temporal Causal Diagnostics and Context Purification"
title_zh: AgentSentry：通过时序因果诊断与上下文净化缓解 LLM 智能体中的间接提示注入
authors: "Tian Zhang, Yiwei Xu, Juan Wang, Keyan Guo, Xiaoyang Xu, Bowen Xiao, Quanlong Guan, Jinlin Fan, Jiawei Liu, Zhiquan Liu, Hongxin Hu"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22724v1"
tags: ["query:ai-mmd"]
score: 8.0
evidence: 缓解自主 LLM 智能体中的间接提示注入攻击
tldr: 针对大语言模型智能体面临的间接提示注入（IPI）威胁，本文提出AgentSentry防御框架。该框架首次将多轮IPI建模为时间因果接管，通过在工具返回边界进行受控的反事实重新执行来定位攻击点，并利用因果引导的上下文净化技术消除恶意偏差，同时保留任务相关信息。实验表明，AgentSentry在保持良性性能的同时，显著提升了攻击下的任务效用，为多轮交互场景下的智能体安全提供了新思路。
selection_source: carryover_cache
motivation: 现有的间接提示注入防御主要依赖启发式检测，在复杂的多轮交互中容易导致任务过早终止或误伤正常工具调用。
method: 提出一种基于时间因果诊断和上下文净化的推理时防御框架，通过反事实重新执行定位攻击接管点并净化恶意上下文。
result: "在AgentDojo基准测试中，AgentSentry将攻击下的平均效用提升至74.55%，比最强基线高出20.8至33.6个百分点。"
conclusion: AgentSentry通过因果建模有效解决了多轮交互中的间接提示注入问题，在不损害良性性能的前提下显著增强了智能体的安全性。
---

## 摘要
大语言模型 (LLM) 智能体越来越多地依赖外部工具和检索系统来自主完成复杂任务。然而，这种设计使智能体面临间接提示注入 (IPI) 的风险，其中嵌入在工具输出或检索内容中的攻击者受控上下文会悄无声息地引导智能体行为偏离用户意图。与基于提示的攻击不同，IPI 在多轮轨迹中展开，使得恶意控制难以从合法的任务执行中剥离。现有的推理阶段防御主要依赖于启发式检测和对高风险行为的保守拦截，这可能会在模糊的多轮场景下过早终止工作流或广泛抑制工具的使用。我们提出了 AgentSentry，一种针对工具增强型 LLM 智能体的新型推理阶段检测与缓解框架。据我们所知，AgentSentry 是首个将多轮 IPI 建模为时序因果接管的推理阶段防御方案。它通过在工具返回边界进行受控的反事实重新执行来定位接管点，并通过因果引导的上下文净化实现安全持续运行，该净化过程在保留任务相关证据的同时移除攻击诱导的偏差。我们在 AgentDojo 基准测试上评估了 AgentSentry，涵盖四个任务套件、三类 IPI 攻击家族以及多个黑盒 LLM。AgentSentry 消除了成功的攻击并在攻击下保持了强大的效用，实现了 74.55% 的平均攻击下效用 (UA)，在不降低良性性能的情况下，比最强的基准模型提高了 20.8 到 33.6 个百分点。

## Abstract
Large language model (LLM) agents increasingly rely on external tools and retrieval systems to autonomously complete complex tasks. However, this design exposes agents to indirect prompt injection (IPI), where attacker-controlled context embedded in tool outputs or retrieved content silently steers agent actions away from user intent. Unlike prompt-based attacks, IPI unfolds over multi-turn trajectories, making malicious control difficult to disentangle from legitimate task execution. Existing inference-time defenses primarily rely on heuristic detection and conservative blocking of high-risk actions, which can prematurely terminate workflows or broadly suppress tool usage under ambiguous multi-turn scenarios. We propose AgentSentry, a novel inference-time detection and mitigation framework for tool-augmented LLM agents. To the best of our knowledge, AgentSentry is the first inference-time defense to model multi-turn IPI as a temporal causal takeover. It localizes takeover points via controlled counterfactual re-executions at tool-return boundaries and enables safe continuation through causally guided context purification that removes attack-induced deviations while preserving task-relevant evidence. We evaluate AgentSentry on the \textsc{AgentDojo} benchmark across four task suites, three IPI attack families, and multiple black-box LLMs. AgentSentry eliminates successful attacks and maintains strong utility under attack, achieving an average Utility Under Attack (UA) of 74.55 %, improving UA by 20.8 to 33.6 percentage points over the strongest baselines without degrading benign performance.

---

## 论文详细总结（自动生成）

### 论文总结：AgentSentry - 通过时序因果诊断与上下文净化缓解 LLM 智能体中的间接提示注入

#### 1. 核心问题与整体含义
论文针对的是**工具增强型大语言模型（LLM）智能体**面临的**间接提示注入（Indirect Prompt Injection, IPI）**威胁。在多轮对话和多工具调用的复杂任务中，攻击者通过在外部文档、邮件或网页中嵌入恶意指令，诱导智能体执行非授权操作（如数据外泄）。
*   **研究动机**：现有的防御手段（如启发式检测或严格过滤）往往过于保守，容易导致任务过早终止或误伤正常的工具调用，且难以识别在多轮交互中逐渐发生的“接管”过程。
*   **核心目标**：在不牺牲任务完成度（Utility）的前提下，精准识别并拦截恶意指令，实现智能体的“安全持续运行”。

#### 2. 方法论
AgentSentry 提出了一个推理时的防御框架，核心思想是将 IPI 建模为**时序因果接管（Temporal Causal Takeover）**。
*   **时序因果诊断**：在每个“工具返回边界”（即工具输出刚并入上下文、下一动作发出前），系统会进行受控的**反事实重新执行（Counterfactual Re-executions）**。
*   **四种干预方案**：
    1.  `orig`：原始输入 + 原始上下文。
    2.  `mask`：中性探测指令 + 原始上下文（测试上下文是否主导行为）。
    3.  `mask_sanitized`：中性探测指令 + 净化后的上下文。
    4.  `orig_sanitized`：原始输入 + 净化后的上下文。
*   **因果效应评估**：通过计算**平均因果效应（ACE）**、**间接效应（IE）**和**直接效应（DE）**，量化外部上下文对智能体决策的控制程度。如果 IE 显著增加且 ACE 衰减，则判定发生“接管”。
*   **上下文净化与安全持续**：一旦检测到接管，系统会净化上下文（保留事实证据，移除祈使句和优先级覆盖指令），并修正当前动作，而非直接终止任务。

#### 3. 实验设计
*   **基准测试**：使用 **AgentDojo** (v0.1.35)，包含 949 个安全测试案例。
*   **场景套件**：TRAVEL（旅行）、WORKSPACE（办公）、BANKING（银行）、SLACK（通讯）。
*   **攻击家族**：Important Instructions（重要指令）、Tool Knowledge（工具知识）、InjecAgent（注入代理）。
*   **对比方法**：
    *   提示词增强：Delimiting, Repeat Prompt。
    *   过滤与检测：Tool Filter, DeBERTa Detector。
    *   轨迹/对齐防御：MELON, Task Shield。
*   **测试模型**：GPT-4o, GPT-3.5-turbo, Qwen-3-Max。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **推理开销**：由于该方法属于推理时防御，且涉及多次反事实重新执行（每个边界需 $4K+B$ 次额外调用），其主要开销体现在 API 调用成本和推理延迟上。作者提到通过轻量化配置（如 $K=1$）可将开销控制在可接受范围内。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个领域、3 种攻击类型、3 个主流黑盒 LLM，总计近千组测试案例。
*   **消融实验**：对时序聚合、净化机制、探测指令类型等关键组件进行了详细的消融分析。
*   **充分性评价**：实验设计较为全面且客观。通过在不同能力等级的模型（从 GPT-3.5 到 GPT-4o）上验证，证明了该方法的泛化性。对比基线涵盖了从简单提示到最先进的轨迹防御，对比公平。

#### 6. 主要结论与发现
*   **防御效果**：AgentSentry 在所有测试中实现了 **0% 的攻击成功率 (ASR)**。
*   **效用保持**：在攻击环境下，平均任务效用 (UA) 达到 **74.55%**，比最强基线（Task Shield 和 MELON）高出 20.8 到 33.6 个百分点。
*   **良性性能**：在无攻击的正常场景下，AgentSentry 不会降低智能体的原有性能（FPR 为 0%）。
*   **因果价值**：实验证明，基于因果效应的诊断比单纯的语义相似度或启发式规则更能精准定位攻击点。

#### 7. 优点
*   **首创性**：首次将多轮 IPI 视为因果接管过程，提供了更深层的理论解释。
*   **高平衡性**：在极低 ASR 的同时保持了极高的任务完成度，解决了“防御即终止”的痛点。
*   **黑盒适用**：无需访问模型参数或进行微调，完全适用于商业 API 驱动的智能体。
*   **可解释性**：通过因果效应轨迹图，可以直观地看到攻击是在哪一轮、通过哪个工具输出开始产生影响的。

#### 8. 不足与局限
*   **推理延迟**：多次重新执行会显著增加单步决策的时间，对于实时性要求极高的应用可能存在挑战。
*   **成本问题**：API 调用次数成倍增加，增加了运行成本。
*   **基准限制**：目前的基准测试（AgentDojo）中，攻击往往在注入后较快触发。对于更长程、更隐蔽的“慢速渗透”攻击，该方法的时序窗口设置可能需要更复杂的调优。
*   **应用限制**：依赖于能够“回滚”或“模拟执行”的智能体环境，对于某些状态不可逆的物理环境工具调用，需依赖完善的 Dry-run 机制。

（完）
