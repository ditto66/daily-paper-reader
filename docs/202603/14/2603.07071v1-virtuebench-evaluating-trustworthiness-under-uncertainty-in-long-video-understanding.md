---
title: "VirtueBench: Evaluating Trustworthiness under Uncertainty in Long Video Understanding"
title_zh: VirtueBench：评估长视频理解中不确定性下的可信度
authors: "Xueqing Yu, Bohan Li, Yan Li, Zhenheng Yang"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07071v1"
tags: ["query:semma"]
score: 8.0
evidence: 评估视觉语言模型在不确定性下可信度与诚实性的基准
tldr: VirtueBench是一个评估视觉语言模型（VLM）在长视频理解中面对不确定性时可信度的基准。针对因抽帧导致关键信息缺失的情况，该基准通过多层级采样构建可回答与不可回答的案例，旨在纠正模型盲目猜测的行为，推动开发在信息不足时能诚实拒绝回答的可靠模型。
selection_source: fresh_fetch
motivation: 现有的长视频理解评估忽略了因关键帧缺失导致的不确定性，导致盲目猜测的模型得分虚高，而诚实拒绝的模型被误判。
method: 构建了包含不同帧采样层级的VirtueBench基准，明确区分可回答和不可回答的情况，以评估模型在信息缺失时的拒绝行为。
result: "对25个模型的评估显示，拒绝准确率差异巨大（0%到70%以上），且大多数模型在缺乏明确提示时拒绝意愿显著下降。"
conclusion: 评估结果强调了开发具备不确定性感知能力的可信VLM的必要性，并呼吁建立更注重可靠性的评估标准。
---

## 摘要
近期，视觉语言模型（VLMs）在多模态理解任务中取得了显著进展，但其在长视频理解方面的评估仍然不可靠。由于帧输入受限，回答问题所需的关键帧可能会在模型的输入中缺失。然而，在面临此类不确定性时，如实拒绝回答的模型会被标记为错误，而那些进行猜测的模型可能会巧合地给出正确答案，从而获得虚高的准确率。这导致了误导性的评估结果，并鼓励模型进行猜测而非诚实响应。为解决这一问题，我们推出了 VirtueBench，这是一个专门用于评估不确定性下模型可信度的基准测试。VirtueBench 为每个视频构建了多个帧采样级别，并提供了区分可回答和不可回答情况的基准真相（ground truths）。对 25 个开源和商业 VLMs 的评估揭示了不同模型系列之间截然不同的拒绝行为，拒绝准确率从表现最好模型的 70% 以上到最差模型的接近 0% 不等。此外，当提示词没有明确要求时，大多数模型的拒绝率会大幅下降。这些发现强调了在强调可靠性和可信度的基准测试和排行榜引导下，开发用于多模态理解的可信 VLMs 的必要性。

## Abstract
Recent Vision-Language Models (VLMs) have made remarkable progress in multimodal understanding tasks, yet their evaluation on long video understanding remains unreliable. Due to limited frame inputs, key frames necessary for answering the question may be missing from the model's input. However, models that truthfully refuse to answer under such uncertainty are marked as incorrect, while those that guess may coincidentally produce the correct answer and thus obtain deceptively higher accuracy, leading to misleading evaluation results and encouraging models to guess rather than respond honestly. To address this issue, we introduce VirtueBench, a benchmark explicitly designed to assess model trustworthiness under uncertainty. VirtueBench constructs multiple frame-sampling levels for each video and provides ground truths that distinguish between answerable and unanswerable cases. Evaluations on 25 open-source and commercial VLMs reveal distinct refusal behaviors across different model families, with refusal accuracy ranging from over 70% in the best models to nearly 0% in the worst. Moreover, most models exhibit a substantial drop in refusal when the prompt does not explicitly require them to do so. These findings highlight the need for developing trustworthy VLMs for multimodal understanding, guided by benchmarks and leaderboards that emphasize reliability and trustworthiness.

---

## 论文详细总结（自动生成）

这是一份关于论文《VirtueBench: Evaluating Trustworthiness under Uncertainty in Long Video Understanding》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前长视频理解模型（VLM）在处理超长视频时，受限于输入帧数（通常仅采样数百帧），往往会丢失回答问题所需的关键信息。
*   **研究动机**：现有的评估体系存在严重偏差——当关键帧缺失时，如实回答“不知道”的模型会被判错，而盲目猜测且碰巧猜对的模型反而获得高分。这种“奖励猜测、惩罚诚实”的机制误导了模型优化方向，鼓励了幻觉的产生，导致评估结果无法真实反映模型的可信度。
*   **整体含义**：论文旨在建立一个新的基准，评估模型在面对“信息不足”的不确定性时，能否表现出诚实性（即拒绝回答），从而推动开发更安全、更可靠的多模态模型。

### 2. 论文提出的方法论
*   **核心思想**：通过对同一视频进行不同密度的帧采样，构建一个动态的、区分“可回答性”的评估基准。
*   **关键技术细节**：
    *   **多层级采样**：将原始视频统一采样为 64、128、256、512 和 1024 帧五个层级。
    *   **动态标注**：针对每个采样层级，人工核查关键信息是否存在。若当前层级的帧不足以回答问题，则标准答案（Ground Truth）标注为“视频未提供足够信息”。
    *   **数据清洗流线**：从现有长视频基准（如 MLVU, Video-MME 等）收集数据，将多选题转化为开放式问题，并剔除仅凭常识即可回答的题目。
    *   **评估框架**：采用 **LLM-as-a-judge**（以 GPT-4o 为裁判）。评估分为两步：首先检测模型是否拒绝回答；其次，对于有答案的情况验证准确性，对于无答案的情况，仅当模型拒绝回答时才判定为正确。

### 3. 实验设计
*   **数据集/场景**：VirtueBench 包含 1,328 个高质量标注实例，涵盖感知（Perception，如物体识别、动作检测）和推理（Reasoning，如时间推理、因果关系）两大类。
*   **对比方法**：评估了 **25 个** 主流 VLM，包括：
    *   **开源模型**：Qwen2.5-VL 系列、Qwen3-VL 系列、InternVL3.5 系列、LLaVA-Video、Mimo-VL、Keye-VL 等。
    *   **商业模型**：GPT-4o、GPT-5 (chat/preview)、Gemini-2.5-Flash。
*   **Benchmark 设置**：对比了模型在不同帧数输入下的表现，并特别分析了“拒绝准确率”（Refusal Accuracy）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的训练算力（如 GPU 型号、数量或训练时长）。
*   **原因分析**：由于该论文的主要贡献是提出一个评估基准（Benchmark）并对现有模型进行推理测试，而非训练新模型，因此重点在于推理评估的流程。

### 5. 实验数量与充分性
*   **实验规模**：对 25 个模型在 5 个不同帧采样层级上进行了全面测试，实验组数超过 100 组（模型 × 采样层级）。
*   **消融实验**：进行了“提示词消融实验”，对比了在有/无“诚实性指令”下模型的拒绝行为变化。
*   **充分性与公平性**：
    *   **充分性**：实验覆盖了从 7B 到 72B 不同参数规模的模型，以及推理增强型（Thinking）模型，数据维度丰富。
    *   **客观性**：通过人工双重校验标注，并使用统一的 Prompt 和裁判模型，保证了评估标准的客观性。

### 6. 论文的主要结论与发现
*   **性能鸿沟**：Gemini-2.5-Flash 和 Qwen3-VL-32B 是表现最强的模型。
*   **拒绝行为差异**：不同家族模型表现迥异。Qwen 系列和商业模型表现出较强的拒绝能力（拒绝准确率 >50%），而 InternVL、LLaVA-Video 等模型几乎从不拒绝，倾向于盲目猜测。
*   **规模效应**：在同一家族中，参数量越大的模型通常拒绝行为越诚实。
*   **强化学习与思考模式的作用**：经过 RL 优化或具备“Thinking”模式的模型，其拒绝准确率显著提升，表明推理能力的增强有助于模型识别信息的不完整性。
*   **提示词敏感性**：大多数模型在没有明确要求“诚实”的情况下，拒绝率会暴跌，说明模型默认被训练成“答题机器”而非“诚实助手”。

### 7. 优点
*   **视角独特**：首次系统性地关注长视频理解中由于“采样丢失”导致的不确定性问题。
*   **设计科学**：多层级采样设计非常符合长视频处理的实际工程场景（即 token 预算限制）。
*   **纠偏作用**：通过引入“拒绝”作为正确选项，纠正了现有排行榜对“猜测行为”的盲目奖励。

### 8. 不足与局限
*   **评估成本与偏见**：高度依赖 GPT-4o 作为裁判，可能存在 LLM 偏见或高昂的评估成本。
*   **数据集规模**：1,328 个实例虽然质量高，但相比于一些大规模自动化生成的基准，规模仍显有限。
*   **应用限制**：目前仅作为评估工具，未提出具体的训练方案来解决模型不愿拒绝的问题。
*   **覆盖范围**：主要关注视觉信息的缺失，未深入探讨音频或字幕信息缺失带来的不确定性。

（完）
