---
title: "Towards Cold-Start Drafting and Continual Refining: A Value-Driven Memory Approach with Application to NPU Kernel Synthesis"
title_zh: 迈向冷启动草拟与持续精炼：一种价值驱动的记忆方法及其在 NPU 算子内核合成中的应用
authors: "Yujie Zheng, Zhuo Li, Shengtao Zhang, Hanjing Wang, Junjie Sheng, Jiaqian Wang, Junchi Yan, Weinan Zhang, Ying Wen, Bo Tang, Muning Wen"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10846v1"
tags: ["query:semma"]
score: 9.0
evidence: 用于持续优化的自进化智能体框架
tldr: 针对新兴NPU架构中内核合成面临的“数据墙”和冷启动挑战，本文提出EvoKernel框架。该框架采用基于价值驱动记忆的强化学习方法，通过自演进的智能体实现从初始草案生成到持续性能优化的全生命周期自动化。EvoKernel利用Q值检索机制和跨任务记忆共享，显著提升了通用大模型在数据稀缺领域的代码生成正确率和执行效率，为利基硬件生态的代码合成提供了新思路。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在数据稀缺的NPU编程领域因缺乏训练数据而导致的性能大幅下降和冷启动难题。
method: 提出一种名为EvoKernel的自演进智能体框架，利用价值驱动的记忆检索和强化学习机制来自动化内核的草案生成与性能精炼。
result: "在NPU内核基准测试中，该方法将模型的代码正确率从11.0%大幅提升至83.0%，并实现了3.60倍的执行加速。"
conclusion: 研究证明通过价值引导的经验积累和跨任务记忆共享，通用大模型能够有效掌握特定硬件领域的复杂内核合成任务。
---

## 摘要
将大语言模型部署到数据稀缺的编程领域面临着重大挑战，特别是在新兴的领域特定架构（DSA）上的算子内核合成，其中“数据墙”限制了可用的训练数据。虽然模型在 CUDA 等数据丰富的平台上表现出色，但在 NPU 编程等数据稀缺的生态系统中，其性能会发生灾难性的下降。为了在不进行昂贵微调的情况下克服这一冷启动障碍，我们引入了 EvoKernel，这是一个自我进化的智能体框架，能够自动化从初始草拟到持续精炼的算子内核合成全生命周期。EvoKernel 通过将合成过程建模为基于记忆的强化学习任务来解决这一问题。通过一种新颖的价值驱动检索机制，它学习特定阶段的 Q 值，根据经验对当前目标的贡献（无论是引导生成可行的草案还是迭代优化延迟）来确定经验的优先级。此外，通过启用跨任务记忆共享，该智能体能够将从简单算子中获得的见解推广到复杂算子。通过构建 KernelBench 的 NPU 变体并进行评估，EvoKernel 将前沿模型的正确率从 11.0% 提升至 83.0%，并通过迭代精炼实现了相比初始草案 3.60 倍的中值加速。这表明，价值引导的经验积累使通用模型能够掌握小众硬件生态系统中的算子内核合成任务。我们的官方页面位于 https://evokernel.zhuo.li。

## Abstract
Deploying Large Language Models to data-scarce programming domains poses significant challenges, particularly for kernel synthesis on emerging Domain-Specific Architectures where a "Data Wall" limits available training data. While models excel on data-rich platforms like CUDA, they suffer catastrophic performance drops on data-scarce ecosystems such as NPU programming. To overcome this cold-start barrier without expensive fine-tuning, we introduce EvoKernel, a self-evolving agentic framework that automates the lifecycle of kernel synthesis from initial drafting to continual refining. EvoKernel addresses this by formulating the synthesis process as a memory-based reinforcement learning task. Through a novel value-driven retrieval mechanism, it learns stage-specific Q-values that prioritize experiences based on their contribution to the current objective, whether bootstrapping a feasible draft or iteratively refining latency. Furthermore, by enabling cross-task memory sharing, the agent generalizes insights from simple to complex operators. By building an NPU variant of KernelBench and evaluating on it, EvoKernel improves frontier models' correctness from 11.0% to 83.0% and achieves a median speedup of 3.60x over initial drafts through iterative refinement. This demonstrates that value-guided experience accumulation allows general-purpose models to master the kernel synthesis task on niche hardware ecosystems. Our official page is available at https://evokernel.zhuo.li.

---

## 论文详细总结（自动生成）

### 论文总结：EvoKernel —— 价值驱动的 NPU 内核合成自进化框架

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大语言模型（LLMs）在 CUDA 等数据丰富的编程领域表现优异，但在新兴的领域特定架构（DSA，如华为昇腾 NPU）上面临“数据墙”挑战。由于缺乏公开代码、文档晦涩且编译器反馈模糊，LLM 在这些领域的性能会发生“灾难性塌陷”（如 GPT-5.2 在 CUDA 上正确率 92%，但在 NPU 上仅 14%）。
*   **核心问题**：如何在不进行昂贵微调（SFT）且缺乏专家演示的情况下，让通用 LLM 自主掌握数据稀缺的硬件内核（Kernel）合成任务？
*   **整体含义**：论文提出了 **EvoKernel**，一个通过自进化记忆和强化学习驱动的智能体框架，旨在解决 NPU 编程的冷启动草拟与持续性能精炼问题。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：将内核合成建模为**基于记忆的马尔可夫决策过程（M-MDP）**。智能体不更新模型权重，而是通过学习“检索什么记忆最有价值”来进化。
*   **关键技术细节**：
    *   **两阶段流水线**：
        1.  **冷启动草拟（Drafting）**：目标是实现功能正确性。利用 Q1 值检索有助于生成可行代码的经验。
        2.  **持续精炼（Refining）**：目标是降低延迟。利用 Q2 值检索优化技巧和高性能代码片段。
    *   **价值驱动检索（Value-Driven Retrieval）**：不同于传统的语义相似度检索（RAG），EvoKernel 为记忆条目学习特定阶段的 **Q 值**。
    *   **统一价值更新**：采用蒙特卡洛（MC）更新规则：$Q = Q + \alpha(r - Q)$。奖励 $r$ 根据编译结果、正确性和延迟提升动态计算。
    *   **多门控验证器（Multi-Gate Verifier）**：包含防作弊（防止模型调用 Python API 伪造结果）、编译、正确性和性能分析四个门控。
    *   **跨任务记忆共享**：允许智能体将简单算子（L1）的经验迁移到复杂算子（L2）。

#### 3. 实验设计
*   **数据集/场景**：
    *   **KernelBench (NPU 变体)**：将原有的 GPU 基准测试适配至华为 Ascend C 编程语言。
    *   **扩展负载**：Attention Set（70个算子）和 DeepSeek mHC 内核（15个算子）。
*   **对比方法**：
    *   **Pass@k**：无状态的多次采样。
    *   **Iterative Refinement**：带反馈的简单迭代修复（无跨任务记忆）。
    *   **Codex (OpenAI)**：具备自主 Shell 和文件系统访问权限的强力智能体。
*   **基座模型**：GPT-5.2、DeepSeek-V3.2、Qwen3-Coder-30B。

#### 4. 资源与算力
*   **算力设备**：论文提到在华为昇腾（Ascend）硬件上进行内核编译与性能分析，但**未明确说明具体的 NPU 型号数量及总训练/演进时长**。
*   **实验配置**：每个算子的迭代预算设为 $T=30$；验证过程设有 20 分钟的超时限制。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 250 个以上的内核算子，进行了主实验、消融实验、跨难度等级迁移实验（L1 $\to$ L2）以及跨模型迁移实验。
*   **充分性与公平性**：
    *   实验对比了不同能力的基座模型，证明了方法的普适性。
    *   消融实验对比了“价值驱动”与“启发式/相似度驱动”的检索，验证了核心组件的有效性。
    *   引入了防作弊机制，确保了实验结果的客观性，防止模型通过“走捷径”通过测试。

#### 6. 主要结论与发现
*   **性能飞跃**：在 GPT-5.2 上，EvoKernel 将 NPU 内核的正确率从 11.0% 提升至 **83.0%**。
*   **优化显著**：在获得正确代码后，通过持续精炼实现了 **3.60 倍**的中值加速，部分算子甚至超过了官方 Torch-NPU 的性能。
*   **记忆迁移**：强模型（GPT-5.2）积累的记忆可以显著提升弱模型（DeepSeek/Qwen）的表现，且简单任务的经验能有效加速复杂任务的解决。
*   **涌现课程**：价值驱动机制使智能体自发形成了从易到难的“隐式课程学习”。

#### 7. 优点（亮点）
*   **无需微调**：通过非参数化的记忆演进解决冷启动问题，避免了昂贵的模型训练。
*   **价值导向**：突破了传统 RAG 仅依赖语义相似度的局限，通过强化学习识别真正“好用”的参考代码。
*   **闭环进化**：集成了从代码生成、硬件反馈到价值更新的完整闭环，具备极强的自主性。

#### 8. 不足与局限
*   **基座依赖性**：实验显示该方法在强模型（如 GPT-5.2）上效果惊人，但在弱模型上提升相对有限，说明其依赖模型的高级上下文学习能力。
*   **验证开销**：内核合成涉及真实的编译和硬件运行，每一轮迭代的时间成本较高。
*   **领域局限**：目前主要针对 Ascend C 语言，虽然理论上可迁移，但针对其他新兴硬件（如 TPU、国产其他芯片）的泛化性尚需更多实验验证。

（完）
