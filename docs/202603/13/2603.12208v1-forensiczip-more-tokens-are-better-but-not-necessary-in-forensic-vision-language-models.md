---
title: "ForensicZip: More Tokens are Better but Not Necessary in Forensic Vision-Language Models"
title_zh: ForensicZip：取证视觉语言模型中 Token 越多越好但并非必要
authors: "Yingxin Lai, Zitong Yu, Jun Wang, Linlin Shen, Yong Xu, Xiaochun Cao"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12208v1"
tags: ["query:ai-mmd"]
score: 9.0
evidence: 用于伪造检测的取证视觉语言模型
tldr: "针对多模态大模型在多媒体取证中计算开销大的问题，本文提出ForensicZip框架。该方法摒弃了传统的语义驱动剪枝，转而从伪造驱动视角出发，利用出生-死亡最优传输模型量化时间不连续性，并结合高频先验提取关键取证证据。在仅保留10%标记的情况下，实现了显著的推理加速和计算量降低，同时保持了顶尖的伪造检测性能。"
selection_source: fresh_fetch
motivation: 现有的视觉标记剪枝方法主要关注语义显著性，容易误删背景中包含的高频异常和时间抖动等关键伪造痕迹。
method: 引入一种无需训练的框架，通过出生-死亡最优传输模型量化标记演化，并结合高频先验来区分取证证据与语义内容。
result: "在Deepfake和AIGC基准上，该方法在10%标记保留率下实现了2.97倍加速和超过90%的计算量削减，且性能保持领先。"
conclusion: ForensicZip证明了针对取证特征进行标记压缩可以在大幅降低多模态大模型计算成本的同时，不损失伪造检测的准确性。
---

## 摘要
多模态大语言模型 (MLLMs) 通过为伪造检测生成文本解释，实现了可解释的多媒体取证。然而，处理密集的视觉序列会产生高昂的计算成本，特别是对于高分辨率图像和视频。视觉 Token 剪枝是一种实用的加速策略，但现有方法主要由语义驱动，即保留显著对象而丢弃背景区域，而高频异常和时间抖动等篡改痕迹通常存在于背景中。为了解决这一问题，我们引入了 ForensicZip，这是一个无需训练的框架，它从伪造驱动的角度重新定义了 Token 压缩。ForensicZip 将时间维度的 Token 演化建模为带有松弛虚拟节点的生灭最优传输 (Birth-Death Optimal Transport) 问题，从而量化指示瞬态生成伪影的物理不连续性。取证评分进一步结合了基于传输的新颖性与高频先验，以便在大比例压缩下将取证证据与语义内容分离。在 Deepfake 和 AIGC 基准测试上的实验表明，在 10% 的 Token 保留率下，ForensicZip 实现了 2.97 倍的加速和超过 90% 的 FLOPs 减少，同时保持了最先进的检测性能。

## Abstract
Multimodal Large Language Models (MLLMs) enable interpretable multimedia forensics by generating textual rationales for forgery detection. However, processing dense visual sequences incurs high computational costs, particularly for high-resolution images and videos. Visual token pruning is a practical acceleration strategy, yet existing methods are largely semantic-driven, retaining salient objects while discarding background regions where manipulation traces such as high-frequency anomalies and temporal jitters often reside. To address this issue, we introduce ForensicZip, a training-free framework that reformulates token compression from a forgery-driven perspective. ForensicZip models temporal token evolution as a Birth-Death Optimal Transport problem with a slack dummy node, quantifying physical discontinuities indicating transient generative artifacts. The forensic scoring further integrates transport-based novelty with high-frequency priors to separate forensic evidence from semantic content under large-ratio compression. Experiments on deepfake and AIGC benchmarks show that at 10\% token retention, ForensicZip achieves $2.97\times$ speedup and over 90\% FLOPs reduction while maintaining state-of-the-art detection performance.

---

## 论文详细总结（自动生成）

### ForensicZip 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在多媒体取证领域展现出强大的解释能力，但其处理高分辨率图像或长视频时，密集的视觉 Token 会导致巨大的计算开销（预填充阶段的计算瓶颈）。
*   **现有矛盾**：现有的 Token 剪枝技术多为“语义驱动”，倾向于保留视觉显著的对象（如人脸、物体中心），而丢弃背景。
*   **取证特异性**：伪造痕迹（如高频噪声、边缘不连续、时间抖动）往往分布在语义不显著的背景或边缘区域。
*   **研究目标**：提出一种“伪造驱动”的 Token 压缩框架，在大幅减少 Token 数量的同时，精准保留关键的取证证据。

#### 2. 方法论：核心思想与关键技术
ForensicZip 是一个**无需训练（Training-free）**的插件式框架，其核心思想是将 Token 的演化视为物理守恒规律的违反。

*   **传输新颖性估计 (Transport Novelty Estimation, TNE)**：
    *   将相邻帧之间的 Token 演化建模为**生灭最优传输 (Birth-Death Optimal Transport)** 问题。
    *   引入**虚拟节点 (Dummy Node)**：在计算 Token 匹配成本矩阵时增加一行一列。如果一个 Token 在下一帧找不到合理的来源，其质量会被路由到虚拟节点，产生“出生（Birth）”成本；反之则为“死亡（Death）”成本。
    *   通过 Sinkhorn 迭代求解传输计划，量化这种指示生成伪影的物理不连续性。
*   **取证评分 (Forensic Scoring, FS)**：
    *   **高频先验集成**：利用 3x3 的拉普拉斯（Laplacian）算子提取空间域的高频响应。
    *   **软与门（Soft AND Gate）**：将时间维度的传输异常得分与空间维度的边缘响应相乘。只有同时具备时间不连续性和空间高频特征的 Token 才会获得高分。
*   **物理 Top-K 剪枝**：根据最终得分保留前 $K$ 个 Token，直接缩短输入 LLM 的序列长度，实现端到端加速。

#### 3. 实验设计
*   **数据集与场景**：
    *   **通用与 AIGC 检测**：FakeVLM (FakeClue, LOKI), DMimage, FakeShield-AIGC, SIDA。
    *   **Deepfake 取证**：DD-VQA, FakeShield-DeepFake, DFFD。
    *   **传统篡改与幻觉测试**：FakeShield-PhotoShop (CASIA 等), POPE。
*   **Benchmark 与对比方法**：
    *   **基准模型**：LLaVA-OneVision-7B, SIDA-7B/13B, FakeVLM, FakeShield。
    *   **对比剪枝方法**：FastV, SparseVLM, VisionTrim, LLaVA-Scissor。
*   **评估指标**：准确率 (Acc), F1 分数, FLOPs, 推理延迟 (Latency), 显存占用 (GPU Mem), 吞吐量。

#### 4. 资源与算力
*   **硬件环境**：使用了 **NVIDIA A100 (80GB)** GPU。
*   **训练时长**：由于该方法是**无需训练**的推理侧优化，因此不存在训练时长。
*   **推理开销**：文中指出最优传输求解器（OT Solver）的额外开销极低（线性增长），相对于 Transformer 层的计算节省几乎可以忽略不计。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从图像到视频、从人脸伪造到 AIGC 生成的 8 个以上主流取证数据集。
*   **消融实验**：非常充分。分别验证了 TNE 模块、FS 模块、不同最优传输公式（平衡 vs 不平衡）、不同空间算子（Sobel vs Laplacian）以及超参数（正则化强度、惩罚项、迭代次数）的影响。
*   **客观性**：通过在 POPE 这种非取证基准上测试，证明了该方法在减少冗余的同时不会损害模型的通用理解能力，甚至能通过减少噪声提升精度。

#### 6. 主要结论与发现
*   **高效压缩**：在仅保留 **10%** 视觉 Token 的极端情况下，ForensicZip 实现了 **2.97 倍**的推理加速，并减少了超过 **90%** 的 FLOPs。
*   **性能领先**：在 10% 保留率下，传统语义剪枝方法性能往往会崩溃（接近随机猜测），而 ForensicZip 在 FakeClue 等数据集上仍能保持 **97.74%** 的高准确率。
*   **显存优化**：物理剪枝使得显存占用从 26.7GB 降至 18.5GB，显著提升了部署的实用性。

#### 7. 优点（亮点）
*   **范式创新**：首次指出语义显著性与取证证据之间的“逆相关”性，将剪枝逻辑从“看物体”转向“找异常”。
*   **数学严谨**：引入带虚拟节点的最优传输理论，能够优雅地捕捉生成模型在时空维度上的物理违和感。
*   **即插即用**：无需任何重新训练或微调，可直接应用于现有的各种取证 MLLM。

#### 8. 不足与局限
*   **单图模式退化**：在处理单张图像时，由于缺乏时间维度，TNE 模块会退化为简单的空间均值对比，其捕捉伪影的能力可能不如视频序列强。
*   **超参数依赖**：虽然实验证明其对超参数较鲁棒，但在极端不同的生成算法（如不同扩散步数或架构）下，生灭惩罚项 $c$ 可能需要微调以达到最优效果。
*   **应用限制**：主要针对具有物理不连续性的生成/篡改痕迹，对于语义层面的高级伪造（如完全真实的重演但内容虚假）可能仍需依赖 LLM 本身的逻辑推理。

（完）
