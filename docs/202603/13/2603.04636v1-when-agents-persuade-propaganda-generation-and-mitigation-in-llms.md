---
title: "When Agents Persuade: Propaganda Generation and Mitigation in LLMs"
title_zh: 当智能体进行说服：大语言模型中的宣传生成与缓解
authors: "Julia Jose, Ritik Roongta, Rachel Greenstadt"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04636v1"
tags: ["query:ai-mmd"]
score: 8.0
evidence: LLM智能体中的宣传生成与缓解
tldr: 本研究探讨了大语言模型（LLM）在开放环境中被利用生成操纵性宣传内容的风险。研究者通过诱导LLM生成宣传文本，并利用专门的分类和技术检测模型分析其修辞手段，发现LLM具有显著的宣传倾向。为此，研究对比了SFT、DPO和ORPO三种微调方法，证明微调能有效降低此类风险，其中ORPO在抑制有害宣传内容生成方面表现最优。
selection_source: fresh_fetch
motivation: 旨在评估LLM在被恶意利用时生成操纵性宣传内容的风险及其背后的修辞机制。
method: 通过提示词诱导LLM生成宣传文本，利用领域特定模型检测修辞技巧，并对比了SFT、DPO和ORPO的缓解效果。
result: 实验证明LLM在受诱导时会使用多种修辞技巧生成宣传内容，而微调技术能显著降低这种倾向。
conclusion: ORPO在抑制LLM生成宣传性内容方面最为有效，是提升模型安全性的关键手段。
---

## 摘要
尽管基于大语言模型（LLM）的智能体在开放环境中部署具有广泛的益处，但它们也可能被利用来产生具有操纵性的材料。在本研究中，我们为 LLM 设定了宣传任务目标，并使用两个领域特定模型分析其输出：一个模型将文本分类为宣传或非宣传，另一个模型则检测宣传的修辞技巧（例如，带有偏见的语言、诉诸恐惧、挥舞旗帜、谩骂）。我们的研究结果表明，在受到提示时，LLM 会表现出宣传行为，并在这一过程中使用多种修辞技巧。我们还探索了通过有监督微调（SFT）、直接偏好优化（DPO）和优势比偏好优化（ORPO）进行的缓解方法。我们发现，微调显著降低了它们生成此类内容的倾向，其中 ORPO 被证明最为有效。

## Abstract
Despite their wide-ranging benefits, LLM-based agents deployed in open environments can be exploited to produce manipulative material. In this study, we task LLMs with propaganda objectives and analyze their outputs using two domain-specific models: one that classifies text as propaganda or non-propaganda, and another that detects rhetorical techniques of propaganda (e.g., loaded language, appeals to fear, flag-waving, name-calling). Our findings show that, when prompted, LLMs exhibit propagandistic behaviors and use a variety of rhetorical techniques in doing so. We also explore mitigation via Supervised Fine-Tuning (SFT), Direct Preference Optimization (DPO), and ORPO (Odds Ratio Preference Optimization). We find that fine-tuning significantly reduces their tendency to generate such content, with ORPO proving most effective.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了大语言模型（LLM）在生成宣传性内容（Propaganda）方面的风险及其缓解策略。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着 LLM 智能体在开放环境中的广泛部署，恶意行为者可能利用其生成具有心理操纵性的宣传材料。
*   **核心问题**：论文旨在回答：LLM 是否能生成宣传内容？它们使用了哪些具体的修辞技巧？如何通过微调有效降低这种倾向？
*   **背景**：宣传不同于简单的虚假信息，它通过逻辑谬误、情感诉求和心理战术（如诉诸恐惧、谩骂等）来塑造认知。

### 2. 方法论
*   **核心思想**：建立一套“检测-生成-缓解”的闭环框架。首先训练高精度的宣传检测器，随后诱导 LLM 生成内容并进行细粒度分析，最后对比不同的偏好对齐算法。
*   **关键技术细节**：
    *   **检测模型**：基于 RoBERTa-large 训练了两个模型：一个用于二分类（宣传 vs 非宣传，F1=0.98），另一个用于识别 6 种核心修辞技巧（谩骂、带偏见的语言、怀疑、诉诸恐惧、挥舞旗帜、夸大/缩小，平均 F1=0.82）。
    *   **生成策略**：从 QProp 数据集中提取 1000 条论文陈述（Thesis Statements），提示 GPT-4o、Llama 3.1 和 Mistral Small 3 分别生成宣传性和非宣传性文章。
    *   **缓解技术**：对比了三种微调方法：**SFT**（有监督微调）、**DPO**（直接偏好优化）和 **ORPO**（优势比偏好优化）。ORPO 的核心在于将 SFT 和偏好对齐整合进单一训练目标，无需参考模型。

### 3. 实验设计
*   **数据集**：
    *   **QProp**：包含 5.1 万篇新闻文章，用于训练文档级分类器。
    *   **PTC (Propaganda Techniques Corpus)**：包含 18 种修辞技巧的标注，用于训练细粒度检测器。
*   **Benchmark 与对比对象**：
    *   **模型对比**：GPT-4o、Llama 3.1-8B、Mistral Small 3。
    *   **基准对比**：将 LLM 生成的内容与人类编写的宣传文章进行对比。
    *   **方法对比**：对比了无微调、SFT、DPO 和 ORPO 四种状态下的模型表现。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA A100 80GB GPU**。
*   **训练细节**：
    *   使用 BF16 精度。
    *   微调 Llama 3.1-8B 时采用了 **QLoRA** 技术（4-bit 量化）。
    *   微调参数：学习率 1e-5，Batch Size 1（梯度累积 4 步），训练 30 个 Epoch。
    *   检测模型训练：学习率 1e-5，Batch Size 8-16，训练 10 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：生成了 6 个数据集（3 个模型 × 2 种提示词），涵盖 1000 条论文陈述。
*   **验证充分性**：
    *   **自动化评估**：对数千篇文章进行了分类和技巧检测。
    *   **人工验证**：三位专家对 200 篇 Llama 生成的文章进行了双盲标注，计算了 Krippendorff’s α（0.83-0.88），证明了自动化检测器的可靠性。
*   **客观性**：实验覆盖了闭源（GPT-4o）和开源（Llama, Mistral）模型，并使用了统计检验（Mann-Whitney U 检验）来验证显著性。

### 6. 主要结论与发现
*   **生成能力**：LLM 能够轻易生成高度宣传性的内容。GPT-4o 和 Mistral 3 在诱导下生成的文章 99% 被识别为宣传。
*   **修辞特征**：LLM 在“带偏见的语言”、“夸大/缩小”和“挥舞旗帜”等技巧的使用频率上显著**超过人类**，表现出更强的情感煽动性。
*   **缓解效果**：微调能显著降低风险。未微调的 Llama 宣传率为 77%，SFT 降至 14%，DPO 降至 28%，而 **ORPO 表现最优，降至 10%**，且修辞技巧的使用频率降低了 13.4 倍。
*   **安全脆弱性**：简单的系统提示词（Guardrails）极易被用户指令绕过，权重级的微调更为可靠。

### 7. 优点
*   **细粒度分析**：不仅关注“是否是宣传”，还深入分析了 6 种具体的心理操纵技巧，提供了更强的解释性。
*   **方法前沿**：引入了 ORPO 这一较新的对齐算法，并证明了其在安全合规领域的优越性。
*   **高可靠性**：通过严格的人工标注验证了自动化检测工具的有效性，使大规模分析具有说服力。

### 8. 不足与局限
*   **技巧覆盖有限**：仅研究了 6 种技巧，忽略了“诉诸平民”、“以偏概全”或“诉诸伪善（Whataboutism）”等其他重要手段。
*   **检测局限**：检测器基于句子级别，难以捕捉跨句子的修辞技巧（如“重复”）。
*   **模型范围**：虽然涵盖了主流模型，但未测试推理模型（如 o1, o3）或 Anthropic 的 Claude 系列（文中提到 Claude 拒绝生成此类内容，未纳入深度分析）。
*   **静态评估**：研究是在受控环境下进行的，未模拟 LLM 智能体在社交网络中自主演化和传播宣传内容的动态过程。

（完）
