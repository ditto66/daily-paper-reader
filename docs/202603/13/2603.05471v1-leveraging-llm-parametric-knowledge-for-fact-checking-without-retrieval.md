---
title: Leveraging LLM Parametric Knowledge for Fact Checking without Retrieval
title_zh: 利用大语言模型参数化知识进行无需检索的事实核查
authors: "Artem Vazhentsev, Maria Marina, Daniil Moskovskiy, Sergey Pletenev, Mikhail Seleznyov, Mikhail Salnikov, Elena Tutubalina, Vasily Konovalov, Irina Nikishina, Alexander Panchenko, Viktor Moskvoretskii"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05471v1"
tags: ["query:ai-mmd"]
score: 8.0
evidence: 利用LLM参数化知识为智能体AI系统进行事实核查
tldr: 本研究探讨了在不依赖外部检索的情况下，利用大语言模型（LLM）的参数化知识进行事实核查。针对传统检索增强方法受限于检索误差和数据可用性的问题，作者提出了无需检索的事实核查任务，并构建了涵盖长尾知识、多语言等维度的评估框架。研究发现利用模型内部表示优于基于概率的方法，据此提出INTRA方法，实现了SOTA性能，为提升AI系统可信度提供了新路径。
selection_source: fresh_fetch
motivation: 传统事实核查依赖外部检索，易受检索错误限制且未充分利用LLM自身的内在知识。
method: 提出了无需检索的事实核查任务及评估框架，并开发了利用模型内部表示交互的INTRA方法。
result: 实验表明INTRA在9个数据集上均优于基于Logit的方法，展现出强大的泛化能力和SOTA性能。
conclusion: 无需检索的事实核查是提升系统可扩展性及作为训练奖励信号的有效方向，可与检索框架互补。
---

## 摘要
可信度是基于大语言模型（LLM）构建的智能体 AI 系统面临的核心研究挑战。为了增强信任，通常通过检索外部知识并使用 LLM 验证声明与检索证据的一致性，来检查来自不同来源（包括人类编写的文本、网络内容和模型输出）的自然语言声明的真实性。因此，此类方法受限于检索错误和外部数据的可用性，同时在很大程度上未利用模型内在的事实验证能力。我们提出了无需检索的事实核查任务，重点是验证任意来源的自然语言声明。为了研究这一设定，我们引入了一个专注于泛化能力的综合评估框架，测试了对以下方面的鲁棒性：(i) 长尾知识，(ii) 声明来源的多样性，(iii) 多语言能力，以及 (iv) 长文本生成。在 9 个数据集、18 种方法和 3 个模型上的实验表明，与利用模型内部表示的方法相比，基于 logit 的方法通常表现不佳。基于这一发现，我们引入了 INTRA，这是一种利用内部表示之间相互作用的方法，实现了具有强泛化能力的 SOTA 性能。更广泛地说，我们的工作确立了无需检索的事实核查作为一个有前景的研究方向，它可以补充基于检索的框架，提高可扩展性，并使此类系统能够作为训练期间的奖励信号或集成到生成过程中的组件。

## Abstract
Trustworthiness is a core research challenge for agentic AI systems built on Large Language Models (LLMs). To enhance trust, natural language claims from diverse sources, including human-written text, web content, and model outputs, are commonly checked for factuality by retrieving external knowledge and using an LLM to verify the faithfulness of claims to the retrieved evidence. As a result, such methods are constrained by retrieval errors and external data availability, while leaving the models intrinsic fact-verification capabilities largely unused. We propose the task of fact-checking without retrieval, focusing on the verification of arbitrary natural language claims, independent of their source. To study this setting, we introduce a comprehensive evaluation framework focused on generalization, testing robustness to (i) long-tail knowledge, (ii) variation in claim sources, (iii) multilinguality, and (iv) long-form generation. Across 9 datasets, 18 methods and 3 models, our experiments indicate that logit-based approaches often underperform compared to those that leverage internal model representations. Building on this finding, we introduce INTRA, a method that exploits interactions between internal representations and achieves state-of-the-art performance with strong generalization. More broadly, our work establishes fact-checking without retrieval as a promising research direction that can complement retrieval-based frameworks, improve scalability, and enable the use of such systems as reward signals during training or as components integrated into the generation process.

---

## 论文详细总结（自动生成）

这是一份关于论文《Leveraging LLM Parametric Knowledge for Fact Checking without Retrieval》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前大语言模型（LLM）的事实核查主要依赖检索增强（RAG）管道。这种方式存在三大缺陷：(1) 高延迟；(2) 严重依赖外部数据库的质量和覆盖范围；(3) 忽略了 LLM 预训练中习得的庞大“参数化知识”。
*   **核心问题**：论文探讨了**无需检索的事实核查（Fact-checking without retrieval）**任务，即仅通过分析 LLM 处理声明时的内部信号（如隐藏层状态、注意力模式），来判断一个自然语言声明的真实性，而非检查其与外部证据的忠实度。

### 2. 方法论：INTRA (Intrinsic Truthfulness Assessment)
*   **核心思想**：整合模型内部多层表示的交互信息，通过轻量级监督学习构建一个泛化能力强的事实检测器。
*   **关键技术细节**：
    *   **Token 与层选择**：不局限于首尾 Token，而是利用序列中所有 Token 的隐藏状态。通过一个可学习的注意力池化（Attention Pooling）机制，将序列级的隐藏状态聚合为嵌入向量。
    *   **层级评分（Layer-wise Scoring）**：在模型的每一层（通常选择中间层）之上训练线性分类器，预测该层视角下的真实性概率。
    *   **跨层聚合**：论文发现不同任务的最优层不同。INTRA 提取中间层（如 Llama 3.1 的 11-22 层）的预测结果，进行**分位数归一化（Quantile Normalization）**以标准化不同层的分值，最后通过 L2 回归模型输出最终的真实性得分。
*   **算法流程**：输入声明 -> 提取中间层隐藏状态 -> 注意力池化 -> 层级分类 -> 分位数归一化 -> 线性回归聚合 -> 输出真实性概率。

### 3. 实验设计
*   **数据集/场景**：构建了包含 9 个数据集的综合评估框架，涵盖五个维度：
    1.  **长尾知识**：AC-PopQA（基于实体流行度）。
    2.  **声明来源**：AVeriTeC（人类编写）、Common Claims（GPT-3 生成）。
    3.  **多语言**：X-Fact（涵盖 25 种语言）。
    4.  **长文本生成**：AC-WH（Wild Hallucinations）、UHead。
    5.  **规则生成**：Cities、Companies、CounterFact。
*   **对比方法**：共对比了 18 种方法，包括：
    *   **无监督方法**：序列概率（SP）、困惑度（PPL）、口头化评分（Verb，即让模型直接回答真假）。
    *   **监督方法**：SAPLMA、MIND、CCS（对比一致性搜索）、SATRMD 等。
    *   **基准（Benchmark）**：以 GPT-4.1 的表现作为参考，并对比了带检索的 Verb+RAG 方法。
*   **基础模型**：Llama 3.1-8B、Ministral 8B、Phi-4-mini。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA A100 GPU** 上运行。
*   **效率表现**：INTRA 的推理时间约为 **56ms/实例**，相比之下，带检索的 Verb+RAG 需要约 950ms。INTRA 的计算开销比检索方法低约 20 倍。
*   **训练细节**：论文未详细给出总训练时长，但强调其分类器和回归层是“轻量级”的，仅需在 PopQA 的训练集上进行微调。

### 5. 实验数量与充分性
*   **实验规模**：跨越 3 个模型、9 个数据集、18 种方法，并针对流行度、语言、声明位置进行了细分分析，实验矩阵非常庞大。
*   **充分性**：
    *   **泛化性测试**：在 PopQA 上训练，在其余 8 个完全不同领域的数据集上测试，验证了 OOD（分布外）性能。
    *   **统计严谨性**：使用了 ROC-AUC 和 PR-AUC 指标，并通过 2000 次 Bootstrap 重采样进行显著性检验（95% 置信水平）。
    *   **消融实验**：对层选择范围（如 0-8 层 vs 11-22 层）进行了详细对比。

### 6. 主要结论与发现
*   **内部表示优于概率**：利用模型内部隐藏状态的方法（如 INTRA）在准确率和鲁棒性上显著优于仅依赖输出 Logit（如 PPL、SP）的方法。
*   **中间层最关键**：实验证实 LLM 的中间层包含最丰富的事实性信号，首尾层效果较差。
*   **长尾优势**：在处理罕见实体（低流行度）时，INTRA 的提升最为显著，而基于概率的方法在长尾数据上极易失效。
*   **多语言挑战**：虽然内部信号有效，但在非拉丁语系或低资源语言上，所有方法的表现仍有波动。

### 7. 优点
*   **高效性**：无需外部检索，极大地降低了事实核查的延迟和成本。
*   **强泛化性**：INTRA 在跨模型、跨领域任务中表现稳定，证明了其捕捉的是通用的“真实性”特征。
*   **互补性**：该方法可以作为 RAG 系统的补充，在检索失败或数据缺失时提供可靠的内在判断。

### 8. 不足与局限
*   **数据集污染风险**：论文指出一些规则生成的旧数据集（如 Cities）已接近饱和，可能存在训练数据污染，导致某些基准方法表现虚高。
*   **小语种支持有限**：在 X-Fact 的多语言测试中，部分语言由于样本量太少，无法得出统计学上可靠的结论。
*   **依赖特定训练**：虽然是轻量级的，但 INTRA 仍需要一定量的标注数据来拟合层级权重，无法像无监督方法那样“开箱即用”。

（完）
